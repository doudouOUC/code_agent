# qwen-code PRs · 2026-08-03 ~ 2026-08-09 (W32 周内累计)

> 本文件已整理 2026-08-03 至 2026-08-08（Asia/Shanghai）创建的 @doudouOUC 个人 PR。口径为 `QwenLM/qwen-code` 中 author 为 @doudouOUC 且 createdAt 落在对应北京时间日窗口内的 PR；只在窗口内更新、关闭或合入，但创建时间不在窗口内的 PR 不计入新增统计。open PR 只记录当前 diff 方案，不能视为 `main` 已落地能力。

**主题**: MCP unsafe replay guard、WebUI live journal truncation recovery、caller-supplied session ID admission、daemon memory pressure observation、write_file prior-read prompt guidance、ACP tool-result text projection、daemon active ACP child RSS aggregation、tool-result microcompaction low watermark、ACP repeated tool execution failure guard、Direct External Context Mem0 write variant、daemon child heap partition modeling、REST SSE stream/client observability、daemon activeWork health state、same-host daemon text read delegation、read permission symlink canonicalization、safe session restore timeout observability、integration-tests typecheckability、selective session restore design

**PR 统计**: 18 PRs - 14 merged / 4 open / 0 closed
**当前已合并 PR 代码量**: +16,919 / -920，231 个文件变更
**全量代码量**: +27,825 / -2,095，356 个文件变更
**类型分布**: fix ×9, feat ×6, docs ×1, perf ×1, refactor ×1
**范围 (scope)**: serve ×7, core ×4, acp ×1, cli ×1, daemon ×1, design ×1, external-context ×1, integration-tests ×1, webui ×1

---

## PR 明细

| PR | 状态 | 作者 | 标题 | 变更 | 文件 | 创建(UTC) | 合并/关闭(UTC) |
|---|---|---|---|---:|---:|---|---|
| [#8387](https://github.com/QwenLM/qwen-code/pull/8387) | ✅ merged | @doudouOUC | fix(core): Avoid replaying unsafe MCP tool calls | +492/-21 | 7 | 08-02 16:21 | 08-03 11:04 |
| [#8414](https://github.com/QwenLM/qwen-code/pull/8414) | ✅ merged | @doudouOUC | fix(webui): recover complete turns after live journal truncation | +2451/-166 | 17 | 08-03 03:02 | 08-05 02:51 |
| [#8415](https://github.com/QwenLM/qwen-code/pull/8415) | 🟡 open | @doudouOUC | fix(serve): Coordinate caller-supplied session IDs | +3781/-724 | 47 | 08-03 03:08 | — |
| [#8423](https://github.com/QwenLM/qwen-code/pull/8423) | ✅ merged | @doudouOUC | feat(serve): observe daemon and child memory against real denominators | +2121/-78 | 30 | 08-03 04:29 | 08-07 06:10 |
| [#8428](https://github.com/QwenLM/qwen-code/pull/8428) | ✅ merged | @doudouOUC | fix(core): Clarify write_file prior-read guidance | +134/-1 | 5 | 08-03 06:00 | 08-03 10:36 |
| [#8450](https://github.com/QwenLM/qwen-code/pull/8450) | ✅ merged | @doudouOUC | fix(cli): Bound ACP textual tool-result payloads | +1169/-4 | 9 | 08-03 09:51 | 08-05 15:11 |
| [#8462](https://github.com/QwenLM/qwen-code/pull/8462) | ✅ merged | @doudouOUC | feat(serve): report aggregate ACP child RSS, not just the primary's | +350/-38 | 12 | 08-03 13:47 | 08-03 14:01 |
| [#8464](https://github.com/QwenLM/qwen-code/pull/8464) | ✅ merged | @doudouOUC | perf(core): clear tool results to a low watermark to preserve prompt cache | +649/-33 | 7 | 08-03 13:52 | 08-04 19:00 |
| [#8469](https://github.com/QwenLM/qwen-code/pull/8469) | 🟡 open draft | @doudouOUC | feat(acp): Protect against repeated tool execution failures | +2744/-63 | 24 | 08-03 15:25 | — |
| [#8507](https://github.com/QwenLM/qwen-code/pull/8507) | ✅ merged | @doudouOUC | feat(external-context): Add optional Mem0 memory writes | +3026/-94 | 36 | 08-04 03:12 | 08-07 16:52 |
| [#8508](https://github.com/QwenLM/qwen-code/pull/8508) | ✅ merged | @doudouOUC | refactor(serve): model a per-child heap partition of the daemon budget | +625/-42 | 24 | 08-04 03:36 | 08-04 12:32 |
| [#8572](https://github.com/QwenLM/qwen-code/pull/8572) | ✅ merged | @doudouOUC | feat(daemon): Add SSE stream and client observability | +1974/-159 | 23 | 08-05 08:17 | 08-06 16:44 |
| [#8588](https://github.com/QwenLM/qwen-code/pull/8588) | ✅ merged | @doudouOUC | feat(serve): Expose active work state | +2650/-122 | 20 | 08-05 13:07 | 08-08 06:34 |
| [#8620](https://github.com/QwenLM/qwen-code/pull/8620) | ✅ merged | @doudouOUC | fix(serve): Allow approved same-host text reads outside workspace | +533/-32 | 17 | 08-06 02:48 | 08-07 07:33 |
| [#8636](https://github.com/QwenLM/qwen-code/pull/8636) | ✅ merged | @doudouOUC | fix(core): Resolve symlinks before granting read permission | +429/-69 | 5 | 08-06 09:42 | 08-06 12:12 |
| [#8691](https://github.com/QwenLM/qwen-code/pull/8691) | 🟡 open | @doudouOUC | fix(serve): Make session restore timeouts safe and observable | +3672/-388 | 52 | 08-07 13:14 | — |
| [#8693](https://github.com/QwenLM/qwen-code/pull/8693) | ✅ merged | @doudouOUC | fix(integration-tests): make the project typecheckable and fix what that found | +316/-61 | 19 | 08-07 13:16 | 08-08 16:32 |
| [#8743](https://github.com/QwenLM/qwen-code/pull/8743) | 🟡 open draft | @doudouOUC | docs(design): Plan selective session restore | +709/-0 | 2 | 08-08 14:37 | — |

---

## PR 解决问题、实现方式与 feature 处理

| PR | 解决了什么问题 | 最终怎么实现（open 只登记当前观察） | 对应 feature 文档 |
|---|---|---|---|
| [#8387](https://github.com/QwenLM/qwen-code/pull/8387) | MCP 连接在工具执行后断开时，旧逻辑可能自动重连并重放同一次工具调用；如果工具不是明确幂等或只读，自动 replay 可能重复写入、重复发起外部动作或掩盖真实执行结果。 | 最终实现新增安全 replay 判定：仅在 trusted server、trusted workspace、工具 annotations 存在且声明 `idempotentHint:true` 或无冲突 `readOnlyHint:true` 时自动重试；连接恢复后还会用 rediscovered tool 重新校验 trust、workspace trust 与 annotations。其余连接丢失统一返回固定 unsafe replay 错误，提示不要自动重试。 | 已更新 daemon MCP guardrail 口径。完整实现见 [implementations/pr-8387.md](implementations/pr-8387.md)。 |
| [#8414](https://github.com/QwenLM/qwen-code/pull/8414) | WebUI live journal 环形缓冲被截断后，用户可能只能看到 `history_truncated` marker 和残缺 turn；仅依赖 replay event 会丢失同一 turn 的完整输出，影响长输出、慢客户端或移动端恢复体验。 | 最终实现让 daemon 在 live-journal truncation marker 中带上 `promptId`，WebUI 在看到 marker 后先保持当前 UI，不立即破坏 transcript；等同一 prompt 的 terminal 到达后，用一次 same-session load 拉取 memory replay，校验目标 user input 与 terminal，再用重建出的 suffix 原子替换 marker 后的残缺尾部。repair 失败只提示一次并继续消费 live SSE。 | 已更新 daemon WebUI/transport 口径。完整实现见 [implementations/pr-8414.md](implementations/pr-8414.md)。 |
| [#8415](https://github.com/QwenLM/qwen-code/pull/8415) | 外部调用方希望指定会话 ID 来把 REST/ACP/SDK 调用与自己的 thread 对齐，但未协调的 caller-supplied session id 会与 live session、pending create、archive/history 或其它 workspace 冲突，甚至产生 orphan session。 | 当前 open diff 新增 `session_id_override` capability、严格 UUID v1-v5 lowercase validator、daemon-wide requested-session admission 和 refcounted pending claim。REST `POST /session {sessionId}`、ACP `_meta["qwen-code/sessionId"]`、TS/Java/MCP SDK 均先 gate capability，再验证 response 是否 honor 请求；冲突、历史已存在和 restore 竞争均返回 typed error 并清理 orphan。 | 已更新 daemon session lifecycle、protocol 与 SDK 口径。当前实现见 [implementations/pr-8415.md](implementations/pr-8415.md)。 |
| [#8423](https://github.com/QwenLM/qwen-code/pull/8423) | daemon status 已有内存绝对值和预算模型，但没有用真实 cgroup/host/heap denominator 计算压力等级，操作者无法判断当前 daemon root 离可用内存耗尽还有多近。 | 最终实现新增 `--memory-pressure-mode off|observe`，默认 observe；status 中报告 `runtime.memory.pressure`，ratio 取 daemon root RSS / available memory 与 V8 heap used / heap limit 的较大值，并标注 source、level 与原始字节数。observe 模式只产生 warning，不做 admission 或进程杀停。 | 已更新 daemon resource budgeting 口径。完整实现见 [implementations/pr-8423.md](implementations/pr-8423.md)。 |
| [#8428](https://github.com/QwenLM/qwen-code/pull/8428) | `write_file` prompt guidance 仍容易被模型理解成“创建/生成文件可直接写目标路径”；在 prior-read enforcement 打开时，这类 blind overwrite 会被拒绝，也可能在未确认文件状态时覆盖已有内容。 | 最终实现直接收紧 `WriteFileTool` 描述和系统 prompt 示例：除非本轮会话已经确认目标不存在或读过当前内容，否则创建/生成目标文件前必须先 `read_file`；示例改为先读目标 test 文件，读到不存在后再写。测试用 snapshot 和 write-file 单测锁住描述与示例顺序。 | 已更新原子文件写/写文件口径。完整实现见 [implementations/pr-8428.md](implementations/pr-8428.md)。 |
| [#8450](https://github.com/QwenLM/qwen-code/pull/8450) | ACP transport 会把工具结果文本投影给 UI/live replay；超大 textual payload 即使不进入模型，也会撑大 ACP update、history replay 或 virtual subagent replay，拖慢 WebUI/IDE 并放大内存压力。 | 最终实现是在 ACP transport boundary 增加 65,536 byte JSON 预算，只裁剪 canonical text blocks 的 `content` 和 string `rawOutput`，保留 A2UI、structured diff、terminal/media/mixed payload、canonical transcript、model-facing response 与 offline export。超限文本按 UTF-8 JSON byte 预算保留 head/tail 和固定 marker，live、history replay、virtual subagent replay 共用同一投影函数。 | 已更新 tool response budget / ACP transport 口径。完整实现见 [implementations/pr-8450.md](implementations/pr-8450.md)。 |
| [#8462](https://github.com/QwenLM/qwen-code/pull/8462) | #8423/#8245 只能报告 daemon root 和 primary ACP child RSS；多 workspace 或多 active ACP child 时，status 会低估总子进程内存占用，操作者看不出是否有未采样 child。 | 最终实现把 status 的 `runtime.memory.childRssCoverage` 扩展为 `active_children`，新增 `runtime.memory.children`，同步遍历所有 live managed runtime child 并汇总已缓存 RSS，报告 `rssBytes`、`sampled` 和 `oldestReadingAgeMs`。它只做观测，不把 child aggregate 折入 memory pressure ratio。 | 已更新 daemon resource budgeting 口径。完整实现见 [implementations/pr-8462.md](implementations/pr-8462.md)。 |
| [#8464](https://github.com/QwenLM/qwen-code/pull/8464) | active tool-result history budget 在刚超过阈值时只清到低于阈值，长会话会频繁小幅 compaction，破坏 prompt cache locality；pending batch 还可能错误消耗 committed history 的 keep-recent slots。 | 最终实现把 size-triggered microcompaction 目标改为 `threshold / 2` 低水位，触发仍是超过阈值，`-1` disable 不变。pending result 继续计入虚拟总量但不再占 committed keep-recent 槽；metadata/debug log 暴露 low watermark 和 soft-exceeded 情况。 | 已更新 context compression 口径。完整实现见 [implementations/pr-8464.md](implementations/pr-8464.md)。 |
| [#8469](https://github.com/QwenLM/qwen-code/pull/8469) | ACP 前台工具如果反复以同一种执行错误失败，模型可能不断重试同一工具，浪费 turn、打满日志并延迟用户可见失败；现有 stop guard 只覆盖 Todo 等特定模式，缺少基于 execution outcome 的通用保护。 | 当前 draft diff 新增 prompt-local repeated tool execution failure guard，默认 `shadow`。它只统计完全 settle 的前台 ACP batch 中真实进入 execution 后的 terminal error，key 为 `(policyToolName, executionErrorType)`；达到 8 次且跨至少 2 个 batch 后在 warn/enforce 模式注入固定纠偏提醒，再次匹配时 enforce 可停止自动续跑并关闭 Todo continuation，直到新用户输入重置。telemetry 只记录低基数字段，不采集参数、输出、路径或 MCP server 名。 | 已更新 daemon ACP/telemetry 口径。当前实现见 [implementations/pr-8469.md](implementations/pr-8469.md)。 |
| [#8507](https://github.com/QwenLM/qwen-code/pull/8507) | Direct External Context 已有只读检索和 submitted-prompt auto recall，但可信团队没有一个窄范围、管理员绑定 provider/app_id、内容可见确认的 Mem0 记忆写入入口；直接暴露 Mem0 MCP 又会扩大管理面和写入语义。 | 最终实现只在严格 v1 Mem0 config 且 `write.enabled=true` 时注册非幂等 `context_remember({content})`，把已确认内容原样作为一条 Mem0 V3 Direct Import user message 发送，固定 `app_id` 与 `infer:false`。写路径不预搜索、不规范化、不重试、不轮询、不缓存、不去重；`SUCCEEDED` 映射 `stored`，带有效 UUID 的 `PENDING` 映射 `accepted`，超时/取消/坏 JSON/redirect/非法状态等不确定结果映射为 `unknown` MCP 错误并明确禁止自动 retry。专用 `PreToolUse` Hook 展示完整可逆转义内容并二次确认，默认 approval 下仍先走普通 MCP prompt，YOLO 下也保留内容确认。 | 已更新 Direct External Context 口径。完整实现见 [implementations/pr-8507.md](implementations/pr-8507.md)。 |
| [#8508](https://github.com/QwenLM/qwen-code/pull/8508) | `getAcpMemoryArgs()` 把 host-derived heap ceiling 原样发给每个 ACP child，最多 25 个 workspace 会把 32GB 主机建模成 25×16GB 的子进程堆授权；早期按派生时刻递减 share 的方案又不能约束已运行 child，且零池时把 `--max-old-space-size=0` 误当作零上限。 | 最终实现只建模、不应用：新增 `--child-heap-mode off|observe`（默认 observe），status 报告 `limits.memory.childHeap.mode/maxConcurrentChildren/perChildCeilingMb/refusals`。模型给每个 child 一个恒定 ceiling，并让 `maxConcurrentChildren * perChildCeilingMb <= modeled.childPoolMb`；容不下一个 512MB floor child 时返回 `maxConcurrentChildren:0`、`perChildCeilingMb:null`。spawn argv 保持 host-derived ceiling，`limits.memory.enforced` 仍是字面量 `false`，`enforce` flag、`ChildHeapPoolExhaustedError` 和应用路径均被删除。 | 已更新 daemon resource budgeting 口径。完整实现见 [implementations/pr-8508.md](implementations/pr-8508.md)。 |
| [#8572](https://github.com/QwenLM/qwen-code/pull/8572) | session id 只能标识逻辑会话，无法区分重连产生的每条物理 REST SSE stream；现有 slow-client warning、resync 和 close 日志难以关联具体 stream、前驱、队列状态、背压 episode 或关闭原因。 | 最终实现给每条成功接受的 REST SSE stream 分配稳定 UUID，打开、slow-client warning、eviction、state resync 和关闭都会输出带 stream/client/session 关联的 telemetry 与 daemon log。close 记录 duration、settled frame count、last written event id、backpressure/live-lag stats、terminal attribution 和 close reason。TS SDK 发送可选 connection reason 与 predecessor accepted-stream lineage，WebUI 只在能区分 prompt restart、normal end、transport error 或 resync 时填 reason；所有字段 optional，旧 daemon/header 被网关剥离时保持兼容，诊断不包含 payload 或 auth data。 | 已更新 daemon SSE/SDK/WebUI/telemetry 口径。完整实现见 [implementations/pr-8572.md](implementations/pr-8572.md)。 |
| [#8588](https://github.com/QwenLM/qwen-code/pull/8588) | `activePrompts` 在主 prompt settle 后会降为 0，即使该 prompt 启动的后台 Agent 仍在运行或终态通知尚未写回父 session；重启控制器若只看 active prompt，可能过早重启 daemon。 | 最终实现给 `GET /health?deep=1` 增加 additive `activeWork`。任一 managed/draining workspace 中存在 accepted-but-unsettled prompt、running background Agent、或正在被接收/排队/持久化/父 continuation 处理的 Agent terminal notification 时为 true。daemon 与 ACP child 通过初始化 `_meta` 私有版本能力协商，支持的 session 串行发布 active-work transitions 并在 active 期间 15s heartbeat；bridge 校验 session ownership 与单调序列，单 session 45s 缺预期上报会回收 owning ACP channel。自动 detach cleanup、prompt settle cleanup、attach rollback 和 idle reap 都保留 active-work session；显式 close/kill/shutdown/channel-exit 保持强制语义。 | 已更新 daemon health/lifecycle 口径。完整实现见 [implementations/pr-8588.md](implementations/pr-8588.md)。 |
| [#8620](https://github.com/QwenLM/qwen-code/pull/8620) | same-host `qwen serve` 的 ACP text read 仍委派给 daemon `WorkspaceFileSystem`，导致用户批准后也无法读取 workspace 外的普通文本；同时 direct `read_file` 与 write/edit 的 prior read 共用该委派路径，逼迫模型退回 shell 读文件。 | 最终实现为 daemon-owned same-host bridge 新增 `delegateReadTextFileToClient:false`，让子进程广告 `readTextFile:false, writeTextFile:true`：直接 `read_file`、write/edit/notebook/sed/artifact pre-read 都回到子进程本地 `FileSystemService` 和普通 CLI 权限；最终 text write 仍委派到 `WorkspaceFileSystem`，继续保留 workspace/trust/symlink/atomic write/audit 边界。默认 embedded、primary、static secondary、dynamic runtime 四条 daemon 构造路径统一接线，通用 ACP 与 caller-injected bridge 仍保留默认委派读；WFS read adapter 仅作为异常/capability-violating 读请求的 fail-closed fallback。 | 已更新 daemon FS 边界与 ACP bridge 口径。完整实现见 [implementations/pr-8620.md](implementations/pr-8620.md)。 |
| [#8636](https://github.com/QwenLM/qwen-code/pull/8636) | file read 默认权限会按原始路径判断 always-allow roots；攻击者可在 agent 可写目录里放 symlink 指向任意宿主文件，让 `read_file` 绕过 ask 直接读取外部路径。 | 最终实现先用 `realpathNearestExisting(path.resolve(requestedPath))` canonicalize 被读路径，再与 canonicalized always-allow roots 比较；managed memory 分支只 canonicalize candidate，memory root 保持 lexical，避免 repo 内 `.qwen/` symlink 把受管边界搬走。`realpathNearestExisting` 被提升到 shared utils，支持不存在尾段、悬挂 symlink、最多 40 跳并不抛错；新增 symlink/dangling/root-link/Windows skip 测试。 | 已更新权限系统文件读取默认权限口径。完整实现见 [implementations/pr-8636.md](implementations/pr-8636.md)。 |
| [#8691](https://github.com/QwenLM/qwen-code/pull/8691) | 大型 persisted JSONL 会话在 load/resume restore 时可能超过旧的 10s 初始化超时；等待方超时后 ACP child 仍在恢复，可能误杀带有 sibling session 的 channel、留下 late writer lease/orphan session，且 HTTP/ACP/SDK/WebUI 只能看到泛化 init timeout。 | 当前 open diff 增加专用 session restore deadline：server 解析 `--session-restore-timeout-ms` 并在 `/capabilities.limits.sessionRestoreTimeoutMs` 公告，SDK/WebUI 用 server budget 派生 restore request timeout/watchdog；显式 `sessionRestoreTimeoutMs` 即使低于默认值也优先，未显式设置时仅让大于 60s 的 `initializeTimeoutMs` 抬高 restore budget。bridge 将 restore in-flight 拆成 public timeout promise 与真实 settlement promise；public 超时返回 retryable 504/JSON-RPC `restore_timeout`，保留 admission/capacity 到实际 ACP settle，再执行 exactly-once session close cleanup。清理/settlement 不确定时 quarantine channel 或返回 `acp_channel_unavailable`，reason 可区分 `restore_cleanup_failed`、`awaiting_abandoned_cleanup`、`restore_settlement_overdue`。restore span、prompt trace context、late result、cleanup/quarantine 结果和 child stage timing 全部低敏记录。 | 已更新 daemon session lifecycle、protocol/capabilities、HTTP/ACP error、SDK/WebUI restore timeout 与 telemetry 口径。当前实现见 [implementations/pr-8691.md](implementations/pr-8691.md)。 |
| [#8693](https://github.com/QwenLM/qwen-code/pull/8693) | `integration-tests/tsconfig.json` 因 `paths` 中的 `"//"` 字符串注释和 inherited composite 设置无法 typecheck；integration tests 不是 workspace，CI workspace typecheck 覆盖不到它，导致未声明变量、错误 helper 类型和 scenario config 放错层等问题被长期隐藏。 | 最终实现修正 integration-tests tsconfig：移除非法 paths 注释、关闭 composite、补 DOM lib、通过 paths 指向 core/sdk/acp-bridge/webui/channel package source，关闭 index-signature 点访问限制，并为 `@lydell/node-pty` 指向仓库类型文件；新增 `jsdom` / `@types/jsdom`，同时让 `integrations/external-context/tsconfig.json` 覆盖 root vitest globals/types，避免 DOM/jsdom 类型泄漏。随后按真实 TS 约束修复 helper 类型、channel plugin import、streaming union narrowing、terminal-bench/env 读取、`write_file` 断言 message 和 terminal-capture `gif:true` 配置层级。该 PR 只补测试工程与类型覆盖，不新增用户可见 feature。 | 不新增 feature 文档；已在本周实现文档中记录测试基础设施最终实现。完整实现见 [implementations/pr-8693.md](implementations/pr-8693.md)。 |
| [#8743](https://github.com/QwenLM/qwen-code/pull/8743) | 当前 session restore 在 daemon 路径会在 `loadCliConfig()` 和 `Config.initialize()` 两次完整 materialize persisted transcript，再在 replay 阶段才受 `historyPageSize` 限制；大型会话即便只请求最近页面，也要承担重复全量读取和内存峰值。 | 当前 draft 只新增设计和计划文档，不改运行时代码。方案定义 daemon 内部 selective restore projection：restore 阶段先构建 fresh transcript index，再按 consumer 需求读取 runtime resume state、recent/all/none replay page 或 artifact-only projection；`runtimeUuids` 与 `replayUuids` 分离，保留压缩、token/telemetry、attribution、file history、artifact、goal、background task 等 metadata hints。recent replay 复用 transcript pager 的 backward selector，并有 count、4MiB source byte budget、16MiB expansion ceiling、`hasMore`/anchor/partial/error 语义。 | 已在 daemon session lifecycle / roadmap 中登记为 #8691 之后、runtime 未落地的 selective restore 设计方案。当前设计见 [implementations/pr-8743.md](implementations/pr-8743.md)。 |

## PR 对应 feature 覆盖

| feature 文档 | 本周新增/复核 PR | 文档动作 |
|---|---|---|
| [daemon-serve-mode/](../../feature/daemon-serve-mode/) | #8387 / #8414 / #8415 / #8423 / #8450 / #8462 / #8469(draft open) / #8508 / #8572 / #8588 / #8620 / #8691(open) / #8743(draft open) | 补 MCP unsafe replay guard、WebUI live journal repair、caller-supplied session id admission、memory pressure / active child RSS status、ACP tool-result transport projection、repeated execution failure guard、child heap partition status model、SSE stream observability、activeWork deep-health/lifecycle、same-host daemon text read delegation、safe session restore timeout/quarantine/error mapping，以及 selective session restore projection 设计口径。 |
| [sdk.md](../../feature/sdk.md) / [daemon-serve-mode/10-client-adapters-and-sdk.md](../../feature/daemon-serve-mode/10-client-adapters-and-sdk.md) | #8415 / #8572 / #8691(open) | 补 TS/Java/MCP SDK 的 `sessionId` override capability gate、response verification 与 typed conflict errors，REST SSE stream id、connection reason、lineage 与旧 daemon fallback，以及 TS daemon SDK restore timeout derivation/fallback/order。 |
| [atomic-file-write.md](../../feature/atomic-file-write.md) | #8428 | 补 `write_file` prior-read guidance 与 blind-overwrite enforcement 的提示边界。 |
| [tool-response-budget.md](../../feature/tool-response-budget.md) | #8450 | 补 ACP transport-only textual projection 与 model-facing finalizer 的边界差异。 |
| [context-compression.md](../../feature/context-compression.md) | #8464 | 补 active tool-result microcompaction 低水位目标和 pending result keep-recent 修正。 |
| [telemetry-observability/](../../feature/telemetry-observability/) | #8469(draft open) / #8572 / #8691(open) / #8743(draft open) | 补 repeated tool execution failure guard telemetry 的低基数字段与隐私边界，REST SSE stream lifecycle / slow-client / resync / close telemetry、session restore timeout/late-result/cleanup/quarantine/stage timing 低敏观测，以及 selective restore 设计中的未来 restore stage telemetry 口径。 |
| [external-context-provider.md](../../feature/external-context-provider.md) | #8507 | 补可选 Mem0 write variant、`context_remember` 非幂等写入语义、内容可见确认 Hook 与不确定结果禁止自动重试边界。 |
| [daemon-serve-mode/05-workspace-files-and-fs-boundary.md](../../feature/daemon-serve-mode/05-workspace-files-and-fs-boundary.md) / [07-acp-bridge-and-permission.md](../../feature/daemon-serve-mode/07-acp-bridge-and-permission.md) | #8620 | 补 same-host daemon-owned bridge 中 local text read 与 delegated text write 的能力分离、WFS read fallback 和剩余安全/审计边界。 |
| [daemon-serve-mode/01-http-server-and-middleware.md](../../feature/daemon-serve-mode/01-http-server-and-middleware.md) / [02-sse-event-bus.md](../../feature/daemon-serve-mode/02-sse-event-bus.md) / [03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md) / [04-capabilities-and-protocol.md](../../feature/daemon-serve-mode/04-capabilities-and-protocol.md) / [09-roadmap-coverage-and-gaps.md](../../feature/daemon-serve-mode/09-roadmap-coverage-and-gaps.md) / [11-webui-and-transport.md](../../feature/daemon-serve-mode/11-webui-and-transport.md) | #8572 / #8588 / #8691(open) / #8743(draft open) | 补 REST SSE stream lifecycle diagnostics、deep-health `activeWork` additive 字段、active work lifecycle gate、safe session restore timeout、restore error/quarantine protocol、active work/restore timeout 不等于真实 liveness 的限制，以及 selective restore 与 #8678/#8691/transactional WebUI/durable checkpoint 的排序边界。 |
| [permission-system.md](../../feature/permission-system.md) | #8636 | 补 read-file 默认权限在 always-allow roots 前 canonicalize symlink 的防绕过口径。 |
| 集成测试工程 | #8693 | 仅整理实现文档；该 PR 修复 integration-tests typecheck 与测试配置，不新增或调整 feature 技术方案。 |

_周内累计按个人 PR 口径更新于 2026-08-08_
