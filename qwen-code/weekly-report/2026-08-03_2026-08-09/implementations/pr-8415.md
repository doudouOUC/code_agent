# PR #8415 · fix(serve): Coordinate caller-supplied session IDs

链接：https://github.com/QwenLM/qwen-code/pull/8415
状态：merged
规模：+3829 / -724，48 个文件

## 解决了什么问题

REST、ACP、SDK 调用方需要把 qwen-code session 与自己的 thread/session 体系对齐，因此希望传入 caller-supplied session id。直接接受该字段会带来多类冲突：与 live session 重名、与同一时间 pending create 抢占、与 archived/history session 重叠、跨 workspace 恢复错位，或者 ACP child 实际创建了另一个 id 但调用方误以为请求被 honor。

## 最终实现方案

PR 新增共享 session id validator 和 `session_id_override` capability。caller-supplied ID 只能是 lowercase RFC UUID v1-v5，不允许 nil UUID、unsupported version/variant、Arena suffix、路径字符或非 string。内部 session id 仍可使用 Arena suffix，外部请求入口不接受。

daemon 侧新增 `RequestedSessionIdAdmission`。创建路径同步安装 pending claim，再检查 live bridges、draining/replaced bridge generation、registered workspace 的 active/archived/worktree history；restore 路径只允许同一 bridge generation 共享 in-flight recovery claim，其它 workspace 或 generation 继续返回 workspace conflict。Draining/replaced generation 在确认 shutdown 前仍参与冲突判定，runtime 级 persistence check 使用各 generation 固定的 output directory。claim release 绑定 identity，防止其它路径误释放。

协议层面，REST `POST /session {sessionId}` 与 ACP `session/new._meta["qwen-code/sessionId"]` 都强制 `sessionScope:"thread"`。bridge 返回后必须比较 actual id 与 requested id，不一致映射为 `session_id_not_honored` 并清理 orphan。Stdio ACP agent 在读取 settings/fs 前先验证 session id，同 ID startup 串行化，错误以 ACP INVALID_PARAMS 返回而不是直接退出进程。TS/Java/MCP SDK 都新增字段、capability gate 与 response verification。

## 关键代码路径

- `packages/cli/src/config/session-id.ts`：caller-supplied session id validation，锁定 lowercase RFC UUID v1-v5。
- `packages/cli/src/serve/session-id-admission.ts`：daemon-wide live/pending/persisted conflict admission 与 refcounted pending claim。
- `packages/cli/src/serve/server.ts` / `routes/session.ts` / `acp-http/dispatch.ts`：REST/ACP request field、capability gate、actual id verification 和 orphan cleanup。
- `packages/sdk-typescript/src/daemon/DaemonClient.ts` / `AcpWsTransport.ts` / daemon MCP serve bridge：TS SDK 与 MCP bridge 的 session id override surface、capability gate、response verification。
- `packages/sdk-java/qwencode/src/main/java/com/alibaba/qwen/code/daemon/*`：Java SDK request builder、daemon client 与测试覆盖。

## 验证方式

PR 声明已通过 CLI 定向 Vitest 730/730、ACP bridge Vitest 469/469、TypeScript SDK Vitest 477/477、Java Maven tests 127/127（5 个环境依赖 skip）、真实 bundle daemon route integration 36/36，以及 `npm run build`、`npm run bundle`、`npm run typecheck`、`npm run lint`。

## 对应 feature 文档

- `qwen-code/feature/daemon-serve-mode/03-session-lifecycle.md`
- `qwen-code/feature/daemon-serve-mode/04-capabilities-and-protocol.md`
- `qwen-code/feature/daemon-serve-mode/10-client-adapters-and-sdk.md`
- `qwen-code/feature/sdk.md`
