# PR #4393 最终实现文档

> 来源 PR: [feat(telemetry): propagate X-Qwen-Code-Session-Id on outbound LLM requests (part 2 of #4384)](https://github.com/QwenLM/qwen-code/pull/4393)
> 状态: ⬜ closed
> 变更规模: +533/-51，13 个文件
> 时间: created `05-21`；merged/closed `05-21`

## 解决的问题

未作为已落地实现；session-id 传播，因无 allowlist 隐私隐患被重构取代。

## 最终实现方案

代码实现所述（openai/anthropic fetch wrapper 注入 header，Gemini 静态 header 并文档化 staleness）。（关闭合理）— session-id 先合入 #4390 后又被 R4 整体移除→改到未来 `outboundCorrelation.*`；

## 审查结论

- 一致性: 代码实现所述（openai/anthropic fetch wrapper 注入 header，Gemini 静态 header 并文档化 staleness）。
- 描述准确性: 准确（含 Gemini 限制）。
- 正确性: （关闭合理）— session-id 先合入 #4390 后又被 R4 整体移除→改到未来 `outboundCorrelation.*`；其 `wrapFetchWithCorrelation` 仅以 `getTelemetryEnabled()` 为门、无 host allowlist，会把 session id 发往所有第三方 LLM endpoint（隐私问题，正是重构动因），代码已不在 main。
- 结论: 正确地被取代/关闭；无 allowlist 的隐私隐患推动了重构。

## 归档口径

- 归档周目录：`2026-05-18_2026-05-24`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
