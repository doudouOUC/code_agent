# PR #4417 最终实现文档

> 来源 PR: [feat(telemetry): Phase 4a — TTFT capture + GenAI semconv dual-emit (#3731)](https://github.com/QwenLM/qwen-code/pull/4417)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1190 / -2，7 个文件
> 时间: merged `2026-05-22T02:54:12Z`；closed `2026-05-22T02:54:12Z`

## 实现目标

围绕 `Phase 4a — TTFT capture + GenAI semconv dual-emit (#3731)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts、packages/core/src/core/loggingContentGenerator/streamContentDetection.ts、packages/core/src/telemetry/session-tracing.ts`，这些文件承载 `Phase 4a — TTFT capture + GenAI semconv dual-emit (#3731)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `hasUserVisibleContent、isUserVisiblePart`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `EVENT_API_RETRY、REQUEST_PREPARATION、NETWORK_LATENCY、RESPONSE_PROCESSING、TOKEN_PROCESSING`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('captures ttftMs on the first user-visible stream chunk (Phase 4a)', async () => {、it('forwards cachedInputTokens from usageMetadata to endLLMRequestSpan (Phase 4a)', async () => {、it('leaves ttftMs undefined when stream yields no user-visible chunks (Phase 4a)', async () => {、it('forwards cachedInputTokens to endLLMRequestSpan on non-stream success (Phase 4a)', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/telemetry-llm-request-timing-design.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/core/loggingContentGenerator/streamContentDetection.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/core/loggingContentGenerator/streamContentDetection.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- `docs/design/telemetry-llm-request-timing-design.md`

## 代码变更线索

### 新增或调整的实现符号
- `hasUserVisibleContent`
- `isUserVisiblePart`

### 接口、命令或环境变量
- `EVENT_API_RETRY`
- `REQUEST_PREPARATION`
- `NETWORK_LATENCY`
- `RESPONSE_PROCESSING`
- `TOKEN_PROCESSING`

### 测试用例线索
- `it('captures ttftMs on the first user-visible stream chunk (Phase 4a)', async () => {`
- `it('forwards cachedInputTokens from usageMetadata to endLLMRequestSpan (Phase 4a)', async () => {`
- `it('leaves ttftMs undefined when stream yields no user-visible chunks (Phase 4a)', async () => {`
- `it('forwards cachedInputTokens to endLLMRequestSpan on non-stream success (Phase 4a)', async () => {`
- `describe('hasUserVisibleContent', () => {`
- `it('returns true for non-empty text part', () => {`
- `it('returns false for empty text part', () => {`
- `it('returns true for functionCall part', () => {`
- `it('returns true for inlineData part', () => {`
- `it('returns true for executableCode part', () => {`

## 代码阅读结论

- 最终实现以 PR #4417 的合入代码为准，核心判断来自 7 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
