# PR #4410 最终实现文档

> 来源 PR: [feat(telemetry): Phase 3 — qwen-code.subagent span with concurrent isolation (#3731)](https://github.com/QwenLM/qwen-code/pull/4410)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +2469 / -111，15 个文件
> 时间: merged `2026-06-05T09:12:35Z`；closed `2026-06-05T09:12:35Z`

## 实现目标

围绕 `Phase 3 — qwen-code.subagent span with concurrent isolation (#3731)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/gemini.tsx、packages/cli/src/ui/components/InputPrompt.tsx、packages/cli/src/ui/components/background-view/BackgroundTasksDialog.tsx`，这些文件承载 `Phase 3 — qwen-code.subagent span with concurrent isolation (#3731)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `getCurrentAgentDepth、SPAN_SUBAGENT、BRIDGE_SKIP_EVENT_NAMES、SPAN_TTL_MS_DEFAULT、SPAN_TTL_MS_LONG、ttlFor、SubagentInvocationKind、SubagentStatus`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SPAN_TTL_MS、SPAN_TTL_MS_DEFAULT、SPAN_TTL_MS_LONG、EVENT_SUBAGENT_EXECUTION、SPAN_SUBAGENT、FORK_PLACEHOLDER_RESULT、NOOP_SPAN、SERVICE_NAME、BRIDGE_SKIP_EVENT_NAMES、LONG_TTL_SUBAGENT_KINDS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('agent-context (depth) — #3731 Phase 3', () => {、it('returns 0 outside any frame', () => {、it('top-level subagent has depth 0', async () => {、it('auto-increments per nesting: top=0, child=1, grandchild=2', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/telemetry-subagent-spans-design.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/gemini.tsx`
- `packages/cli/src/ui/components/InputPrompt.tsx`
- `packages/cli/src/ui/components/background-view/BackgroundTasksDialog.tsx`
- `packages/cli/src/ui/contexts/BackgroundTaskViewContext.tsx`
- `packages/core/src/agents/runtime/agent-context.ts`
- `packages/core/src/telemetry/constants.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/log-to-span-processor.ts`
- `packages/core/src/telemetry/session-tracing.ts`
- `packages/core/src/tools/agent/agent.ts`

### 测试与验证文件
- `packages/core/src/agents/runtime/agent-context.test.ts`
- `packages/core/src/telemetry/log-to-span-processor.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`
- `packages/core/src/tools/agent/agent.test.ts`

### 文档、配置与发布材料
- `docs/design/telemetry-subagent-spans-design.md`

## 代码变更线索

### 新增或调整的实现符号
- `getCurrentAgentDepth`
- `SPAN_SUBAGENT`
- `BRIDGE_SKIP_EVENT_NAMES`
- `SPAN_TTL_MS_DEFAULT`
- `SPAN_TTL_MS_LONG`
- `ttlFor`
- `SubagentInvocationKind`
- `SubagentStatus`
- `StartSubagentSpanOptions`
- `SubagentSpanMetadata`
- `startSubagentSpan`
- `runInSubagentSpanContext`

### 接口、命令或环境变量
- `SPAN_TTL_MS`
- `SPAN_TTL_MS_DEFAULT`
- `SPAN_TTL_MS_LONG`
- `EVENT_SUBAGENT_EXECUTION`
- `SPAN_SUBAGENT`
- `FORK_PLACEHOLDER_RESULT`
- `NOOP_SPAN`
- `SERVICE_NAME`
- `BRIDGE_SKIP_EVENT_NAMES`
- `LONG_TTL_SUBAGENT_KINDS`

### 测试用例线索
- `describe('agent-context (depth) — #3731 Phase 3', () => {`
- `it('returns 0 outside any frame', () => {`
- `it('top-level subagent has depth 0', async () => {`
- `it('auto-increments per nesting: top=0, child=1, grandchild=2', async () => {`
- `it('sibling subagents at the same nesting level both see the same depth', async () => {`
- `it('callers do not pass depth — it is computed from parent frame only', async () => {`
- `describe('bridge skip-list (#3731 Phase 3)', () => {`
- `it('skips qwen-code.subagent_execution events — no bridge span emitted', async () => {`
- `// log-to-span bridge does NOT emit a span for it (the native`
- `it('still bridges other events normally (e.g. qwen-code.tool_call)', async () => {`

## 代码阅读结论

- 最终实现以 PR #4410 的合入代码为准，核心判断来自 15 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
