# PR #4304 最终实现文档

> 来源 PR: [refactor(acp-bridge): lift BridgeOptions + introduce DaemonStatusProvider seam (#4175 PR 22b/2)](https://github.com/QwenLM/qwen-code/pull/4304)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +852 / -371，11 个文件
> 时间: merged `2026-05-19T01:27:38Z`；closed `2026-05-19T01:27:38Z`

## 实现目标

围绕 `lift BridgeOptions + introduce DaemonStatusProvider seam (#4175 PR 22b/2)` 做重构调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridgeOptions.ts、packages/acp-bridge/src/index.ts、packages/acp-bridge/src/status.ts`，这些文件承载 `lift BridgeOptions + introduce DaemonStatusProvider seam (#4175 PR 22b/2)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DaemonStatusProvider、BridgeOptions、ServePreflightCell、ServePreflightKind、ServeWorkspaceEnvStatus、REQUIRED_NODE_MAJOR、createDaemonStatusProvider、buildDaemonPreflightCells`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--acp、--event-ring-size、--serve、DEFAULT_RING_SIZE、REQUIRED_NODE_MAJOR、QWEN_CLI_ENTRY、STATUS_SCHEMA_VERSION、SERVE_、MAX_WORKSPACE_PATH_LENGTH、SKILL_PARSE_CODES`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('returns idle env envelope when statusProvider is omitted (Mode A fallback)', async () => {、it('returns empty daemon preflight cells when statusProvider is omitted (Mode A fallback)', async () => {、it('falls back to idle env envelope when statusProvider.getEnvStatus throws', async () => {、it('falls back to empty daemon cells when statusProvider.getDaemonPreflightCells throws', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `packages/acp-bridge/README.md、packages/acp-bridge/package.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridgeOptions.ts`
- `packages/acp-bridge/src/index.ts`
- `packages/acp-bridge/src/status.ts`
- `packages/cli/src/serve/daemonStatusProvider.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/server.ts`

### 测试与验证文件
- `packages/acp-bridge/src/status.test.ts`
- `packages/cli/src/serve/httpAcpBridge.test.ts`

### 文档、配置与发布材料
- `packages/acp-bridge/README.md`
- `packages/acp-bridge/package.json`

## 代码变更线索

### 新增或调整的实现符号
- `DaemonStatusProvider`
- `BridgeOptions`
- `ServePreflightCell`
- `ServePreflightKind`
- `ServeWorkspaceEnvStatus`
- `REQUIRED_NODE_MAJOR`
- `createDaemonStatusProvider`
- `buildDaemonPreflightCells`
- `Slot`
- `safeCheck`
- `createIdleEnvStatus`

### 接口、命令或环境变量
- `--acp`
- `--event-ring-size`
- `--serve`
- `DEFAULT_RING_SIZE`
- `REQUIRED_NODE_MAJOR`
- `QWEN_CLI_ENTRY`
- `STATUS_SCHEMA_VERSION`
- `SERVE_`
- `MAX_WORKSPACE_PATH_LENGTH`
- `SKILL_PARSE_CODES`
- `SKILL_FILE_CODES`

### 测试用例线索
- `it('returns idle env envelope when statusProvider is omitted (Mode A fallback)', async () => {`
- `it('returns empty daemon preflight cells when statusProvider is omitted (Mode A fallback)', async () => {`
- `it('falls back to idle env envelope when statusProvider.getEnvStatus throws', async () => {`
- `it('falls back to empty daemon cells when statusProvider.getDaemonPreflightCells throws', async () => {`
- `it('classifies SkillError via .name fallback when instanceof breaks across package boundaries (#4298 follow-up)', () => {`

## 代码阅读结论

- 最终实现以 PR #4304 的合入代码为准，核心判断来自 11 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
