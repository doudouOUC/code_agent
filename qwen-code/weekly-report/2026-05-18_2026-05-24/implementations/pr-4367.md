# PR #4367 最终实现文档

> 来源 PR: [feat(telemetry): support custom resource attributes and add metric cardinality controls](https://github.com/QwenLM/qwen-code/pull/4367)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1897 / -60，13 个文件
> 时间: merged `2026-05-21T05:54:37Z`；closed `2026-05-21T05:54:37Z`

## 实现目标

围绕 `support custom resource attributes and add metric cardinality controls` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/settingsSchema.ts、packages/core/src/config/config.ts、packages/core/src/telemetry/config.ts`，这些文件承载 `support custom resource attributes and add metric cardinality controls` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `TelemetryMetricsSettings、RESERVED_RESOURCE_ATTRIBUTE_KEYS、parseOtelResourceAttributes、stripReservedResourceAttributes、coerceStringResourceAttributes、ResourceAttributeWarnings、warn`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `OTEL_RESOURCE_ATTRIBUTES、SERVICE_NAME、SERVICE_VERSION、OTEL_SERVICE_NAME、QWEN_、OTEL_、OTEL_EXPORTER_OTLP_ENDPOINT、OTEL_EXPORTER_OTLP_、OTEL_EXPORTER_OTLP_HEADERS、OTEL_TRACES_SAMPLER`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('resolveTelemetrySettings — resource attributes', () => {、it('returns undefined resourceAttributes when nothing set', async () => {、it('parses OTEL_RESOURCE_ATTRIBUTES from env', async () => {、it('merges settings on top of env (settings wins)', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/telemetry-resource-attributes-design.md、docs/developers/development/telemetry.md、packages/vscode-ide-companion/schemas/settings.schema.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/settingsSchema.ts`
- `packages/core/src/config/config.ts`
- `packages/core/src/telemetry/config.ts`
- `packages/core/src/telemetry/metrics.ts`
- `packages/core/src/telemetry/resource-attributes.ts`
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/core/src/telemetry/config.test.ts`
- `packages/core/src/telemetry/metrics.test.ts`
- `packages/core/src/telemetry/resource-attributes.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- `docs/design/telemetry-resource-attributes-design.md`
- `docs/developers/development/telemetry.md`
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `TelemetryMetricsSettings`
- `RESERVED_RESOURCE_ATTRIBUTE_KEYS`
- `parseOtelResourceAttributes`
- `stripReservedResourceAttributes`
- `coerceStringResourceAttributes`
- `ResourceAttributeWarnings`
- `warn`

### 接口、命令或环境变量
- `OTEL_RESOURCE_ATTRIBUTES`
- `SERVICE_NAME`
- `SERVICE_VERSION`
- `OTEL_SERVICE_NAME`
- `QWEN_`
- `OTEL_`
- `OTEL_EXPORTER_OTLP_ENDPOINT`
- `OTEL_EXPORTER_OTLP_`
- `OTEL_EXPORTER_OTLP_HEADERS`
- `OTEL_TRACES_SAMPLER`

### 测试用例线索
- `describe('resolveTelemetrySettings — resource attributes', () => {`
- `it('returns undefined resourceAttributes when nothing set', async () => {`
- `it('parses OTEL_RESOURCE_ATTRIBUTES from env', async () => {`
- `it('merges settings on top of env (settings wins)', async () => {`
- `it('reads service.name from OTEL_SERVICE_NAME alone', async () => {`
- `it('reads service.name from OTEL_RESOURCE_ATTRIBUTES alone', async () => {`
- `it('drops user-provided session.id from env with warning', async () => {`
- `it('drops user-provided session.id from settings with warning', async () => {`
- `it('drops non-string settings values', async () => {`
- `it('OTEL_SERVICE_NAME wins over OTEL_RESOURCE_ATTRIBUTES.service.name', async () => {`

## 代码阅读结论

- 最终实现以 PR #4367 的合入代码为准，核心判断来自 13 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
