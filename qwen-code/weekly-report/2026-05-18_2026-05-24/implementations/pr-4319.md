# PR #4319 最终实现文档

> 来源 PR: [feat(acp-bridge): F1 — acp-bridge package self-sufficiency (#4175 mechanical lift + BridgeFileSystem seam)](https://github.com/QwenLM/qwen-code/pull/4319)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +5620 / -4710，19 个文件
> 时间: merged `2026-05-19T16:26:05Z`；closed `2026-05-19T16:26:05Z`

## 实现目标

围绕 `F1 — acp-bridge package self-sufficiency (#4175 mechanical lift + BridgeFileSystem seam)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridge.ts、packages/acp-bridge/src/bridgeClient.ts、packages/acp-bridge/src/bridgeFileSystem.ts`，这些文件承载 `F1 — acp-bridge package self-sufficiency (#4175 mechanical lift + BridgeFileSystem seam)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `defaultSpawnChannelFactory、STDERR_LINE_CAP_CHARS、KILL_HARD_DEADLINE_MS、SCRUBBED_CHILD_ENV_KEYS、killChild、writeStderrLine、MAX_RESOLVED_PERMISSION_RECORDS、MAX_EARLY_EVENT_SESSIONS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `POST /session/:id/model never returns、--acp、--event-ring-size、--unhandled-rejections、--prompt-deadline、--memory-file-name、QWEN_CLI_ENTRY、SCRUBBED_CHILD_ENV_KEYS、OPENAI_API_KEY、ANTHROPIC_API_KEY`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `* `readTextFile` delegate every call to it (and the inline、describe('BridgeClient — BridgeFileSystem injection seam (F1 step 5)', () => {、describe('writeTextFile', () => {、it('delegates to the injected fileSystem.writeText, bypassing the inline fs proxy', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `packages/acp-bridge/README.md、packages/acp-bridge/package.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridge.ts`
- `packages/acp-bridge/src/bridgeClient.ts`
- `packages/acp-bridge/src/bridgeFileSystem.ts`
- `packages/acp-bridge/src/bridgeOptions.ts`
- `packages/acp-bridge/src/channel.ts`
- `packages/acp-bridge/src/index.ts`
- `packages/acp-bridge/src/internal/stderrLine.ts`
- `packages/acp-bridge/src/permission.ts`
- `packages/acp-bridge/src/spawnChannel.ts`
- `packages/cli/src/acp-integration/acpAgent.ts`
- `packages/cli/src/config/config.ts`
- `packages/cli/src/config/normalizeDisabledTools.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridgeClient.test.ts`
- `packages/acp-bridge/src/spawnChannel.test.ts`
- `packages/cli/src/config/normalizeDisabledTools.test.ts`

### 文档、配置与发布材料
- `packages/acp-bridge/README.md`
- `packages/acp-bridge/package.json`

## 代码变更线索

### 新增或调整的实现符号
- `defaultSpawnChannelFactory`
- `STDERR_LINE_CAP_CHARS`
- `KILL_HARD_DEADLINE_MS`
- `SCRUBBED_CHILD_ENV_KEYS`
- `killChild`
- `writeStderrLine`
- `MAX_RESOLVED_PERMISSION_RECORDS`
- `MAX_EARLY_EVENT_SESSIONS`
- `MAX_EARLY_EVENTS_PER_SESSION`
- `EARLY_EVENT_TTL_MS`
- `describeStatKind`
- `sliceLineRange`

### 接口、命令或环境变量
- `POST /session/:id/model never returns`
- `--acp`
- `--event-ring-size`
- `--unhandled-rejections`
- `--prompt-deadline`
- `--memory-file-name`
- `QWEN_CLI_ENTRY`
- `SCRUBBED_CHILD_ENV_KEYS`
- `OPENAI_API_KEY`
- `ANTHROPIC_API_KEY`
- `QWEN_`
- `DASHSCOPE_API_KEY`
- `QWEN_SERVER_TOKEN`
- `STDERR_LINE_CAP_CHARS`
- `KILL_HARD_DEADLINE_MS`
- `OPENAI_`

### 测试用例线索
- `* `readTextFile` delegate every call to it (and the inline`
- `describe('BridgeClient — BridgeFileSystem injection seam (F1 step 5)', () => {`
- `describe('writeTextFile', () => {`
- `it('delegates to the injected fileSystem.writeText, bypassing the inline fs proxy', async () => {`
- `it('does NOT touch real fs when delegating — invalid path never reaches fs.realpath', async () => {`
- `describe('readTextFile', () => {`
- `it('delegates to the injected fileSystem.readText, bypassing the inline fs proxy', async () => {`
- `it('propagates fileSystem.readText errors to the caller', async () => {`
- `describe('inline fallback when fileSystem is omitted (regression guard)', () => {`
- `it('writeTextFile actually writes to disk through the inline proxy', async () => {`

## 代码阅读结论

- 最终实现以 PR #4319 的合入代码为准，核心判断来自 19 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
