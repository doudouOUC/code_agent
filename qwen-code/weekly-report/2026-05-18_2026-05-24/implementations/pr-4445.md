# PR #4445 最终实现文档

> 来源 PR: [refactor(acp-bridge): F1 test split — lift bridge.test.ts (6861 LOC) to acp-bridge](https://github.com/QwenLM/qwen-code/pull/4445)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +597 / -449，5 个文件
> 时间: merged `2026-05-23T08:46:09Z`；closed `2026-05-23T08:46:09Z`

## 实现目标

围绕 `F1 test split — lift bridge.test.ts (6861 LOC) to acp-bridge` 做重构调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/internal/testUtils.ts`，这些文件承载 `F1 test split — lift bridge.test.ts (6861 LOC) to acp-bridge` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `WS_A、WS_B、SESS_A、makeBridge、FakeAgentOpts、FakeAgent、ChannelHandle、makeChannel`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `PROTOCOL_VERSION、SESS_A、BX9_P`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('createHttpAcpBridge — daemon-host status provider integration', () => {、it('answers /workspace/env from process state without consulting ACP, idle or live', async () => {、it('returns daemon preflight cells with not_started ACP cells when idle', async () => {、it('merges daemon cells with live ACP-side preflight cells when a channel is up', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `packages/acp-bridge/package.json、packages/cli/vitest.config.ts` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/internal/testUtils.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridge.test.ts`
- `packages/cli/src/serve/daemonStatusProvider.test.ts`

### 文档、配置与发布材料
- `packages/acp-bridge/package.json`
- `packages/cli/vitest.config.ts`

## 代码变更线索

### 新增或调整的实现符号
- `WS_A`
- `WS_B`
- `SESS_A`
- `makeBridge`
- `FakeAgentOpts`
- `FakeAgent`
- `ChannelHandle`
- `makeChannel`

### 接口、命令或环境变量
- `PROTOCOL_VERSION`
- `SESS_A`
- `BX9_P`

### 测试用例线索
- `describe('createHttpAcpBridge — daemon-host status provider integration', () => {`
- `it('answers /workspace/env from process state without consulting ACP, idle or live', async () => {`
- `it('returns daemon preflight cells with not_started ACP cells when idle', async () => {`
- `it('merges daemon cells with live ACP-side preflight cells when a channel is up', async () => {`
- `it('falls back to idle ACP cells + envelope error when extMethod throws mid-preflight', async () => {`

## 代码阅读结论

- 最终实现以 PR #4445 的合入代码为准，核心判断来自 5 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
