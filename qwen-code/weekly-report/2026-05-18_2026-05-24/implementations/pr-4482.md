# PR #4482 最终实现文档

> 来源 PR: [fix(telemetry): improve LogToSpan bridge error info and TUI handling](https://github.com/QwenLM/qwen-code/pull/4482)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +593 / -17，4 个文件
> 时间: merged `2026-05-27T12:13:52Z`；closed `2026-05-27T12:13:52Z`

## 实现目标

围绕 `improve LogToSpan bridge error info and TUI handling` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/telemetry/log-to-span-processor.ts、packages/core/src/telemetry/sdk.ts`，这些文件承载 `improve LogToSpan bridge error info and TUI handling` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `LogToSpanDiagnosticsSink、formatExportError`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `EXPORT_TIMEOUT_MS、QWEN_DEBUG_LOG_FILE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('export failure diagnostics', () => {、it('falls back to error.name when message is empty (HTTP/2 / stripped reason phrase)', async () => {、it('JSON-escapes embedded newlines in message and data so the record stays on one line', async () => {、it('truncates response data snippets to 200 bytes before stringifying', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/telemetry/log-to-span-processor.ts`
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/core/src/telemetry/log-to-span-processor.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `LogToSpanDiagnosticsSink`
- `formatExportError`

### 接口、命令或环境变量
- `EXPORT_TIMEOUT_MS`
- `QWEN_DEBUG_LOG_FILE`

### 测试用例线索
- `describe('export failure diagnostics', () => {`
- `it('falls back to error.name when message is empty (HTTP/2 / stripped reason phrase)', async () => {`
- `it('JSON-escapes embedded newlines in message and data so the record stays on one line', async () => {`
- `it('truncates response data snippets to 200 bytes before stringifying', async () => {`
- `it('omits httpCode when err.code is a non-numeric networking code (ECONNREFUSED)', async () => {`
- `it('reports error=unknown when result.error is missing', async () => {`
- `it('routes diagnostics to an injected sink without touching stderr', async () => {`
- `it('routes buffer-overflow warnings through the injected sink', async () => {`
- `it('routes export timeout through the injected sink', async () => {`
- `it('routes export-threw (synchronous exporter exception) through the injected sink', async () => {`

## 代码阅读结论

- 最终实现以 PR #4482 的合入代码为准，核心判断来自 4 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
