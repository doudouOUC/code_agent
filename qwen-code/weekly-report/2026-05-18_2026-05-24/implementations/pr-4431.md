# PR #4431 最终实现文档

> 来源 PR: [fix(core): preserve uid in atomicWriteFile to avoid breaking shared-write files](https://github.com/QwenLM/qwen-code/pull/4431)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +206 / -47，4 个文件
> 时间: merged `2026-06-01T08:15:16Z`；closed `2026-06-01T08:15:16Z`

## 实现目标

围绕 `preserve uid in atomicWriteFile to avoid breaking shared-write files` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/services/worktreeSessionService.ts、packages/core/src/utils/atomicFileWrite.ts、packages/core/src/utils/runtimeStatus.ts`，这些文件承载 `preserve uid in atomicWriteFile to avoid breaking shared-write files` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `writeInPlaceWithFdGuards、O_NOFOLLOW、O_NONBLOCK`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `MOVED_TO、CAP_CHOWN、O_NOFOLLOW、O_CREAT、O_WRONLY、O_TRUNC、EOWNERSHIP_CHANGED、O_NONBLOCK、EINPLACE_WRITE_FAILED、EINODE_UNLINKED_DURING_WRITE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('writeInPlaceWithFdGuards', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/services/worktreeSessionService.ts`
- `packages/core/src/utils/atomicFileWrite.ts`
- `packages/core/src/utils/runtimeStatus.ts`

### 测试与验证文件
- `packages/core/src/utils/atomicFileWrite.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `writeInPlaceWithFdGuards`
- `O_NOFOLLOW`
- `O_NONBLOCK`

### 接口、命令或环境变量
- `MOVED_TO`
- `CAP_CHOWN`
- `O_NOFOLLOW`
- `O_CREAT`
- `O_WRONLY`
- `O_TRUNC`
- `EOWNERSHIP_CHANGED`
- `O_NONBLOCK`
- `EINPLACE_WRITE_FAILED`
- `EINODE_UNLINKED_DURING_WRITE`

### 测试用例线索
- `describe('writeInPlaceWithFdGuards', () => {`

## 代码阅读结论

- 最终实现以 PR #4431 的合入代码为准，核心判断来自 4 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
