# PR #4360 最终实现文档

> 来源 PR: [feat(serve+sdk): F4 prereq — daemon protocol completion (serverTimestamp / provenance / errorKind / state_resync_required)](https://github.com/QwenLM/qwen-code/pull/4360)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1500 / -26，13 个文件
> 时间: merged `2026-05-21T03:11:21Z`；closed `2026-05-21T03:11:21Z`

## 实现目标

围绕 `F4 prereq — daemon protocol completion (serverTimestamp / provenance / errorKind / state_resync_required)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridgeClient.ts、packages/acp-bridge/src/eventBus.ts、packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.ts`，这些文件承载 `F4 prereq — daemon protocol completion (serverTimestamp / provenance / errorKind / state_resync_required)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DaemonStateResyncRequiredData、DaemonStateResyncRequiredEvent、RESYNC_PASSTHROUGH_TYPES、isStateResyncRequiredData、FsErrorShape、isFsErrorShape、preserveFsErrorOverAcp`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `EVENT_SCHEMA_VERSION、RESYNC_PASSTHROUGH_TYPES、SERVE_ERROR_KINDS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('resolveToolProvenance (#4175 F4 prereq)', () => {、it('classifies a plain tool name as builtin (no serverId)', () => {、it('classifies a tool name without mcp__ prefix as builtin', () => {、it('classifies mcp__<server>__<tool> as mcp with serverId', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridgeClient.ts`
- `packages/acp-bridge/src/eventBus.ts`
- `packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.ts`
- `packages/cli/src/serve/server.ts`
- `packages/sdk-typescript/src/daemon/events.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/index.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridgeClient.test.ts`
- `packages/acp-bridge/src/eventBus.test.ts`
- `packages/cli/src/acp-integration/session/HistoryReplayer.test.ts`
- `packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/daemonEvents.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `DaemonStateResyncRequiredData`
- `DaemonStateResyncRequiredEvent`
- `RESYNC_PASSTHROUGH_TYPES`
- `isStateResyncRequiredData`
- `FsErrorShape`
- `isFsErrorShape`
- `preserveFsErrorOverAcp`

### 接口、命令或环境变量
- `EVENT_SCHEMA_VERSION`
- `RESYNC_PASSTHROUGH_TYPES`
- `SERVE_ERROR_KINDS`

### 测试用例线索
- `describe('resolveToolProvenance (#4175 F4 prereq)', () => {`
- `it('classifies a plain tool name as builtin (no serverId)', () => {`
- `it('classifies a tool name without mcp__ prefix as builtin', () => {`
- `it('classifies mcp__<server>__<tool> as mcp with serverId', () => {`
- `it('preserves underscores in the tool segment', () => {`
- `it('classifies malformed mcp__ prefix (only one segment) as builtin', () => {`
- `it('classifies mcp__<empty>__<tool> as builtin (empty server segment)', () => {`
- `it('classifies any tool as subagent when subagentMeta is present', () => {`
- `it('classifies a plain builtin tool with subagentMeta as subagent', () => {`
- `describe('provenance stamping on emit (#4175 F4 prereq)', () => {`

## 代码阅读结论

- 最终实现以 PR #4360 的合入代码为准，核心判断来自 13 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
