# PR #4305 最终实现文档

> 来源 PR: [fix(serve): post-merge fixes for #4291 review (7 threads)](https://github.com/QwenLM/qwen-code/pull/4305)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +768 / -161，5 个文件
> 时间: merged `2026-05-19T05:40:46Z`；closed `2026-05-19T05:40:46Z`

## 实现目标

围绕 `post-merge fixes for #4291 review (7 threads)` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/auth/deviceFlow.ts、packages/cli/src/serve/auth/qwenDeviceFlowProvider.ts、packages/cli/src/serve/server.ts`，这些文件承载 `post-merge fixes for #4291 review (7 threads)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `callerIsDeviceFlowInitiator、SANITIZE_FOR_STDERR_RE、sanitizeForStderr、makeRegistryPollTimeoutError`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--unhandled-rejections、DEVICE_FLOW_POLL_TIMEOUT_MS、QWEN_SERVE_DEBUG、SANITIZE_FOR_STDERR_RE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('sanitizes control characters in attacker-controlled err.name on the non-OAuth path (round-4 #4)', async () => {、it('does NOT attach late-poll observer when the provider beats the timeout (round-5 #1: pollTimedOut race)', async () => {、it('sanitizes hostile latePollResult.kind in late-observer audit (round-5 #3)', async () => {、it('sanitizes hostile lateErr.name in late-rejection observer audit (round-5 #2)', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/auth/deviceFlow.ts`
- `packages/cli/src/serve/auth/qwenDeviceFlowProvider.ts`
- `packages/cli/src/serve/server.ts`

### 测试与验证文件
- `packages/cli/src/serve/auth/deviceFlow.test.ts`
- `packages/cli/src/serve/auth/qwenDeviceFlowProvider.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `callerIsDeviceFlowInitiator`
- `SANITIZE_FOR_STDERR_RE`
- `sanitizeForStderr`
- `makeRegistryPollTimeoutError`

### 接口、命令或环境变量
- `--unhandled-rejections`
- `DEVICE_FLOW_POLL_TIMEOUT_MS`
- `QWEN_SERVE_DEBUG`
- `SANITIZE_FOR_STDERR_RE`

### 测试用例线索
- `it('sanitizes control characters in attacker-controlled err.name on the non-OAuth path (round-4 #4)', async () => {`
- `it('does NOT attach late-poll observer when the provider beats the timeout (round-5 #1: pollTimedOut race)', async () => {`
- `it('sanitizes hostile latePollResult.kind in late-observer audit (round-5 #3)', async () => {`
- `it('sanitizes hostile lateErr.name in late-rejection observer audit (round-5 #2)', async () => {`
- `it('sanitizes Unicode lookalike controls (U+2028 LINE SEPARATOR, bidi, ZWNBSP) in oauthError (round-5 #4)', async () => {`
- `it('audits a provider-thrown DeviceFlowPollTimeoutError as a real failure (round-6 #4: brand-aware self-filter)', async () => {`
- `it('survives a throwing audit sink in the late-poll observer (round-6 #2: terminal .catch())', async () => {`
- `it('sanitizes rawProviderError before interpolating into the audit hint (round-6 #3)', async () => {`
- `it('sanitizes Unicode bidi ISOLATE controls U+2066–U+2069 (CVE-2021-42574 Trojan Source) (round-6 #5)', async () => {`

## 代码阅读结论

- 最终实现以 PR #4305 的合入代码为准，核心判断来自 5 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
