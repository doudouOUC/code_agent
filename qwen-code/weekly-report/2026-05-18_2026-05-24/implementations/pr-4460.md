# PR #4460 最终实现文档

> 来源 PR: [fix(core): F2 cleanup PR B — self-heal observability (W133-a + W134)](https://github.com/QwenLM/qwen-code/pull/4460)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +405 / -5，3 个文件
> 时间: merged `2026-05-23T15:23:30Z`；closed `2026-05-23T15:23:30Z`

## 实现目标

围绕 `F2 cleanup PR B — self-heal observability (W133-a + W134)` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/tools/mcp-client.ts、packages/core/src/tools/mcp-pool-entry.ts`，这些文件承载 `F2 cleanup PR B — self-heal observability (W133-a + W134)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `SweepResult`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--debug`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('threads upstream onerror cause into failed event lastError (W133-a)', async () => {、it('falls back to synthetic-only marker when no upstream onerror was captured (W133-a fallback)', async () => {、it('emits structured warn log when silent-drop sweep throws on pid discovery (W134)', async () => {、it('emits structured warn log when silent-drop sweep partially signals descendants (W134 partial-signal)', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/tools/mcp-client.ts`
- `packages/core/src/tools/mcp-pool-entry.ts`

### 测试与验证文件
- `packages/core/src/tools/mcp-transport-pool.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `SweepResult`

### 接口、命令或环境变量
- `--debug`

### 测试用例线索
- `it('threads upstream onerror cause into failed event lastError (W133-a)', async () => {`
- `it('falls back to synthetic-only marker when no upstream onerror was captured (W133-a fallback)', async () => {`
- `it('emits structured warn log when silent-drop sweep throws on pid discovery (W134)', async () => {`
- `it('emits structured warn log when silent-drop sweep partially signals descendants (W134 partial-signal)', async () => {`

## 代码阅读结论

- 最终实现以 PR #4460 的合入代码为准，核心判断来自 3 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
