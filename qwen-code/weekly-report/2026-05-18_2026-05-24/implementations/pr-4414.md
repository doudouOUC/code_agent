# PR #4414 最终实现文档

> 来源 PR: [feat(cli): background housekeeping for stale file-history dirs](https://github.com/QwenLM/qwen-code/pull/4414)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1059 / -12，13 个文件
> 时间: merged `2026-06-02T06:36:41Z`；closed `2026-06-02T06:36:41Z`

## 实现目标

围绕 `background housekeeping for stale file-history dirs` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/settingsSchema.ts、packages/cli/src/gemini.tsx、packages/cli/src/ui/contexts/KeypressContext.tsx`，这些文件承载 `background housekeeping for stale file-history dirs` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `MS_PER_DAY、MS_PER_HOUR、SWEEP_CONCURRENCY、CleanupResult、CleanupOptions、getCutoffDate、cleanupOldFileHistoryBackups、isENOENT`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--help、--prompt、--unhandled-rejections、FOCUS_IN、FILE_HISTORY_DIR、MS_PER_DAY、MS_PER_HOUR、SWEEP_CONCURRENCY、STARTUP_DELAY_MS、RECURRING_INTERVAL_MS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('getCutoffDate', () => {、it('returns now - N days for N > 0', () => {、it('clamps to 1 hour when cleanupPeriodDays = 0 (active-session safety)', () => {、it('treats negative values as 0 (defends against schema-bypass)', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/users/configuration/settings.md、packages/vscode-ide-companion/schemas/settings.schema.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/settingsSchema.ts`
- `packages/cli/src/gemini.tsx`
- `packages/cli/src/ui/contexts/KeypressContext.tsx`
- `packages/cli/src/utils/housekeeping/cleanup.ts`
- `packages/cli/src/utils/housekeeping/lastInteractionAt.ts`
- `packages/cli/src/utils/housekeeping/scheduler.ts`
- `packages/cli/src/utils/housekeeping/throttledOnce.ts`
- `packages/core/src/services/fileHistoryService.ts`

### 测试与验证文件
- `packages/cli/src/utils/housekeeping/cleanup.test.ts`
- `packages/cli/src/utils/housekeeping/scheduler.test.ts`
- `packages/cli/src/utils/housekeeping/throttledOnce.test.ts`

### 文档、配置与发布材料
- `docs/users/configuration/settings.md`
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `MS_PER_DAY`
- `MS_PER_HOUR`
- `SWEEP_CONCURRENCY`
- `CleanupResult`
- `CleanupOptions`
- `getCutoffDate`
- `cleanupOldFileHistoryBackups`
- `isENOENT`
- `noteInteraction`
- `msSinceLastInteraction`
- `Config`
- `STARTUP_DELAY_MS`

### 接口、命令或环境变量
- `--help`
- `--prompt`
- `--unhandled-rejections`
- `FOCUS_IN`
- `FILE_HISTORY_DIR`
- `MS_PER_DAY`
- `MS_PER_HOUR`
- `SWEEP_CONCURRENCY`
- `STARTUP_DELAY_MS`
- `RECURRING_INTERVAL_MS`
- `RECENT_INTERACTION_MS`
- `CATCHUP_THRESHOLD_MS`
- `STARTUP_DELAY_CATCHUP_MS`

### 测试用例线索
- `describe('getCutoffDate', () => {`
- `it('returns now - N days for N > 0', () => {`
- `it('clamps to 1 hour when cleanupPeriodDays = 0 (active-session safety)', () => {`
- `it('treats negative values as 0 (defends against schema-bypass)', () => {`
- `describe('cleanupOldFileHistoryBackups', () => {`
- `it('returns zero result when root does not exist', async () => {`
- `it('removes empty root after sweeping nothing', async () => {`
- `it('preserves dirs younger than cutoff', async () => {`
- `it('removes dirs older than cutoff', async () => {`
- `it('preserves new dirs and sweeps old ones in mixed input', async () => {`

## 代码阅读结论

- 最终实现以 PR #4414 的合入代码为准，核心判断来自 13 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
