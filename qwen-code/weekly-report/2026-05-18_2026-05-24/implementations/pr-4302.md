# PR #4302 最终实现文档

> 来源 PR: [fix(telemetry): Phase 1.5 polish — fallback order, abort-as-result, log/span consistency](https://github.com/QwenLM/qwen-code/pull/4302)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +454 / -65，7 个文件
> 时间: merged `2026-05-19T01:21:49Z`；closed `2026-05-19T01:21:49Z`

## 实现目标

围绕 `Phase 1.5 polish — fallback order, abort-as-result, log/span consistency` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/coreToolScheduler.ts、packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts、packages/core/src/telemetry/session-tracing.ts`，这些文件承载 `Phase 1.5 polish — fallback order, abort-as-result, log/span consistency` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `Context、resolveParentContext`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `TOOL_SPAN_STATUS_TOOL_CANCELLED、TOOL_SPAN_STATUS_TOOL_ERROR`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('skips success api_response log when stream span is ended by idle timeout (#4212)', async () => {、it('LLM request span re-parents to active OTel span when no interaction is set (#4212)', () => {、it('tool span re-parents to active OTel span when no interaction is set (#4212)', () => {、it('tool execution span re-parents to active OTel span when no toolContext is set (#4212)', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/coreToolScheduler.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/session-tracing.ts`
- `packages/core/src/telemetry/tracer.ts`

### 测试与验证文件
- `packages/core/src/core/coreToolScheduler.test.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `Context`
- `resolveParentContext`

### 接口、命令或环境变量
- `TOOL_SPAN_STATUS_TOOL_CANCELLED`
- `TOOL_SPAN_STATUS_TOOL_ERROR`

### 测试用例线索
- `it('skips success api_response log when stream span is ended by idle timeout (#4212)', async () => {`
- `it('LLM request span re-parents to active OTel span when no interaction is set (#4212)', () => {`
- `it('tool span re-parents to active OTel span when no interaction is set (#4212)', () => {`
- `it('tool execution span re-parents to active OTel span when no toolContext is set (#4212)', () => {`
- `it('execution sub-span: cancelled flag is NOT set on real exceptions (#4302)', async () => {`
- `it('skips api_error log when stream throws after idle timeout already closed the span (#4302)', async () => {`
- `it('cancelled: true keeps status UNSET while still recording attributes (#4302)', () => {`
- `it('cancelled: false (default) still maps success: false to ERROR status', () => {`

## 代码阅读结论

- 最终实现以 PR #4302 的合入代码为准，核心判断来自 7 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
