# PR #4321 最终实现文档

> 来源 PR: [feat(telemetry): Phase 2 — tool.blocked_on_user + hook spans (#3731)](https://github.com/QwenLM/qwen-code/pull/4321)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +3287 / -99，8 个文件
> 时间: merged `2026-05-21T03:55:51Z`；closed `2026-05-21T03:55:51Z`

## 实现目标

围绕 `Phase 2 — tool.blocked_on_user + hook spans (#3731)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/coreToolScheduler.ts、packages/core/src/core/toolHookTriggers.ts、packages/core/src/telemetry/constants.ts`，这些文件承载 `Phase 2 — tool.blocked_on_user + hook spans (#3731)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `ToolBlockedDecision、ToolBlockedSource、ToolSpanMetadata、HookSpanMetadata、SPAN_TOOL_BLOCKED_ON_USER、SPAN_HOOK、startToolBlockedOnUserSpan、endToolBlockedOnUserSpan`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `TOOL_FAILURE_KIND_PRE_HOOK_BLOCKED、TOOL_SPAN_STATUS_PRE_HOOK_BLOCKED、TOOL_FAILURE_KIND_TOOL_EXCEPTION、EXECUTION_DENIED、SPAN_TOOL_BLOCKED_ON_USER、SPAN_HOOK、NOOP_SPAN、TOOL_FAILURE_KIND_PERMISSION_DENIED、TOOL_FAILURE_KIND_PERMISSION_HOOK_DENIED、TOOL_FAILURE_KIND_PLAN_MODE_BLOCKED`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('tool span is started in _schedule and ended even when pre-hook denies execution (#3731 Phase 2)', async () => {、it('blocked_on_user span ends with cancel when the user rejects (#3731 Phase 2)', async () => {、it('hook span records shouldProceed=false / blockType=denied when pre-hook blocks (#3731 Phase 2)', async () => {、it('hook span records shouldStop=true when post-hook stops execution (#3731 Phase 2)', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/coreToolScheduler.ts`
- `packages/core/src/core/toolHookTriggers.ts`
- `packages/core/src/telemetry/constants.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/core/src/core/coreToolScheduler.test.ts`
- `packages/core/src/core/toolHookTriggers.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `ToolBlockedDecision`
- `ToolBlockedSource`
- `ToolSpanMetadata`
- `HookSpanMetadata`
- `SPAN_TOOL_BLOCKED_ON_USER`
- `SPAN_HOOK`
- `startToolBlockedOnUserSpan`
- `endToolBlockedOnUserSpan`
- `HookEvent`
- `StartHookSpanOptions`
- `startHookSpan`
- `endHookSpan`

### 接口、命令或环境变量
- `TOOL_FAILURE_KIND_PRE_HOOK_BLOCKED`
- `TOOL_SPAN_STATUS_PRE_HOOK_BLOCKED`
- `TOOL_FAILURE_KIND_TOOL_EXCEPTION`
- `EXECUTION_DENIED`
- `SPAN_TOOL_BLOCKED_ON_USER`
- `SPAN_HOOK`
- `NOOP_SPAN`
- `TOOL_FAILURE_KIND_PERMISSION_DENIED`
- `TOOL_FAILURE_KIND_PERMISSION_HOOK_DENIED`
- `TOOL_FAILURE_KIND_PLAN_MODE_BLOCKED`

### 测试用例线索
- `it('tool span is started in _schedule and ended even when pre-hook denies execution (#3731 Phase 2)', async () => {`
- `it('blocked_on_user span ends with cancel when the user rejects (#3731 Phase 2)', async () => {`
- `it('hook span records shouldProceed=false / blockType=denied when pre-hook blocks (#3731 Phase 2)', async () => {`
- `it('hook span records shouldStop=true when post-hook stops execution (#3731 Phase 2)', async () => {`
- `it('every span recorded in a successful tool call is ended (#3731 Phase 2)', async () => {`
- `describe('blocked_on_user spans (#3731 Phase 2)', () => {`
- `it('parents the blocked span under the explicitly-passed tool span', () => {`
- `it('records decision/source attributes on end and leaves status UNSET', () => {`
- `it('is idempotent — second end is a no-op', () => {`
- `it('returns NOOP span when SDK is not initialized', () => {`

## 代码阅读结论

- 最终实现以 PR #4321 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
