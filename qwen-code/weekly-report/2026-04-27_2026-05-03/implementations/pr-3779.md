# PR #3779 最终实现文档

> 来源 PR: [feat(telemetry): define HTTP OTLP endpoint behavior and signal routing](https://github.com/QwenLM/qwen-code/pull/3779)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1387 / -102，11 个文件
> 时间: merged `2026-05-01T14:47:01Z`；closed `2026-05-01T14:47:01Z`

## 实现目标

围绕 `define HTTP OTLP endpoint behavior and signal routing` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/gemini.tsx、packages/core/src/config/config.ts、packages/core/src/telemetry/config.ts`，这些文件承载 `define HTTP OTLP endpoint behavior and signal routing` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `Resource、LogToSpanProcessor、ReadableSpanLike、randomHexString、deriveTraceId、deriveSpanStatus、hrTimeDiff、OTLP_SIGNAL_PATHS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--telemetry、--no-telemetry、--telemetry-target、--telemetry-otlp-endpoint、--telemetry-otlp-protocol、--telemetry-outfile、--telemetry-log-prompts、--no-telemetry-log-prompts、QWEN_TELEMETRY_ENABLED、QWEN_TELEMETRY_TARGET`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('Per-Signal OTLP Endpoint Configuration', () => {、it('should return per-signal endpoints when provided', () => {、it('should return undefined when per-signal endpoints are not provided', () => {、it('resolves per-signal endpoints from OTEL_ env vars', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/development/telemetry.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/gemini.tsx`
- `packages/core/src/config/config.ts`
- `packages/core/src/telemetry/config.ts`
- `packages/core/src/telemetry/log-to-span-processor.ts`
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/cli/src/gemini.test.tsx`
- `packages/core/src/config/config.test.ts`
- `packages/core/src/telemetry/config.test.ts`
- `packages/core/src/telemetry/log-to-span-processor.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- `docs/developers/development/telemetry.md`

## 代码变更线索

### 新增或调整的实现符号
- `Resource`
- `LogToSpanProcessor`
- `ReadableSpanLike`
- `randomHexString`
- `deriveTraceId`
- `deriveSpanStatus`
- `hrTimeDiff`
- `OTLP_SIGNAL_PATHS`
- `OtlpSignal`
- `resolveHttpOtlpUrl`
- `validateUrl`
- `EXPORT_TIMEOUT_MS`

### 接口、命令或环境变量
- `--telemetry`
- `--no-telemetry`
- `--telemetry-target`
- `--telemetry-otlp-endpoint`
- `--telemetry-otlp-protocol`
- `--telemetry-outfile`
- `--telemetry-log-prompts`
- `--no-telemetry-log-prompts`
- `QWEN_TELEMETRY_ENABLED`
- `QWEN_TELEMETRY_TARGET`
- `QWEN_TELEMETRY_OTLP_ENDPOINT`
- `QWEN_TELEMETRY_OTLP_PROTOCOL`
- `QWEN_TELEMETRY_OTLP_TRACES_ENDPOINT`
- `QWEN_TELEMETRY_OTLP_LOGS_ENDPOINT`
- `QWEN_TELEMETRY_OTLP_METRICS_ENDPOINT`
- `QWEN_TELEMETRY_OUTFILE`
- `QWEN_TELEMETRY_LOG_PROMPTS`
- `QWEN_TELEMETRY_USE_COLLECTOR`

### 测试用例线索
- `describe('Per-Signal OTLP Endpoint Configuration', () => {`
- `it('should return per-signal endpoints when provided', () => {`
- `it('should return undefined when per-signal endpoints are not provided', () => {`
- `it('resolves per-signal endpoints from OTEL_ env vars', async () => {`
- `it('QWEN_ env vars take precedence over OTEL_ vars for per-signal endpoints', async () => {`
- `it('resolves per-signal endpoints from settings', async () => {`
- `describe('LogToSpanProcessor', () => {`
- `it('converts a log record to a span on flush', async () => {`
- `it('uses duration_ms to compute span end time', async () => {`
- `it('handles duration_ms that causes second rollover', async () => {`

## 代码阅读结论

- 最终实现以 PR #3779 的合入代码为准，核心判断来自 11 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
