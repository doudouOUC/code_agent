# PR #3685 最终实现文档

> 来源 PR: [feat(sdk-python): add PyPI release workflow](https://github.com/QwenLM/qwen-code/pull/3685)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +2082 / -32，8 个文件
> 时间: merged `2026-05-04T13:07:22Z`；closed `2026-05-04T13:07:22Z`

## 实现目标

围绕 `add PyPI release workflow` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/sdk-python/scripts/get-release-version.js、packages/sdk-typescript/scripts/get-release-version.js、scripts/get-release-version.js`，这些文件承载 `add PyPI release workflow` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `PACKAGE_NAME、TAG_PREFIX、readPyprojectVersion、getArgs、parseVersion、compareVersions、sortDescending、toBaseVersion`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--upgrade、--type、--preview、--stable、--config、--check、--config-file、--qwen、--cwd、--json-only`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('python sdk get-release-version', () => {、it('returns same-channel previous release tags for preview and nightly', async () => {、it('fails when an explicit override conflicts with existing PyPI version', async () => {、it('fails if GitHub release lookup errors for reasons other than not found', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `.github/workflows/release-sdk-python.yml、.github/workflows/sdk-python.yml、package-lock.json、package.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/sdk-python/scripts/get-release-version.js`
- `packages/sdk-typescript/scripts/get-release-version.js`
- `scripts/get-release-version.js`

### 测试与验证文件
- `scripts/tests/get-release-version-python-sdk.test.js`

### 文档、配置与发布材料
- `.github/workflows/release-sdk-python.yml`
- `.github/workflows/sdk-python.yml`
- `package-lock.json`
- `package.json`

## 代码变更线索

### 新增或调整的实现符号
- `PACKAGE_NAME`
- `TAG_PREFIX`
- `readPyprojectVersion`
- `getArgs`
- `parseVersion`
- `compareVersions`
- `sortDescending`
- `toBaseVersion`
- `getAllVersionsFromPyPI`
- `getCurrentPackageBaseVersion`
- `getLatestStableVersion`
- `getLatestPreviewBaseVersion`

### 接口、命令或环境变量
- `--upgrade`
- `--type`
- `--preview`
- `--stable`
- `--config`
- `--check`
- `--config-file`
- `--qwen`
- `--cwd`
- `--json-only`
- `RELEASE_TAG`
- `CREATE_NIGHTLY_RELEASE`
- `CREATE_PREVIEW_RELEASE`
- `DRY_RUN_INPUT`
- `GITHUB_OUTPUT`
- `VERSION_ARGS`
- `IS_NIGHTLY`
- `IS_PREVIEW`

### 测试用例线索
- `describe('python sdk get-release-version', () => {`
- `it('returns same-channel previous release tags for preview and nightly', async () => {`
- `it('fails when an explicit override conflicts with existing PyPI version', async () => {`
- `it('fails if GitHub release lookup errors for reasons other than not found', async () => {`
- `it('fails when the latest preview base is not newer than the latest stable', async () => {`
- `it('fails instead of patch-bumping a stable release derived from preview', async () => {`
- `it('returns the previous stable tag for stable releases', async () => {`
- `it('maps preview versions to PEP 440 package versions', async () => {`
- `it('throws on nightly conflicts instead of silently changing the timestamped version', async () => {`
- `it('throws when PyPI metadata fetch is not ok', async () => {`

## 代码阅读结论

- 最终实现以 PR #3685 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
