# PR #3813 最终实现文档

> 来源 PR: [fix(telemetry): add bounded shutdown timeout and fix service.version resource attribute](https://github.com/QwenLM/qwen-code/pull/3813)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +130 / -3，2 个文件
> 时间: merged `2026-05-05T03:12:14Z`；closed `2026-05-05T03:12:14Z`

## 实现目标

围绕 `add bounded shutdown timeout and fix service.version resource attribute` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/telemetry/sdk.ts`，这些文件承载 `add bounded shutdown timeout and fix service.version resource attribute` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `SHUTDOWN_TIMEOUT_MS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SHUTDOWN_TIMEOUT_MS、SERVICE_VERSION`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should set service.version to the application version, not Node.js version', () => {、it('should complete shutdown within timeout when SDK shutdown hangs', async () => {、it('should complete shutdown normally when SDK resolves before timeout', async () => {、it('should log error when sdk.shutdown() rejects', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `SHUTDOWN_TIMEOUT_MS`

### 接口、命令或环境变量
- `SHUTDOWN_TIMEOUT_MS`
- `SERVICE_VERSION`

### 测试用例线索
- `it('should set service.version to the application version, not Node.js version', () => {`
- `it('should complete shutdown within timeout when SDK shutdown hangs', async () => {`
- `it('should complete shutdown normally when SDK resolves before timeout', async () => {`
- `it('should log error when sdk.shutdown() rejects', async () => {`
- `it('should fall back to "unknown" when getCliVersion returns undefined', () => {`

## 代码阅读结论

- 最终实现以 PR #3813 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
