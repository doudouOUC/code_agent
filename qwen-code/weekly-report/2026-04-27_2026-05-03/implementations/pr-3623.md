# PR #3623 最终实现文档

> 来源 PR: [fix(cli): recognize OpenAI-compatible providers in `qwen auth status`](https://github.com/QwenLM/qwen-code/pull/3623)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +475 / -30，2 个文件
> 时间: merged `2026-04-28T01:06:09Z`；closed `2026-04-28T01:06:09Z`

## 实现目标

围绕 `recognize OpenAI-compatible providers in `qwen auth status`` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/commands/auth/handler.ts`，这些文件承载 `recognize OpenAI-compatible providers in `qwen auth status`` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `ModelProvidersConfig`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `USE_OPENAI、OPENAI_API_KEY、CODING_PLAN_ENV_KEY`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should show Coding Plan when env key is set but codingPlan.region is missing', async () => {、describe('OpenAI-compatible provider (no Coding Plan)', () => {、it('should show OpenAI-compatible status with OPENAI_API_KEY', async () => {、it('should show OpenAI-compatible status with custom envKey from modelProviders', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/commands/auth/handler.ts`

### 测试与验证文件
- `packages/cli/src/commands/auth/status.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `ModelProvidersConfig`

### 接口、命令或环境变量
- `USE_OPENAI`
- `OPENAI_API_KEY`
- `CODING_PLAN_ENV_KEY`

### 测试用例线索
- `it('should show Coding Plan when env key is set but codingPlan.region is missing', async () => {`
- `describe('OpenAI-compatible provider (no Coding Plan)', () => {`
- `it('should show OpenAI-compatible status with OPENAI_API_KEY', async () => {`
- `it('should show OpenAI-compatible status with custom envKey from modelProviders', async () => {`
- `it('should show OpenAI-compatible status with settings.security.auth.apiKey', async () => {`
- `it('should show incomplete when no API key is found for OpenAI-compatible provider', async () => {`
- `it('should detect API key via default model when model.name is unset', async () => {`
- `it('should show Coding Plan when detected via modelProviders entry (no codingPlan.region)', async () => {`
- `it('should show Incomplete when explicit envKey is missing even if OPENAI_API_KEY is set', async () => {`
- `it('should show OpenAI-compatible when stale Coding Plan key exists but active model is generic', async () => {`

## 代码阅读结论

- 最终实现以 PR #3623 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
