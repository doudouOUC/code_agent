# PR #3726 最终实现文档

> 来源 PR: [feat(core): add Monitor(...) permission namespace](https://github.com/QwenLM/qwen-code/pull/3726)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +169 / -15，5 个文件
> 时间: merged `2026-04-29T09:38:02Z`；closed `2026-04-29T09:38:02Z`

## 实现目标

围绕 `add Monitor(...) permission namespace` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/permissions/permission-manager.ts、packages/core/src/permissions/rule-parser.ts、packages/core/src/tools/monitor.ts`，这些文件承载 `add Monitor(...) permission namespace` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `SHELL_LIKE_TOOLS、SHELL_TOOL_NAMES`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SHELL_LIKE_TOOLS、SHELL_TOOL_NAMES、DISPLAY_NAME_TO_VERB`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('parses Monitor alias', async () => {、it('parses Monitor with command specifier', async () => {、it('Monitor rule matches monitor invocations with command specifier', async () => {、it('Monitor rule does not match run_shell_command', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/permissions/permission-manager.ts`
- `packages/core/src/permissions/rule-parser.ts`
- `packages/core/src/tools/monitor.ts`

### 测试与验证文件
- `packages/core/src/permissions/permission-manager.test.ts`
- `packages/core/src/tools/monitor.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `SHELL_LIKE_TOOLS`
- `SHELL_TOOL_NAMES`

### 接口、命令或环境变量
- `SHELL_LIKE_TOOLS`
- `SHELL_TOOL_NAMES`
- `DISPLAY_NAME_TO_VERB`

### 测试用例线索
- `it('parses Monitor alias', async () => {`
- `it('parses Monitor with command specifier', async () => {`
- `it('Monitor rule matches monitor invocations with command specifier', async () => {`
- `it('Monitor rule does not match run_shell_command', async () => {`
- `it('Bash rule does not match monitor', async () => {`
- `describe('monitor command-level evaluation', () => {`
- `it('Monitor(...) allow rule matches monitor invocations', async () => {`
- `it('Monitor(...) deny rule blocks monitor invocations', async () => {`
- `it('Monitor approval does NOT allow run_shell_command', async () => {`
- `it('Bash approval does NOT allow monitor', async () => {`

## 代码阅读结论

- 最终实现以 PR #3726 的合入代码为准，核心判断来自 5 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
