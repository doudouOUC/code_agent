# PR #3792 最终实现文档

> 来源 PR: [fix(core): address post-merge monitor tool and UI routing issues](https://github.com/QwenLM/qwen-code/pull/3792)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +664 / -227，21 个文件
> 时间: merged `2026-05-04T13:19:41Z`；closed `2026-05-04T13:19:41Z`

## 实现目标

围绕 `address post-merge monitor tool and UI routing issues` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/ui/commands/clearCommand.ts、packages/cli/src/ui/hooks/useResumeCommand.ts、packages/cli/src/ui/utils/backgroundWorkUtils.ts`，这些文件承载 `address post-merge monitor tool and UI routing issues` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `hasBlockingBackgroundWork、resetBackgroundStateForSessionSwitch、SHELL_TOOL_NAMES、getToolCallComponent、ToolCallRouter`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SHELL_TOOL_NAMES、THROTTLE_REFILL_INTERVAL_MS、THROTTLE_BURST_SIZE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('includes droppedLines count in terminal notification text', () => {、it('settles as completed when exit and close both report null code and null signal', async () => {、it('blocks session clearing while a background shell is still running', async () => {、it('keeps sub-command in confirmation scope when AST read-only check fails', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `package-lock.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/ui/commands/clearCommand.ts`
- `packages/cli/src/ui/hooks/useResumeCommand.ts`
- `packages/cli/src/ui/utils/backgroundWorkUtils.ts`
- `packages/core/src/permissions/permission-manager.ts`
- `packages/core/src/permissions/rule-parser.ts`
- `packages/core/src/tools/monitor.ts`
- `packages/vscode-ide-companion/src/webview/components/messages/toolcalls/index.tsx`
- `packages/webui/src/components/ChatViewer/ChatViewer.tsx`
- `packages/webui/src/components/toolcalls/index.ts`
- `packages/webui/src/components/toolcalls/routing.ts`
- `packages/webui/src/index.ts`

### 测试与验证文件
- `packages/cli/src/nonInteractiveCli.test.ts`
- `packages/cli/src/ui/commands/clearCommand.test.ts`
- `packages/cli/src/ui/hooks/useResumeCommand.test.ts`
- `packages/cli/src/ui/utils/backgroundWorkUtils.test.ts`
- `packages/cli/src/utils/errors.test.ts`
- `packages/core/src/services/monitorRegistry.test.ts`
- `packages/core/src/tools/monitor.test.ts`
- `packages/vscode-ide-companion/src/webview/components/messages/toolcalls/index.test.tsx`
- `packages/webui/src/components/toolcalls/routing.test.ts`

### 文档、配置与发布材料
- `package-lock.json`

## 代码变更线索

### 新增或调整的实现符号
- `hasBlockingBackgroundWork`
- `resetBackgroundStateForSessionSwitch`
- `SHELL_TOOL_NAMES`
- `getToolCallComponent`
- `ToolCallRouter`

### 接口、命令或环境变量
- `SHELL_TOOL_NAMES`
- `THROTTLE_REFILL_INTERVAL_MS`
- `THROTTLE_BURST_SIZE`

### 测试用例线索
- `it('includes droppedLines count in terminal notification text', () => {`
- `it('settles as completed when exit and close both report null code and null signal', async () => {`
- `it('blocks session clearing while a background shell is still running', async () => {`
- `it('keeps sub-command in confirmation scope when AST read-only check fails', async () => {`
- `it('recovers token bucket when clock moves backwards (suspend/resume)', async () => {`
- `it('forwards isFirst and isLast props to the underlying component', () => {`
- `describe('getToolCallComponent', () => {`
- `it('routes agent execution to AgentToolCall', () => {`
- `it('routes read-family kinds to ReadToolCall', () => {`
- `it('routes write to WriteToolCall', () => {`

## 代码阅读结论

- 最终实现以 PR #3792 的合入代码为准，核心判断来自 21 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
