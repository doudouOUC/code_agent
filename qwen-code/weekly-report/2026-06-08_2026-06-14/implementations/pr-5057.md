# PR #5057 最终实现文档

> 来源 PR: [fix(core): Persist file history snapshot updates](https://github.com/QwenLM/qwen-code/pull/5057)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1206 / -30，12 个文件
> 时间: merged `2026-06-13T22:47:04Z`；closed `2026-06-13T22:47:04Z`

## 实现目标

让 file history snapshot 在单轮 edit/write 后立即持久化，避免进程在下一轮 snapshot 前退出导致 resume 后 `/rewind` 丢最后一轮文件历史。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：`trackEdit` 真正新增或修复备份后追加记录最新 `file_history_snapshot`；保持 append-only payload 和按 promptId last-wins 恢复模型，补 core client/session/chat recording/ACP tests 和 E2E 计划。
- 代码落地：新增或调整的实现符号包括 `FileHistorySnapshotRecorder`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 验证方式：测试标题覆盖 `it('records the latest file history snapshot after makeSnapshot', async () => {、it('wires file history snapshot updates to chat recording', async () => {、describe('file history snapshot persistence', () => {、it('calls makeSnapshot for UserQuery turns', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `.qwen/design/2026-06-13-file-history-snapshot-persistence.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/core/src/config/config.ts`
- `packages/core/src/core/client.ts`
- `packages/core/src/services/chatRecordingService.ts`
- `packages/core/src/services/fileHistoryService.ts`

### 测试与验证文件
- `.qwen/e2e-tests/2026-06-13-file-history-snapshot-persistence.md`
- `packages/cli/src/acp-integration/session/Session.test.ts`
- `packages/core/src/config/config.test.ts`
- `packages/core/src/core/client.test.ts`
- `packages/core/src/services/chatRecordingService.test.ts`
- `packages/core/src/services/fileHistoryService.test.ts`
- `packages/core/src/services/sessionService.test.ts`

### 文档、配置与发布材料
- `.qwen/design/2026-06-13-file-history-snapshot-persistence.md`

## 代码变更线索

### 新增或调整的实现符号
- `FileHistorySnapshotRecorder`

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `it('records the latest file history snapshot after makeSnapshot', async () => {`
- `it('wires file history snapshot updates to chat recording', async () => {`
- `describe('file history snapshot persistence', () => {`
- `it('calls makeSnapshot for UserQuery turns', async () => {`
- `it('records the latest snapshot after a UserQuery snapshot', async () => {`
- `it('does not call makeSnapshot for ToolResult and Retry turns', async () => {`
- `it('swallows makeSnapshot rejection and still yields content', async () => {`
- `it('swallows recordFileHistorySnapshot errors and still yields content', async () => {`
- `describe('recordFileHistorySnapshot', () => {`
- `it('writes a system record with the serialized snapshot payload', async () => {`

## 代码阅读结论

- 最终实现以 PR #5057 的合入代码为准，核心判断来自 12 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
