# PR #4954 最终实现文档

> 来源 PR: [fix(serve): isolate per-session stats in daemon mode](https://github.com/QwenLM/qwen-code/pull/4954)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +275 / -55，10 个文件
> 时间: merged `2026-06-11T03:12:42Z`；closed `2026-06-11T03:12:42Z`

## 实现目标

修复 daemon 多 session 下 `/session/:id/stats` 返回进程全局统计的问题。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：`UiTelemetryService` 双写全局与 per-session metrics；ACP stats 改按 sessionId 读取，session cleanup 时移除 per-session 指标，测试覆盖多 session 隔离和旧全局行为。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/cli/src/acp-integration/acpAgent.ts、packages/cli/src/nonInteractiveCliCommands.ts、packages/core/src/core/client.ts、packages/core/src/followup/suggestionGenerator.ts、packages/core/src/services/sessionService.ts` 的文件级改动为锚点。
- 对外入口/配置：代码证据中出现 `EVENT_API_RESPONSE、EVENT_API_ERROR、EVENT_TOOL_CALL、MAX_CLOSED_SESSIONS、SESSION_TITLE_MAX_LENGTH`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('Per-Session Metrics Isolation', () => {、it('should isolate metrics by sessionId', () => {、it('should still accumulate to global metrics', () => {、it('should return empty metrics for unknown session', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/acpAgent.ts`
- `packages/cli/src/nonInteractiveCliCommands.ts`
- `packages/core/src/core/client.ts`
- `packages/core/src/followup/suggestionGenerator.ts`
- `packages/core/src/services/sessionService.ts`
- `packages/core/src/telemetry/loggers.ts`
- `packages/core/src/telemetry/uiTelemetry.ts`

### 测试与验证文件
- `packages/core/src/core/client.test.ts`
- `packages/core/src/telemetry/loggers.test.ts`
- `packages/core/src/telemetry/uiTelemetry.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- `EVENT_API_RESPONSE`
- `EVENT_API_ERROR`
- `EVENT_TOOL_CALL`
- `MAX_CLOSED_SESSIONS`
- `SESSION_TITLE_MAX_LENGTH`

### 测试用例线索
- `describe('Per-Session Metrics Isolation', () => {`
- `it('should isolate metrics by sessionId', () => {`
- `it('should still accumulate to global metrics', () => {`
- `it('should return empty metrics for unknown session', () => {`
- `it('should handle events without sessionId (global only)', () => {`
- `it('resetSession should clear only that session', () => {`
- `it('removeSession should prevent late events from recreating bucket', () => {`
- `it('resetSession should re-enable a closed session', () => {`
- `it('should isolate tool call metrics by session', () => {`
- `it('resetSession should not clear global metrics (replay scenario)', () => {`

## 代码阅读结论

- 最终实现以 PR #4954 的合入代码为准，核心判断来自 10 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
