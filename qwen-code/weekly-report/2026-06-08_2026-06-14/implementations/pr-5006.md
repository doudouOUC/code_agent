# PR #5006 最终实现文档

> 来源 PR: [fix(daemon): Sanitize logs and type MCP restarts](https://github.com/QwenLM/qwen-code/pull/5006)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +185 / -11，9 个文件
> 时间: merged `2026-06-12T22:23:21Z`；closed `2026-06-12T22:23:21Z`

## 实现目标

补 daemon 小修：清理日志注入风险，并补齐 pooled MCP restart 的 SDK/web-shell 类型。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：session-delete stderr 做单行脱敏，SDK `restartMcpServer` 转发 `entryIndex` 并暴露 pooled `entries[]` result，web-shell dialog 同时适配单项和池化返回。
- 代码落地：新增或调整的实现符号包括 `LOG_SAFE_RE`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `LOG_SAFE_RE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('_qwen/sessions/delete sanitizes stderr close errors', async () => {、it('forwards entryIndex and returns pool entry results', async () => {、it('_qwen/sessions/delete sanitizes stderr remove errors', async () => {、it('forwards wildcard entryIndex unchanged', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/acpHttp/dispatch.ts`
- `packages/cli/src/serve/acpHttp/jsonRpc.ts`
- `packages/core/src/tools/mcp-pool-key.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/types.ts`
- `packages/web-shell/client/components/dialogs/McpDialog.tsx`
- `packages/web-shell/client/i18n.tsx`

### 测试与验证文件
- `packages/cli/src/serve/acpHttp/transport.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `LOG_SAFE_RE`

### 接口、命令或环境变量
- `LOG_SAFE_RE`

### 测试用例线索
- `it('_qwen/sessions/delete sanitizes stderr close errors', async () => {`
- `it('forwards entryIndex and returns pool entry results', async () => {`
- `it('_qwen/sessions/delete sanitizes stderr remove errors', async () => {`
- `it('forwards wildcard entryIndex unchanged', async () => {`

## 代码阅读结论

- 最终实现以 PR #5006 的合入代码为准，核心判断来自 9 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
