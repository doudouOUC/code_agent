# PR #4906 最终实现文档

> 来源 PR: [feat(telemetry): inject TRACEPARENT env var into shell child processes](https://github.com/QwenLM/qwen-code/pull/4906)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +463 / -104，11 个文件
> 时间: merged `2026-06-10T02:15:39Z`；closed `2026-06-10T02:15:39Z`

## 实现目标

把 W3C `TRACEPARENT` 注入 shell 子进程，扩展链路追踪到 bash/hooks/monitor。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：抽出共享 `trace-context.ts`，shell spawn env 按 `outboundCorrelation.propagateTraceContext` 注入；补配置 schema、sanitize/NOOP 传播守卫和 focused telemetry/shell 测试。
- 代码落地：新增或调整的实现符号包括 `ZERO_TRACE_ID、TraceContext、extractTraceContext、getActiveSpanTraceContext、getSessionRootTraceContext、getTraceContext、formatTraceparent、setShellTracePropagation`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `ZERO_TRACE_ID、INVALID_TRACEID`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('trace-context', () => {、describe('getActiveSpanTraceContext', () => {、it('returns trace context from active span', () => {、it('returns null for NOOP span with zero traceId', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `packages/vscode-ide-companion/schemas/settings.schema.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/settingsSchema.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/sdk.ts`
- `packages/core/src/telemetry/trace-context.ts`
- `packages/core/src/utils/debugLogger.ts`
- `packages/core/src/utils/shellContextEnv.ts`

### 测试与验证文件
- `packages/core/src/telemetry/sdk.test.ts`
- `packages/core/src/telemetry/trace-context.test.ts`
- `packages/core/src/utils/debugLogger.test.ts`
- `packages/core/src/utils/shellContextEnv.test.ts`

### 文档、配置与发布材料
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `ZERO_TRACE_ID`
- `TraceContext`
- `extractTraceContext`
- `getActiveSpanTraceContext`
- `getSessionRootTraceContext`
- `getTraceContext`
- `formatTraceparent`
- `setShellTracePropagation`
- `isShellTracePropagationEnabled`

### 接口、命令或环境变量
- `ZERO_TRACE_ID`
- `INVALID_TRACEID`

### 测试用例线索
- `describe('trace-context', () => {`
- `describe('getActiveSpanTraceContext', () => {`
- `it('returns trace context from active span', () => {`
- `it('returns null for NOOP span with zero traceId', () => {`
- `it('returns null when no active span', () => {`
- `it('returns null when getActiveSpan throws', () => {`
- `describe('getSessionRootTraceContext', () => {`
- `it('returns trace context from session root span', () => {`
- `it('returns null when no session context', () => {`
- `it('returns null when session span has zero traceId', () => {`

## 代码阅读结论

- 最终实现以 PR #4906 的合入代码为准，核心判断来自 11 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
