# PR #4861 最终实现文档

> 来源 PR: [feat(serve): add per-tier HTTP rate limiting for daemon (issue #4514 T3.4)](https://github.com/QwenLM/qwen-code/pull/4861)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1051 / -1，8 个文件
> 时间: merged `2026-06-08T15:22:26Z`；closed `2026-06-08T15:22:26Z`

## 实现目标

为 `qwen serve` 增加可选的分层 HTTP rate limit，保护 daemon HTTP 面。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：新增 `rateLimit.ts` token bucket 和 Express middleware，CLI/配置接入 `--rate-limit`，按 prompt / mutation / read 三档限流；health、SSE、heartbeat、ACP 豁免，异常 fail-open，测试覆盖 bucket refill、分层 key 和路由豁免。
- 代码落地：新增或调整的实现符号包括 `RateLimitTier、RateLimitTierConfig、RateLimitConfig、RateLimiterInstance、TokenBucket、MAX_BUCKETS、GC_REQUEST_INTERVAL、GC_TIMER_INTERVAL_MS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--rate-limit、--rate-limit-prompt、--rate-limit-mutation、--rate-limit-read、--rate-limit-window-ms、--no-rate-limit、QWEN_SERVE_RATE_LIMIT、QWEN_SERVE_RATE_LIMIT_PROMPT、QWEN_SERVE_RATE_LIMIT_MUTATION、QWEN_SERVE_RATE_LIMIT_READ`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('rateLimit', () => {、describe('token bucket - continuous drip', () => {、it('allows requests up to max', () => {、it('refills tokens continuously over time', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/commands/serve.ts`
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/rateLimit.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/server.ts`
- `packages/cli/src/serve/types.ts`

### 测试与验证文件
- `packages/cli/src/serve/rateLimit.test.ts`
- `packages/cli/src/serve/server.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `RateLimitTier`
- `RateLimitTierConfig`
- `RateLimitConfig`
- `RateLimiterInstance`
- `TokenBucket`
- `MAX_BUCKETS`
- `GC_REQUEST_INTERVAL`
- `GC_TIMER_INTERVAL_MS`
- `resolveTier`
- `MAX_CLIENT_ID_LENGTH`
- `CLIENT_ID_RE`
- `normalizeIp`

### 接口、命令或环境变量
- `--rate-limit`
- `--rate-limit-prompt`
- `--rate-limit-mutation`
- `--rate-limit-read`
- `--rate-limit-window-ms`
- `--no-rate-limit`
- `QWEN_SERVE_RATE_LIMIT`
- `QWEN_SERVE_RATE_LIMIT_PROMPT`
- `QWEN_SERVE_RATE_LIMIT_MUTATION`
- `QWEN_SERVE_RATE_LIMIT_READ`
- `QWEN_SERVE_RATE_LIMIT_WINDOW_MS`
- `MAX_BUCKETS`
- `GC_REQUEST_INTERVAL`
- `GC_TIMER_INTERVAL_MS`
- `CLIENT_ID_RE`
- `MAX_CLIENT_ID_LENGTH`

### 测试用例线索
- `describe('rateLimit', () => {`
- `describe('token bucket - continuous drip', () => {`
- `it('allows requests up to max', () => {`
- `it('refills tokens continuously over time', () => {`
- `it('includes Retry-After header on 429', () => {`
- `it('handles clock skew (negative elapsed) via Math.max(0, elapsed)', () => {`
- `describe('tier resolution', () => {`
- `it('classifies POST .../prompt as prompt tier', () => {`
- `it('classifies POST /session as mutation tier', () => {`
- `it('classifies GET as read tier', () => {`

## 代码阅读结论

- 最终实现以 PR #4861 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
