# PR #4862 最终实现文档

> 来源 PR: [feat(test): add daemon connection stress test + refactor perf harness](https://github.com/QwenLM/qwen-code/pull/4862)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1268 / -505，10 个文件
> 时间: merged `2026-06-08T15:22:54Z`；closed `2026-06-08T15:22:54Z`

## 实现目标

补 daemon 连接压力测试，并整理 perf/baseline 测试 harness。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：抽出 `_daemon-harness`、benchmark/report helper 和 mock ACP child；新增 `QWEN_LOADTEST_ENABLED=1` 门控的连接、prompt、SSE、crash recovery、resource snapshot 场景，避免默认 CI 跑重型压力测试。
- 验证方式：测试标题覆盖 `describe('mock ACP agent type compliance', () => {、it('satisfies required Agent interface methods', () => {、'daemon connection stress test (mock ACP, POSIX-only)',、it('rapid lifecycle: create-prompt-close cycles', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- 无。

### 测试与验证文件
- `integration-tests/cli/_daemon-benchmark-helpers.ts`
- `integration-tests/cli/_daemon-harness.ts`
- `integration-tests/cli/_daemon-perf-report.ts`
- `integration-tests/cli/mock-acp-typecheck.test.ts`
- `integration-tests/cli/qwen-daemon-loadtest.test.ts`
- `integration-tests/cli/qwen-daemon-vs-cli-benchmark.test.ts`
- `integration-tests/cli/qwen-serve-baseline.test.ts`
- `integration-tests/fixtures/mock-acp-child/agent.mjs`
- `integration-tests/vitest.config.ts`
- `integration-tests/vitest.loadtest.config.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `describe('mock ACP agent type compliance', () => {`
- `it('satisfies required Agent interface methods', () => {`
- `'daemon connection stress test (mock ACP, POSIX-only)',`
- `it('rapid lifecycle: create-prompt-close cycles', async () => {`
- `it('SSE slow consumer triggers eviction through HTTP', async () => {`
- `it('Last-Event-ID reconnect under concurrent load', async () => {`
- `it('ACP child crash → session error recovery', async () => {`
- `it('burst: concurrent sessions with mock prompts', async () => {`

## 代码阅读结论

- 最终实现以 PR #4862 的合入代码为准，核心判断来自 10 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
