# PR #5105 最终实现文档

> 来源 PR: [feat(acp): dedicated agent permission dialog via _meta.toolName (follow-up to #5085)](https://github.com/QwenLM/qwen-code/pull/5105)
> 状态: ✅ merged
> 变更规模: +251/-5，14 个文件
> 时间: created `06-14`；merged/closed `06-14`

## 解决的问题

在不改 ACP `kind` 的前提下，为 Agent 权限请求恢复专属 “Launch this agent?” UI。

## 最终实现方案

`Session.ts` 在 `session/request_permission` 的 `toolCall._meta.toolName` 镜像规范工具名；VS Code `PermissionDrawer` 和 daemon web-shell `ToolApproval` 读取 `_meta.toolName === 'agent'` 后展示 agent 专属标题与描述。

## 归档口径

- 归档周目录：`2026-06-08_2026-06-14`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
