# PR #3893 最终实现文档

> 来源 PR: [feat(telemetry): add sensitive span attribute opt-in](https://github.com/QwenLM/qwen-code/pull/3893)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +414 / -52，15 个文件
> 时间: merged `2026-05-07T16:36:08Z`；closed `2026-05-07T16:36:08Z`

## 实现目标

围绕 `add sensitive span attribute opt-in` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/settingsSchema.ts、packages/core/src/config/config.ts、packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`，这些文件承载 `add sensitive span attribute opt-in` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `LogToSpanProcessorOptions、MAX_RESPONSE_TEXT_LENGTH、RESPONSE_TEXT_TRUNCATION_SUFFIX、MAX_RESPONSE_TEXT_PREFIX_LENGTH`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES、SENSITIVE_ATTRIBUTE_KEYS、MAX_RESPONSE_TEXT_LENGTH、RESPONSE_TEXT_TRUNCATION_SUFFIX、MAX_RESPONSE_TEXT_PREFIX_LENGTH`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should use includeSensitiveSpanAttributes from settings', async () => {、it('should default includeSensitiveSpanAttributes to false', async () => {、it('should prioritize QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES over settings', async () => {、it("should treat QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES='1' as true", async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/development/telemetry.md、docs/users/configuration/settings.md、packages/vscode-ide-companion/schemas/settings.schema.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/settingsSchema.ts`
- `packages/core/src/config/config.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/config.ts`
- `packages/core/src/telemetry/log-to-span-processor.ts`
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/cli/src/config/config.test.ts`
- `packages/core/src/config/config.test.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/telemetry/config.test.ts`
- `packages/core/src/telemetry/log-to-span-processor.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- `docs/developers/development/telemetry.md`
- `docs/users/configuration/settings.md`
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `LogToSpanProcessorOptions`
- `MAX_RESPONSE_TEXT_LENGTH`
- `RESPONSE_TEXT_TRUNCATION_SUFFIX`
- `MAX_RESPONSE_TEXT_PREFIX_LENGTH`

### 接口、命令或环境变量
- `QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES`
- `SENSITIVE_ATTRIBUTE_KEYS`
- `MAX_RESPONSE_TEXT_LENGTH`
- `RESPONSE_TEXT_TRUNCATION_SUFFIX`
- `MAX_RESPONSE_TEXT_PREFIX_LENGTH`

### 测试用例线索
- `it('should use includeSensitiveSpanAttributes from settings', async () => {`
- `it('should default includeSensitiveSpanAttributes to false', async () => {`
- `it('should prioritize QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES over settings', async () => {`
- `it("should treat QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES='1' as true", async () => {`
- `it("should treat QWEN_TELEMETRY_INCLUDE_SENSITIVE_SPAN_ATTRIBUTES='false' as false", async () => {`
- `it('should return provided includeSensitiveSpanAttributes setting', () => {`
- `it('should default includeSensitiveSpanAttributes to false', () => {`
- `it('defaults includeSensitiveSpanAttributes to false', async () => {`
- `it('parses includeSensitiveSpanAttributes from settings and env', async () => {`
- `it('keeps sensitive attributes when explicitly enabled', async () => {`

## 代码阅读结论

- 最终实现以 PR #3893 的合入代码为准，核心判断来自 15 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
