# PR #3986 最终实现文档

> 来源 PR: [feat(telemetry) suppress OpenTelemetry diagnostics from UI](https://github.com/QwenLM/qwen-code/pull/3986)
> 状态: ✅ merged
> 变更规模: +93/-3，2 个文件
> 时间: created `05-09`；merged/closed `05-09`

## 解决的问题

OTel 诊断从 UI 改路由到 debug log

## 最终实现方案

移除 `DiagConsoleLogger`，改用自建 DiagLogger 路由到 `createDebugLogger('OTEL')`，保留 `DiagLogLevel.WARN`。 `diag.setLogger` 加载期执行，但 debug logger 写日志时惰性解析 session（测试覆盖 `[OTEL]` 前缀），不受时序影响。

## 审查结论

- 一致性: 移除 `DiagConsoleLogger`，改用自建 DiagLogger 路由到 `createDebugLogger('OTEL')`，保留 `DiagLogLevel.WARN`。
- 描述准确性: 准确。
- 正确性: `diag.setLogger` 加载期执行，但 debug logger 写日志时惰性解析 session（测试覆盖 `[OTEL]` 前缀），不受时序影响。
- 结论: 小而正确的日志路由改动。

## 归档口径

- 归档周目录：`2026-05-04_2026-05-10`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
