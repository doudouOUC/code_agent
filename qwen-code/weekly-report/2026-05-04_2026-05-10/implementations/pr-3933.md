# PR #3933 最终实现文档

> 来源 PR: [[codex] fix monitor notifications for subagents](https://github.com/QwenLM/qwen-code/pull/3933)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1772 / -82，19 个文件
> 时间: merged `2026-05-09T05:49:24Z`；closed `2026-05-09T05:49:24Z`

## 实现目标

围绕 `[codex] fix monitor notifications for subagents` 做实现调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/agents/agent-transcript.ts、packages/core/src/agents/background-agent-resume.ts、packages/core/src/agents/background-tasks.ts`，这些文件承载 `[codex] fix monitor notifications for subagents` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `MessageWaiter、AgentExternalInput、createLocalExternalInputQueue、MonitorOwnerLifecycleCallback`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `ROUND_END、EXTERNAL_MESSAGE_PREFIX、EXTERNAL_MESSAGE、MAX_TURNS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('routes owned monitor notifications into a resumed agent queue', async () => {、describe('waitForMessages', () => {、it('resolves with queued input when a running agent is notified', async () => {、it('resolves empty when the wait signal is aborted', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/agents/agent-transcript.ts`
- `packages/core/src/agents/background-agent-resume.ts`
- `packages/core/src/agents/background-tasks.ts`
- `packages/core/src/agents/runtime/agent-core.ts`
- `packages/core/src/agents/runtime/agent-events.ts`
- `packages/core/src/agents/runtime/agent-headless.ts`
- `packages/core/src/agents/runtime/agent-types.ts`
- `packages/core/src/services/chatRecordingService.ts`
- `packages/core/src/services/monitorRegistry.ts`
- `packages/core/src/tools/agent/agent.ts`
- `packages/core/src/tools/monitor.ts`

### 测试与验证文件
- `packages/core/src/agents/agent-transcript.test.ts`
- `packages/core/src/agents/background-agent-resume.test.ts`
- `packages/core/src/agents/background-tasks.test.ts`
- `packages/core/src/agents/runtime/agent-core.test.ts`
- `packages/core/src/agents/runtime/agent-headless.test.ts`
- `packages/core/src/services/monitorRegistry.test.ts`
- `packages/core/src/tools/agent/agent.test.ts`
- `packages/core/src/tools/monitor.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `MessageWaiter`
- `AgentExternalInput`
- `createLocalExternalInputQueue`
- `MonitorOwnerLifecycleCallback`

### 接口、命令或环境变量
- `ROUND_END`
- `EXTERNAL_MESSAGE_PREFIX`
- `EXTERNAL_MESSAGE`
- `MAX_TURNS`

### 测试用例线索
- `it('routes owned monitor notifications into a resumed agent queue', async () => {`
- `describe('waitForMessages', () => {`
- `it('resolves with queued input when a running agent is notified', async () => {`
- `it('resolves empty when the wait signal is aborted', async () => {`
- `it('should wait for external notification after a no-tool response', async () => {`
- `it('should drain queued external notification before finalizing', async () => {`
- `it('should not idle-wait when max turns prevents another round', async () => {`
- `it('routes owner monitor event notifications to the owning agent callback only', () => {`
- `it('does not fall back to the parent callback when owner callback is missing', () => {`
- `it('routes owner monitor terminal notifications to the owning agent callback', () => {`

## 代码阅读结论

- 最终实现以 PR #3933 的合入代码为准，核心判断来自 19 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
