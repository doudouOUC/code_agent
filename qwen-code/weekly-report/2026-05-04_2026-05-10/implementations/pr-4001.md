# PR #4001 最终实现文档

> 来源 PR: [feat(cli): add structured JSON schema output](https://github.com/QwenLM/qwen-code/pull/4001)
> 状态: ⬜ closed
> 变更规模: +1543/-283，25 个文件
> 时间: created `05-09`；merged/closed `05-11`

## 解决的问题

未作为已落地实现；结构化 JSON 输出，与先合入的 #3598 重复而废弃。

## 最终实现方案

（就代码本身）— `structured-output.ts`（AJV 启动期校验、`build()` 校验、`terminalResult`）+ 内部工具接线 + 三语言 SDK `structured_result` 协议字段。 （N/A，废弃）— 同功能 `--json-schema` 由 #3598 先合入（早 18 分钟），本 PR 随即关闭；评论指出 cli/core 部分与 main 重复归零，仅 SDK 协议增量独有。

## 审查结论

- 一致性: （就代码本身）— `structured-output.ts`（AJV 启动期校验、`build()` 校验、`terminalResult`）+ 内部工具接线 + 三语言 SDK `structured_result` 协议字段。
- 描述准确性: 属实，但未提与 #3598 的重叠。
- 正确性: （N/A，废弃）— 同功能 `--json-schema` 由 #3598 先合入（早 18 分钟），本 PR 随即关闭；评论指出 cli/core 部分与 main 重复归零，仅 SDK 协议增量独有。
- 结论: 因并行重复、#3598 先落地而关闭；唯一独有价值是 SDK `structured_result` 协议，可另开小 PR。

## 归档口径

- 归档周目录：`2026-05-04_2026-05-10`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
