# PR #3879 最终实现文档

> 来源 PR: [feat(core): add reactive compression on context overflow](https://github.com/QwenLM/qwen-code/pull/3879)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +797 / -12，5 个文件
> 时间: merged `2026-05-09T03:06:49Z`；closed `2026-05-09T03:06:49Z`

## 实现目标

围绕 `add reactive compression on context overflow` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/geminiChat.ts、packages/core/src/services/chatCompressionService.ts、packages/core/src/utils/contextLengthError.ts`，这些文件承载 `add reactive compression on context overflow` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `CompactTrigger、TryCompressOptions、ContextLengthExceededInfo、MAX_COLLECT_DEPTH、TIMEOUT_PATTERNS、CONTEXT_LENGTH_PATTERNS、parseInteger、parseTokenCounts`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `MAX_COLLECT_DEPTH、TIMEOUT_PATTERNS、CONTEXT_LENGTH_PATTERNS、DEFAULT_TOKEN_LIMIT、ESCALATED_MAX_TOKENS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('reactively compresses and retries once after a context overflow error', async () => {、it('does not attempt reactive compression more than once per send', async () => {、it('does not emit a duplicate RETRY after reactive compression follows another retry', async () => {、it('surfaces the original context overflow when reactive compression is a NOOP', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/geminiChat.ts`
- `packages/core/src/services/chatCompressionService.ts`
- `packages/core/src/utils/contextLengthError.ts`

### 测试与验证文件
- `packages/core/src/core/geminiChat.test.ts`
- `packages/core/src/utils/contextLengthError.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `CompactTrigger`
- `TryCompressOptions`
- `ContextLengthExceededInfo`
- `MAX_COLLECT_DEPTH`
- `TIMEOUT_PATTERNS`
- `CONTEXT_LENGTH_PATTERNS`
- `parseInteger`
- `parseTokenCounts`
- `tryParseEmbeddedJson`
- `collectStrings`
- `uniqueNonEmpty`
- `getContextLengthExceededInfo`

### 接口、命令或环境变量
- `MAX_COLLECT_DEPTH`
- `TIMEOUT_PATTERNS`
- `CONTEXT_LENGTH_PATTERNS`
- `DEFAULT_TOKEN_LIMIT`
- `ESCALATED_MAX_TOKENS`

### 测试用例线索
- `it('reactively compresses and retries once after a context overflow error', async () => {`
- `it('does not attempt reactive compression more than once per send', async () => {`
- `it('does not emit a duplicate RETRY after reactive compression follows another retry', async () => {`
- `it('surfaces the original context overflow when reactive compression is a NOOP', async () => {`
- `it('releases the send-lock when reactive compression throws', async () => {`
- `describe('contextLengthError', () => {`
- `it('parses prompt-too-long actual and limit token counts', () => {`
- `it('parses OpenAI-style maximum context length token counts', () => {`
- `it('extracts nested JSON error messages from strings', () => {`
- `it('extracts nested error object messages', () => {`

## 代码阅读结论

- 最终实现以 PR #3879 的合入代码为准，核心判断来自 5 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
