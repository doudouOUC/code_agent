# PR #3872 最终实现文档

> 来源 PR: [fix(core): shrink file diff session records](https://github.com/QwenLM/qwen-code/pull/3872)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +616 / -20，12 个文件
> 时间: merged `2026-05-06T12:48:25Z`；closed `2026-05-06T12:48:25Z`

## 实现目标

围绕 `shrink file diff session records` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.ts、packages/cli/src/ui/components/messages/ToolMessage.tsx、packages/cli/src/ui/utils/export/collect.ts`，这些文件承载 `shrink file diff session records` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `buildTruncatedDiffReplayText、DiffResultDisplay、buildTruncatedDiffExportText、SESSION_FILE_DIFF_AGGREGATE_CHAR_LIMIT、SESSION_FILE_DIFF_CHAR_LIMIT、SESSION_FILE_CONTENT_CHAR_LIMIT、isFileDiffDisplay、stringLength`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SESSION_FILE_DIFF_AGGREGATE_CHAR_LIMIT、SESSION_FILE_DIFF_CHAR_LIMIT、SESSION_FILE_CONTENT_CHAR_LIMIT`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should not replay truncated session previews as full diffs', async () => {、it('renders a saved-session preview notice for truncated diff results', () => {、describe('collectSessionData', () => {、it('skips line-count fallback for truncated saved-session previews', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.ts`
- `packages/cli/src/ui/components/messages/ToolMessage.tsx`
- `packages/cli/src/ui/utils/export/collect.ts`
- `packages/cli/src/ui/utils/export/normalize.ts`
- `packages/cli/src/utils/truncatedDiffPreview.ts`
- `packages/core/src/services/chatRecordingService.ts`
- `packages/core/src/tools/tools.ts`

### 测试与验证文件
- `packages/cli/src/acp-integration/session/emitters/ToolCallEmitter.test.ts`
- `packages/cli/src/ui/components/messages/ToolMessage.test.tsx`
- `packages/cli/src/ui/utils/export/collect.test.ts`
- `packages/cli/src/ui/utils/export/normalize.test.ts`
- `packages/core/src/services/chatRecordingService.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `buildTruncatedDiffReplayText`
- `DiffResultDisplay`
- `buildTruncatedDiffExportText`
- `SESSION_FILE_DIFF_AGGREGATE_CHAR_LIMIT`
- `SESSION_FILE_DIFF_CHAR_LIMIT`
- `SESSION_FILE_CONTENT_CHAR_LIMIT`
- `isFileDiffDisplay`
- `stringLength`
- `truncateMiddleForSession`
- `buildSyntheticDiffPreview`
- `sanitizeFileDiffForRecording`
- `sanitizeToolCallResultForRecording`

### 接口、命令或环境变量
- `SESSION_FILE_DIFF_AGGREGATE_CHAR_LIMIT`
- `SESSION_FILE_DIFF_CHAR_LIMIT`
- `SESSION_FILE_CONTENT_CHAR_LIMIT`

### 测试用例线索
- `it('should not replay truncated session previews as full diffs', async () => {`
- `it('renders a saved-session preview notice for truncated diff results', () => {`
- `describe('collectSessionData', () => {`
- `it('skips line-count fallback for truncated saved-session previews', async () => {`
- `describe('normalizeSessionData', () => {`
- `it('does not export truncated saved-session previews as full diffs', () => {`
- `it('should keep small file diff resultDisplay unchanged', async () => {`
- `it('should shrink large file diff resultDisplay without mutating input', async () => {`
- `it('should continue stripping nested tool calls from task execution results', async () => {`

## 代码阅读结论

- 最终实现以 PR #3872 的合入代码为准，核心判断来自 12 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
