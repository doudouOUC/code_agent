# PR #4126 最终实现文档

> 来源 PR: [feat(telemetry): unify span creation paths for hierarchical trace tree](https://github.com/QwenLM/qwen-code/pull/4126)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1739 / -877，10 个文件
> 时间: merged `2026-05-16T14:29:56Z`；closed `2026-05-16T14:29:56Z`

## 实现目标

围绕 `unify span creation paths for hierarchical trace tree` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/client.ts、packages/core/src/core/coreToolScheduler.ts、packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`，这些文件承载 `unify span creation paths for hierarchical trace tree` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `runInToolSpanContext、startToolExecutionSpan、TOOL_SPAN_STATUS_TOOL_CANCELLED`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `QWEN_OAUTH、PATH_ARG_KEYS、EXECUTION_DENIED、TOOL_FAILURE_KIND_PRE_HOOK_BLOCKED、TOOL_SPAN_STATUS_PRE_HOOK_BLOCKED、TOOL_FAILURE_KIND_POST_HOOK_STOPPED、TOOL_SPAN_STATUS_POST_HOOK_STOPPED、FS_PATH_TOOL_NAMES、TOOL_FAILURE_KIND_TOOL_ERROR、TOOL_SPAN_STATUS_TOOL_ERROR`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('propagates error when content generation fails', async () => {、it('propagates error when content generation is aborted', async () => {、it('does not set status when no metadata is passed', () => {、it('creates a tool execution span as child of tool span via runInToolSpanContext', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/workflow-tracing-gaps.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/client.ts`
- `packages/core/src/core/coreToolScheduler.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/core/src/core/client.test.ts`
- `packages/core/src/core/coreToolScheduler.test.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- `docs/design/workflow-tracing-gaps.md`

## 代码变更线索

### 新增或调整的实现符号
- `runInToolSpanContext`
- `startToolExecutionSpan`
- `TOOL_SPAN_STATUS_TOOL_CANCELLED`

### 接口、命令或环境变量
- `QWEN_OAUTH`
- `PATH_ARG_KEYS`
- `EXECUTION_DENIED`
- `TOOL_FAILURE_KIND_PRE_HOOK_BLOCKED`
- `TOOL_SPAN_STATUS_PRE_HOOK_BLOCKED`
- `TOOL_FAILURE_KIND_POST_HOOK_STOPPED`
- `TOOL_SPAN_STATUS_POST_HOOK_STOPPED`
- `FS_PATH_TOOL_NAMES`
- `TOOL_FAILURE_KIND_TOOL_ERROR`
- `TOOL_SPAN_STATUS_TOOL_ERROR`

### 测试用例线索
- `it('propagates error when content generation fails', async () => {`
- `it('propagates error when content generation is aborted', async () => {`
- `it('does not set status when no metadata is passed', () => {`
- `it('creates a tool execution span as child of tool span via runInToolSpanContext', () => {`
- `it('falls back gracefully when no tool span is active', () => {`
- `describe('toolContext ALS lifecycle', () => {`
- `it('runInToolSpanContext scopes toolContext via run(), not enterWith', () => {`
- `it('endToolSpan without metadata preserves pre-set status', () => {`
- `it('marks cancellation spans with UNSET status', async () => {`
- `it('marks successful tool calls with OK status via endToolSpan', async () => {`

## 代码阅读结论

- 最终实现以 PR #4126 的合入代码为准，核心判断来自 10 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
