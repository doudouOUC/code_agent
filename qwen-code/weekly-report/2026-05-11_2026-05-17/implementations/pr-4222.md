# PR #4222 最终实现文档

> 来源 PR: [[codex] Add daemon session load/resume](https://github.com/QwenLM/qwen-code/pull/4222)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +2078 / -51，16 个文件
> 时间: merged `2026-05-17T04:58:47Z`；closed `2026-05-17T04:58:47Z`

## 实现目标

围绕 `[codex] Add daemon session load/resume` 做实现调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/acpAgent.ts、packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/httpAcpBridge.ts`，这些文件承载 `[codex] Add daemon session load/resume` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `BridgeRestoreSessionRequest、BridgeSessionState、BridgeRestoredSession、RestoreInProgressError、InFlightRestore、restoreSession、parseOptionalWorkspaceCwd、RestoreSessionRequest`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `POST /session/:id/load、POST /session/:id/resume、--max-sessions、--unhandled-rejections、MAX_WORKSPACE_PATH_LENGTH`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('loads an existing ACP session and registers it for daemon routes', async () => {、it('buffers load replay events until the restored session is registered', async () => {、it('resumes an existing ACP session without calling session/load', async () => {、it('attaches to an already live session instead of loading it twice', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md、docs/users/qwen-serve.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/acpAgent.ts`
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/DaemonSessionClient.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/daemon/types.ts`
- `packages/sdk-typescript/src/index.ts`

### 测试与验证文件
- `packages/cli/src/acp-integration/acpAgent.test.ts`
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`
- `packages/sdk-typescript/test/unit/DaemonSessionClient.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`
- `docs/users/qwen-serve.md`

## 代码变更线索

### 新增或调整的实现符号
- `BridgeRestoreSessionRequest`
- `BridgeSessionState`
- `BridgeRestoredSession`
- `RestoreInProgressError`
- `InFlightRestore`
- `restoreSession`
- `parseOptionalWorkspaceCwd`
- `RestoreSessionRequest`
- `DaemonSessionState`
- `DaemonRestoredSession`

### 接口、命令或环境变量
- `POST /session/:id/load`
- `POST /session/:id/resume`
- `--max-sessions`
- `--unhandled-rejections`
- `MAX_WORKSPACE_PATH_LENGTH`

### 测试用例线索
- `it('loads an existing ACP session and registers it for daemon routes', async () => {`
- `it('buffers load replay events until the restored session is registered', async () => {`
- `it('resumes an existing ACP session without calling session/load', async () => {`
- `it('attaches to an already live session instead of loading it twice', async () => {`
- `it('maps an ACP missing persisted session to SessionNotFoundError', async () => {`
- `it('rejects load while a resume for the same session is in flight', async () => {`
- `it('does not kill a shared channel when one of multiple pending restores fails', async () => {`
- `describe('POST /session/:id/load and /resume', () => {`
- `it('falls back to bound workspace and uses the route session id', async () => {`
- `it('passes explicit cwd through to the bridge', async () => {`

## 代码阅读结论

- 最终实现以 PR #4222 的合入代码为准，核心判断来自 16 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
