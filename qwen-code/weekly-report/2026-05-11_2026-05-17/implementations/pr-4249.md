# PR #4249 最终实现文档

> 来源 PR: [feat(serve): workspace memory and agents CRUD (#4175 Wave 4 PR 16)](https://github.com/QwenLM/qwen-code/pull/4249)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +5318 / -8，23 个文件
> 时间: merged `2026-05-18T06:27:00Z`；closed `2026-05-18T06:27:00Z`

## 实现目标

围绕 `workspace memory and agents CRUD (#4175 Wave 4 PR 16)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/debugMode.ts、packages/cli/src/serve/httpAcpBridge.ts`，这些文件承载 `workspace memory and agents CRUD (#4175 Wave 4 PR 16)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `ServeContextFileScope、ServeWorkspaceMemoryFile、ServeWorkspaceMemoryStatus、ServeAgentLevel、ServeWorkspaceAgentSummary、ServeWorkspaceAgentDetail、ServeWorkspaceAgentsStatus、createIdleWorkspaceMemoryStatus`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `GET /workspace/agents、GET /workspace/agents/:agentType、GET /workspace/memory、POST /workspace/memory、POST /workspace/agents、POST /workspace/agents/:agentType、DELETE /workspace/agents/:agentType、--max-connections、--redact-paths、--redact-errors`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `if (raw.length > 128 || !/^[A-Za-z0-9._:-]+$/.test(raw)) {、describe('workspace agents routes', () => {、it('lists built-in agents alongside on-disk project agents', async () => {、it('returns the full detail (with systemPrompt) on GET /workspace/agents/:agentType', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/debugMode.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/cli/src/serve/status.ts`
- `packages/cli/src/serve/workspaceAgents.ts`
- `packages/cli/src/serve/workspaceMemory.ts`
- `packages/core/src/index.ts`
- `packages/core/src/memory/writeContextFile.ts`
- `packages/core/src/subagents/index.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/events.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/daemon/types.ts`

### 测试与验证文件
- `packages/cli/src/serve/fs/contract.test.ts`
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/cli/src/serve/status.test.ts`
- `packages/cli/src/serve/workspaceAgents.test.ts`
- `packages/cli/src/serve/workspaceMemory.test.ts`
- `packages/core/src/memory/writeContextFile.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`
- `packages/sdk-typescript/test/unit/daemonEvents.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `ServeContextFileScope`
- `ServeWorkspaceMemoryFile`
- `ServeWorkspaceMemoryStatus`
- `ServeAgentLevel`
- `ServeWorkspaceAgentSummary`
- `ServeWorkspaceAgentDetail`
- `ServeWorkspaceAgentsStatus`
- `createIdleWorkspaceMemoryStatus`
- `createIdleWorkspaceAgentsStatus`
- `Config`
- `SubagentConfig`
- `SubagentLevel`

### 接口、命令或环境变量
- `GET /workspace/agents`
- `GET /workspace/agents/:agentType`
- `GET /workspace/memory`
- `POST /workspace/memory`
- `POST /workspace/agents`
- `POST /workspace/agents/:agentType`
- `DELETE /workspace/agents/:agentType`
- `--max-connections`
- `--redact-paths`
- `--redact-errors`
- `QWEN_SERVE_DEBUG`
- `STATUS_SCHEMA_VERSION`
- `ALREADY_EXISTS`
- `VALIDATION_ERROR`
- `INVALID_CONFIG`
- `INVALID_NAME`
- `TOOL_NOT_FOUND`
- `FILE_ERROR`

### 测试用例线索
- `if (raw.length > 128 || !/^[A-Za-z0-9._:-]+$/.test(raw)) {`
- `describe('workspace agents routes', () => {`
- `it('lists built-in agents alongside on-disk project agents', async () => {`
- `it('returns the full detail (with systemPrompt) on GET /workspace/agents/:agentType', async () => {`
- `it('returns 404 agent_not_found for unknown agent', async () => {`
- `it('matches frontmatter name case-insensitively', async () => {`
- `it('creates a project-level agent and emits agent_changed', async () => {`
- `it('creates a user-level agent when scope=global', async () => {`
- `it('returns 409 agent_already_exists when name collides at the same level', async () => {`
- `it('returns 422 invalid_config for missing required fields', async () => {`

## 代码阅读结论

- 最终实现以 PR #4249 的合入代码为准，核心判断来自 23 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
