# PR #4071 最终实现文档

> 来源 PR: [feat(telemetry): add hierarchical session tracing spans](https://github.com/QwenLM/qwen-code/pull/4071)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1318 / -409，6 个文件
> 时间: merged `2026-05-12T16:03:31Z`；closed `2026-05-12T16:03:31Z`

## 实现目标

围绕 `add hierarchical session tracing spans` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/client.ts、packages/core/src/telemetry/constants.ts、packages/core/src/telemetry/index.ts`，这些文件承载 `add hierarchical session tracing spans` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `SPAN_INTERACTION、SPAN_LLM_REQUEST、SPAN_TOOL、SPAN_TOOL_EXECUTION、Attributes、Span、InteractionStatus、StartInteractionOptions`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SPAN_INTERACTION、SPAN_LLM_REQUEST、SPAN_TOOL、SPAN_TOOL_EXECUTION、SERVICE_NAME、NOOP_SPAN、SPAN_TTL_MS、EMPTY_RELEVANT_AUTO_MEMORY_RESULT、FILE_READ_CACHE、MAX_TURNS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('session-tracing', () => {、describe('interaction spans', () => {、it('starts and ends an interaction span with ok status', () => {、it('ends interaction span with error status', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/client.ts`
- `packages/core/src/telemetry/constants.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/sdk.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `SPAN_INTERACTION`
- `SPAN_LLM_REQUEST`
- `SPAN_TOOL`
- `SPAN_TOOL_EXECUTION`
- `Attributes`
- `Span`
- `InteractionStatus`
- `StartInteractionOptions`
- `EndInteractionOptions`
- `LLMRequestMetadata`
- `ToolSpanMetadata`
- `SpanContext`

### 接口、命令或环境变量
- `SPAN_INTERACTION`
- `SPAN_LLM_REQUEST`
- `SPAN_TOOL`
- `SPAN_TOOL_EXECUTION`
- `SERVICE_NAME`
- `NOOP_SPAN`
- `SPAN_TTL_MS`
- `EMPTY_RELEVANT_AUTO_MEMORY_RESULT`
- `FILE_READ_CACHE`
- `MAX_TURNS`

### 测试用例线索
- `describe('session-tracing', () => {`
- `describe('interaction spans', () => {`
- `it('starts and ends an interaction span with ok status', () => {`
- `it('ends interaction span with error status', () => {`
- `it('ends interaction span with cancelled status', () => {`
- `it('is idempotent — ending twice does not double-end', () => {`
- `it('no-ops when SDK is not initialized', () => {`
- `it('increments interaction sequence', () => {`
- `it('records duration_ms and turn_status on end', () => {`
- `describe('LLM request spans', () => {`

## 代码阅读结论

- 最终实现以 PR #4071 的合入代码为准，核心判断来自 6 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
