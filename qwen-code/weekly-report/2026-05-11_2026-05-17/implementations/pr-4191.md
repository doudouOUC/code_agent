# PR #4191 最终实现文档

> 来源 PR: [[codex] feat(serve): add capability registry protocol versions](https://github.com/QwenLM/qwen-code/pull/4191)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +212 / -39，10 个文件
> 时间: merged `2026-05-16T10:07:39Z`；closed `2026-05-16T10:07:39Z`

## 实现目标

围绕 `[codex] feat(serve): add capability registry protocol versions` 做实现调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/index.ts、packages/cli/src/serve/server.ts`，这些文件承载 `[codex] feat(serve): add capability registry protocol versions` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `SERVE_PROTOCOL_VERSION、SUPPORTED_SERVE_PROTOCOL_VERSIONS、ServeProtocolVersion、ServeProtocolVersions、ServeCapabilityDescriptor、SERVE_CAPABILITY_REGISTRY、ServeFeature、SERVE_FEATURES`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SERVE_PROTOCOL_VERSION、SUPPORTED_SERVE_PROTOCOL_VERSIONS、SERVE_CAPABILITY_REGISTRY、SERVE_FEATURES、STAGE1_FEATURES`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('serve capability registry', () => {、it('returns a fresh ordered feature list', () => {、it('marks every current feature as v1', () => {、it('accepts old v1 envelopes without protocolVersions', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/index.ts`
- `packages/cli/src/serve/server.ts`
- `packages/cli/src/serve/types.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/daemon/types.ts`
- `packages/sdk-typescript/src/index.ts`

### 测试与验证文件
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`

## 代码变更线索

### 新增或调整的实现符号
- `SERVE_PROTOCOL_VERSION`
- `SUPPORTED_SERVE_PROTOCOL_VERSIONS`
- `ServeProtocolVersion`
- `ServeProtocolVersions`
- `ServeCapabilityDescriptor`
- `SERVE_CAPABILITY_REGISTRY`
- `ServeFeature`
- `SERVE_FEATURES`
- `getServeFeatures`
- `getServeProtocolVersions`
- `STAGE1_FEATURES`
- `Stage1Feature`

### 接口、命令或环境变量
- `SERVE_PROTOCOL_VERSION`
- `SUPPORTED_SERVE_PROTOCOL_VERSIONS`
- `SERVE_CAPABILITY_REGISTRY`
- `SERVE_FEATURES`
- `STAGE1_FEATURES`

### 测试用例线索
- `describe('serve capability registry', () => {`
- `it('returns a fresh ordered feature list', () => {`
- `it('marks every current feature as v1', () => {`
- `it('accepts old v1 envelopes without protocolVersions', async () => {`
- `it('returns a fresh ordered registered feature list', () => {`
- `it('advertises current-protocol features separately from the registry', () => {`
- `it('marks every current feature with its historical v1 origin', () => {`
- `it('returns protocol version metadata with a fresh supported array', () => {`

## 代码阅读结论

- 最终实现以 PR #4191 的合入代码为准，核心判断来自 10 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
