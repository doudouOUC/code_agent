# PR #4247 最终实现文档

> 来源 PR: [feat(serve): MCP client guardrails (#4175 Wave 3 PR 14)](https://github.com/QwenLM/qwen-code/pull/4247)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +2867 / -31，15 个文件
> 时间: merged `2026-05-18T04:07:23Z`；closed `2026-05-18T04:07:23Z`

## 实现目标

围绕 `MCP client guardrails (#4175 Wave 3 PR 14)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/acpAgent.ts、packages/cli/src/commands/serve.ts、packages/cli/src/serve/capabilities.ts`，这些文件承载 `MCP client guardrails (#4175 Wave 3 PR 14)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `ServeMcpBudgetMode、ServeMcpBudgetStatusCell、McpBudgetMode、McpBudgetConfig、McpTransportKind、McpClientAccounting、BudgetExhaustedError、mcpTransportOf`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `GET /workspace/mcp still reports `clientCount`). Distinct from 、--mcp-client-budget、--mcp-budget-mode、--require-auth、--acp、--workspace、MCP_SERVER_CONNECTION_BATCH_SIZE、SERVE_CAPABILITY_REGISTRY、QWEN_SERVE_MCP_CLIENT_BUDGET、QWEN_SERVE_MCP_BUDGET_MODE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('exposes `modes` metadata on mcp_guardrails (#4175 PR 14)', () => {、it('round-trips PR 14 budget fields on /workspace/mcp', async () => {、describe('McpClientManager — PR 14 guardrails', () => {、it('getMcpClientAccounting returns zero on an empty manager', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md、docs/users/qwen-serve.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/acpAgent.ts`
- `packages/cli/src/commands/serve.ts`
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/status.ts`
- `packages/cli/src/serve/types.ts`
- `packages/core/src/tools/mcp-client-manager.ts`
- `packages/sdk-typescript/src/daemon/types.ts`

### 测试与验证文件
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/cli/src/serve/status.test.ts`
- `packages/core/src/tools/mcp-client-manager.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`
- `docs/users/qwen-serve.md`

## 代码变更线索

### 新增或调整的实现符号
- `ServeMcpBudgetMode`
- `ServeMcpBudgetStatusCell`
- `McpBudgetMode`
- `McpBudgetConfig`
- `McpTransportKind`
- `McpClientAccounting`
- `BudgetExhaustedError`
- `mcpTransportOf`
- `readBudgetFromEnv`
- `DaemonMcpBudgetMode`
- `DaemonMcpBudgetStatusCell`
- `MCP_BUDGET_WARN_FRACTION`

### 接口、命令或环境变量
- `GET /workspace/mcp still reports `clientCount`). Distinct from `
- `--mcp-client-budget`
- `--mcp-budget-mode`
- `--require-auth`
- `--acp`
- `--workspace`
- `MCP_SERVER_CONNECTION_BATCH_SIZE`
- `SERVE_CAPABILITY_REGISTRY`
- `QWEN_SERVE_MCP_CLIENT_BUDGET`
- `QWEN_SERVE_MCP_BUDGET_MODE`
- `SDK_MCP_SERVER_FIELDS`
- `MCP_BUDGET_WARN_FRACTION`
- `WARN_THRESHOLD_RATIO`
- `SCRUBBED_CHILD_ENV_KEYS`
- `QWEN_SERVER_TOKEN`

### 测试用例线索
- `it('exposes `modes` metadata on mcp_guardrails (#4175 PR 14)', () => {`
- `it('round-trips PR 14 budget fields on /workspace/mcp', async () => {`
- `describe('McpClientManager — PR 14 guardrails', () => {`
- `it('getMcpClientAccounting returns zero on an empty manager', async () => {`
- `it('mcpTransportOf maps each transport family correctly', async () => {`
- `it('enforce mode refuses connects past the budget', async () => {`
- `it('warn mode never refuses but tracks oversized reservations', async () => {`
- `it('off mode does not reserve any slot', async () => {`
- `it('refusal is deterministic by config-declaration order', async () => {`
- `it('discoverAllMcpTools resets lastRefusedServerNames each pass', async () => {`

## 代码阅读结论

- 最终实现以 PR #4247 的合入代码为准，核心判断来自 15 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
