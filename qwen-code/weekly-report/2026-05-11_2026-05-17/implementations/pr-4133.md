# PR #4133 最终实现文档

> 来源 PR: [feat(skills): add /stuck diagnostic skill for frozen sessions](https://github.com/QwenLM/qwen-code/pull/4133)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +172 / -0，2 个文件
> 时间: merged `2026-05-16T14:23:02Z`；closed `2026-05-16T14:23:02Z`

## 实现目标

围绕 `add /stuck diagnostic skill for frozen sessions` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：该 PR 主要是文档、配置或发布维护类改动，核心文件为 `packages/core/src/skills/bundled/stuck/SKILL.md`。
- 对外入口/配置：代码证据中出现 `--openai-api-key、RUNTIME_DIR、QWEN_RUNTIME_DIR、QWEN_HOME`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('bundled SKILL.md files', () => {、it('discovers at least one bundled skill', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `packages/core/src/skills/bundled/stuck/SKILL.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- 无。

### 测试与验证文件
- `packages/core/src/skills/bundled-skills.integration.test.ts`

### 文档、配置与发布材料
- `packages/core/src/skills/bundled/stuck/SKILL.md`

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- `--openai-api-key`
- `RUNTIME_DIR`
- `QWEN_RUNTIME_DIR`
- `QWEN_HOME`

### 测试用例线索
- `describe('bundled SKILL.md files', () => {`
- `it('discovers at least one bundled skill', () => {`

## 代码阅读结论

- 最终实现以 PR #4133 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
