# PR #4237 最终实现文档

> 来源 PR: [feat(serve): SSE replay sizing + slow_client_warning backpressure (#4175 Wave 2.5 PR 10)](https://github.com/QwenLM/qwen-code/pull/4237)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +893 / -81，18 个文件
> 时间: merged `2026-05-17T11:30:43Z`；closed `2026-05-17T11:30:44Z`

## 实现目标

围绕 `SSE replay sizing + slow_client_warning backpressure (#4175 Wave 2.5 PR 10)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/commands/serve.ts、packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/eventBus.ts`，这些文件承载 `SSE replay sizing + slow_client_warning backpressure (#4175 Wave 2.5 PR 10)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DEFAULT_RING_SIZE、WARN_THRESHOLD_RATIO、WARN_RESET_RATIO、MIN_QUERY_MAX_QUEUED、MAX_QUERY_MAX_QUEUED、parseMaxQueuedQuery、DaemonSlowClientWarningData、DaemonSlowClientWarningEvent`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--event-ring-size、DEFAULT_RING_SIZE、WARN_RESET_RATIO、WARN_THRESHOLD_RATIO、EVENT_SCHEMA_VERSION、DEFAULT_MAX_QUEUED、MIN_QUERY_MAX_QUEUED、MAX_QUERY_MAX_QUEUED、MAX_EVENT_RING_SIZE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('evicts a slow subscriber when its queue overflows (warning precedes eviction)', async () => {、it('emits slow_client_warning exactly once per overflow episode', async () => {、it('slow_client_warning frame has no id (synthetic, no sequence slot)', async () => {、it('rearms slow_client_warning after queue drains below the hysteresis threshold', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md、docs/users/qwen-serve.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/commands/serve.ts`
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/eventBus.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/server.ts`
- `packages/cli/src/serve/types.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/events.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/index.ts`

### 测试与验证文件
- `packages/cli/src/serve/eventBus.test.ts`
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`
- `packages/sdk-typescript/test/unit/daemonEvents.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`
- `docs/users/qwen-serve.md`

## 代码变更线索

### 新增或调整的实现符号
- `DEFAULT_RING_SIZE`
- `WARN_THRESHOLD_RATIO`
- `WARN_RESET_RATIO`
- `MIN_QUERY_MAX_QUEUED`
- `MAX_QUERY_MAX_QUEUED`
- `parseMaxQueuedQuery`
- `DaemonSlowClientWarningData`
- `DaemonSlowClientWarningEvent`
- `isSlowClientWarningData`
- `BoundedQueueEntry`
- `MAX_EVENT_RING_SIZE`
- `safeLogValue`

### 接口、命令或环境变量
- `--event-ring-size`
- `DEFAULT_RING_SIZE`
- `WARN_RESET_RATIO`
- `WARN_THRESHOLD_RATIO`
- `EVENT_SCHEMA_VERSION`
- `DEFAULT_MAX_QUEUED`
- `MIN_QUERY_MAX_QUEUED`
- `MAX_QUERY_MAX_QUEUED`
- `MAX_EVENT_RING_SIZE`

### 测试用例线索
- `it('evicts a slow subscriber when its queue overflows (warning precedes eviction)', async () => {`
- `it('emits slow_client_warning exactly once per overflow episode', async () => {`
- `it('slow_client_warning frame has no id (synthetic, no sequence slot)', async () => {`
- `it('rearms slow_client_warning after queue drains below the hysteresis threshold', async () => {`
- `it('default ring size is 8000 (#3803 §02 target)', () => {`
- `it('accepts a valid BridgeOptions.eventRingSize at construction time', () => {`
- `it('rejects an invalid eventRingSize at construction time', () => {`
- `it('forwards ?maxQueued=N to the bridge when in [16, 2048]', async () => {`
- `it('omits maxQueued from the bridge call when the query param is absent', async () => {`
- `it('400s a non-decimal ?maxQueued before opening the SSE stream', async () => {`

## 代码阅读结论

- 最终实现以 PR #4237 的合入代码为准，核心判断来自 18 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
