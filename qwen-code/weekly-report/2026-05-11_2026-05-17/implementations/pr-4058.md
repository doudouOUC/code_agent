# PR #4058 最终实现文档

> 来源 PR: [fix(telemetry): address PR #3847 review follow-ups for trace correlation](https://github.com/QwenLM/qwen-code/pull/4058)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +407 / -38，12 个文件
> 时间: merged `2026-05-13T14:02:20Z`；closed `2026-05-13T14:02:20Z`

## 实现目标

围绕 `address PR #3847 review follow-ups for trace correlation` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/coreToolScheduler.ts、packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts、packages/core/src/telemetry/log-to-span-processor.ts`，这些文件承载 `address PR #3847 review follow-ups for trace correlation` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `STREAM_SPAN_MAX_MS、setSessionContext、getCurrentSessionId、WithSpanOptions、shouldForceSampled、STREAM_IDLE_TIMEOUT_MS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `STREAM_SPAN_MAX_MS、OTEL_TRACES_SAMPLER、STREAM_IDLE_TIMEOUT_MS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('leaves cancellation spans with no explicit status (autoOkOnSuccess: false)', async () => {、it('sets cancellation attribute even when span attribute recording fails', async () => {、it('falls back to getCurrentSessionId when log record has no session.id', async () => {、it('prefers log record session.id over getCurrentSessionId', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/coreToolScheduler.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/log-to-span-processor.ts`
- `packages/core/src/telemetry/sdk.ts`
- `packages/core/src/telemetry/session-context.ts`
- `packages/core/src/telemetry/tracer.ts`

### 测试与验证文件
- `packages/core/src/core/coreToolScheduler.test.ts`
- `packages/core/src/telemetry/log-to-span-processor.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`
- `packages/core/src/telemetry/session-context.test.ts`
- `packages/core/src/telemetry/tracer.test.ts`
- `packages/core/src/utils/debugLogger.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `STREAM_SPAN_MAX_MS`
- `setSessionContext`
- `getCurrentSessionId`
- `WithSpanOptions`
- `shouldForceSampled`
- `STREAM_IDLE_TIMEOUT_MS`

### 接口、命令或环境变量
- `STREAM_SPAN_MAX_MS`
- `OTEL_TRACES_SAMPLER`
- `STREAM_IDLE_TIMEOUT_MS`

### 测试用例线索
- `it('leaves cancellation spans with no explicit status (autoOkOnSuccess: false)', async () => {`
- `it('sets cancellation attribute even when span attribute recording fails', async () => {`
- `it('falls back to getCurrentSessionId when log record has no session.id', async () => {`
- `it('prefers log record session.id over getCurrentSessionId', async () => {`
- `describe('getCurrentSessionId', () => {`
- `it('returns undefined when no session has been set', () => {`
- `it('returns the session ID passed to setSessionContext', () => {`
- `it('updates when setSessionContext is called with a new session ID', () => {`
- `it('clears when setSessionContext is called without a session ID', () => {`
- `describe('autoOkOnSuccess option', () => {`

## 代码阅读结论

- 最终实现以 PR #4058 的合入代码为准，核心判断来自 12 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
