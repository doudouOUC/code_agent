# PR #4235 最终实现文档

> 来源 PR: [feat(serve): add client heartbeat (#4175 Wave 2.5 PR 9)](https://github.com/QwenLM/qwen-code/pull/4235)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +581 / -2，15 个文件
> 时间: merged `2026-05-17T10:57:29Z`；closed `2026-05-17T10:57:29Z`

## 实现目标

围绕 `add client heartbeat (#4175 Wave 2.5 PR 9)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/httpAcpBridge.ts、packages/cli/src/serve/server.ts`，这些文件承载 `add client heartbeat (#4175 Wave 2.5 PR 9)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `BridgeHeartbeatResult、BridgeHeartbeatState、HeartbeatResult`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `POST /session/:id/heartbeat`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('recordHeartbeat', () => {、it('updates the per-session timestamp for an anonymous heartbeat', async () => {、it('records per-client timestamps when a trusted client id is supplied', async () => {、it('throws SessionNotFoundError on unknown sessions', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md、docs/users/qwen-serve.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/DaemonSessionClient.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/daemon/types.ts`
- `packages/sdk-typescript/src/index.ts`

### 测试与验证文件
- `integration-tests/cli/qwen-serve-routes.test.ts`
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`
- `packages/sdk-typescript/test/unit/DaemonSessionClient.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`
- `docs/users/qwen-serve.md`

## 代码变更线索

### 新增或调整的实现符号
- `BridgeHeartbeatResult`
- `BridgeHeartbeatState`
- `HeartbeatResult`

### 接口、命令或环境变量
- `POST /session/:id/heartbeat`

### 测试用例线索
- `describe('recordHeartbeat', () => {`
- `it('updates the per-session timestamp for an anonymous heartbeat', async () => {`
- `it('records per-client timestamps when a trusted client id is supplied', async () => {`
- `it('throws SessionNotFoundError on unknown sessions', async () => {`
- `it('rejects an unknown client id without bumping any timestamp', async () => {`
- `it('drops per-client last-seen on detach but preserves the session watermark', async () => {`
- `it('returns a snapshot map that callers cannot use to mutate live state', async () => {`
- `describe('POST /session/:id/heartbeat', () => {`
- `it('200 with the bridge result and forwards the routing id', async () => {`
- `it('forwards X-Qwen-Client-Id into the bridge context and echoes it back', async () => {`

## 代码阅读结论

- 最终实现以 PR #4235 的合入代码为准，核心判断来自 15 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
