# PR #4250 最终实现文档

> 来源 PR: [refactor(serve): add FileSystemService boundary (#4175 Wave 4 PR 18)](https://github.com/QwenLM/qwen-code/pull/4250)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +4753 / -68，16 个文件
> 时间: merged `2026-05-18T05:14:43Z`；closed `2026-05-18T05:14:43Z`

## 实现目标

围绕 `add FileSystemService boundary (#4175 Wave 4 PR 18)` 做重构调整；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/fs/audit.ts、packages/cli/src/serve/fs/errors.ts、packages/cli/src/serve/fs/index.ts`，这些文件承载 `add FileSystemService boundary (#4175 Wave 4 PR 18)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `FS_ACCESS_EVENT_TYPE、FS_DENIED_EVENT_TYPE、AuditContext、FsAccessAuditPayload、FsDeniedAuditPayload、AuditPublisher、hashPath、relForAudit`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `GET /file、EVENT_SCHEMA_VERSION、FS_ACCESS_EVENT_TYPE、FS_DENIED_EVENT_TYPE、QWEN_AUDIT_RAW_PATHS、DEFAULT_STATUS_BY_KIND、SYMLOOP_MAX、MAX_READ_BYTES、MAX_WRITE_BYTES、BINARY_PROBE_BYTES`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('createAuditPublisher', () => {、it('emits fs.access with hashed path and originatorClientId', () => {、it('attaches relPath when includeRawPaths is true', () => {、it('omits truncated/matchedIgnore when not provided', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/fs/audit.ts`
- `packages/cli/src/serve/fs/errors.ts`
- `packages/cli/src/serve/fs/index.ts`
- `packages/cli/src/serve/fs/paths.ts`
- `packages/cli/src/serve/fs/policy.ts`
- `packages/cli/src/serve/fs/workspaceFileSystem.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/core/src/index.ts`

### 测试与验证文件
- `packages/cli/src/serve/fs/audit.test.ts`
- `packages/cli/src/serve/fs/contract.test.ts`
- `packages/cli/src/serve/fs/errors.test.ts`
- `packages/cli/src/serve/fs/paths.test.ts`
- `packages/cli/src/serve/fs/policy.test.ts`
- `packages/cli/src/serve/fs/workspaceFileSystem.test.ts`
- `packages/cli/src/serve/server.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `FS_ACCESS_EVENT_TYPE`
- `FS_DENIED_EVENT_TYPE`
- `AuditContext`
- `FsAccessAuditPayload`
- `FsDeniedAuditPayload`
- `AuditPublisher`
- `hashPath`
- `relForAudit`
- `rawPathsEnabled`
- `CreateAuditPublisherDeps`
- `createAuditPublisher`
- `FsErrorKind`

### 接口、命令或环境变量
- `GET /file`
- `EVENT_SCHEMA_VERSION`
- `FS_ACCESS_EVENT_TYPE`
- `FS_DENIED_EVENT_TYPE`
- `QWEN_AUDIT_RAW_PATHS`
- `DEFAULT_STATUS_BY_KIND`
- `SYMLOOP_MAX`
- `MAX_READ_BYTES`
- `MAX_WRITE_BYTES`
- `BINARY_PROBE_BYTES`
- `ENOENT_TOLERATING_INTENTS`

### 测试用例线索
- `describe('createAuditPublisher', () => {`
- `it('emits fs.access with hashed path and originatorClientId', () => {`
- `it('attaches relPath when includeRawPaths is true', () => {`
- `it('omits truncated/matchedIgnore when not provided', () => {`
- `it('preserves truncated and matchedIgnore when set', () => {`
- `it('emits fs.denied with errorKind and hashed probe path', () => {`
- `it('emits fs.denied even when hint is absent', () => {`
- `it('respects QWEN_AUDIT_RAW_PATHS=1 via env when includeRawPaths is unset', () => {`
- `* #4175 PR 18 contract test (commit 8 of plan).`
- `describe('contract: resolveWithinWorkspace ↔ WorkspaceContext', () => {`

## 代码阅读结论

- 最终实现以 PR #4250 的合入代码为准，核心判断来自 16 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
