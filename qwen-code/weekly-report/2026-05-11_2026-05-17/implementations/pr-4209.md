# PR #4209 最终实现文档

> 来源 PR: [feat(serve): per-request sessionScope override on POST /session (#4175 Wave 2 PR 5)](https://github.com/QwenLM/qwen-code/pull/4209)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +512 / -20，8 个文件
> 时间: merged `2026-05-16T15:54:21Z`；closed `2026-05-16T15:54:21Z`

## 实现目标

围绕 `per-request sessionScope override on POST /session (#4175 Wave 2 PR 5)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/serve/capabilities.ts、packages/cli/src/serve/httpAcpBridge.ts、packages/cli/src/serve/server.ts`，这些文件承载 `per-request sessionScope override on POST /session (#4175 Wave 2 PR 5)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `doSpawn、InvalidSessionScopeError`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--sessionScope`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('per-request sessionScope:thread overrides daemon-wide single (#4175 PR 5)', async () => {、it('per-request sessionScope:single overrides daemon-wide thread (#4175 PR 5)', async () => {、it('thread-scope first call does NOT pollute the single-scope attach slot (#4175 PR 5 mixed-scope leak)', async () => {、it('rejects an invalid per-request sessionScope', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/qwen-serve-protocol.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/httpAcpBridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`

### 测试与验证文件
- `packages/cli/src/serve/httpAcpBridge.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/DaemonClient.test.ts`

### 文档、配置与发布材料
- `docs/developers/qwen-serve-protocol.md`

## 代码变更线索

### 新增或调整的实现符号
- `doSpawn`
- `InvalidSessionScopeError`

### 接口、命令或环境变量
- `--sessionScope`

### 测试用例线索
- `it('per-request sessionScope:thread overrides daemon-wide single (#4175 PR 5)', async () => {`
- `it('per-request sessionScope:single overrides daemon-wide thread (#4175 PR 5)', async () => {`
- `it('thread-scope first call does NOT pollute the single-scope attach slot (#4175 PR 5 mixed-scope leak)', async () => {`
- `it('rejects an invalid per-request sessionScope', async () => {`
- `it('passes through a valid `sessionScope` to the bridge (#4175 PR 5)', async () => {`
- `it('400 invalid_session_scope when `sessionScope` is not "single"/"thread"', async () => {`
- `it('omits `sessionScope` from the bridge request when the field is absent', async () => {`
- `it('forwards sessionScope when supplied (#4175 PR 5)', async () => {`
- `it('omits sessionScope from the body when the field is absent', async () => {`
- `it('symmetric mixed-scope leak: single-first does NOT trap a later thread call into the single slot', async () => {`

## 代码阅读结论

- 最终实现以 PR #4209 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
