# PR #4097 最终实现文档

> 来源 PR: [feat(telemetry): add interaction span and detailed sensitive attributes](https://github.com/QwenLM/qwen-code/pull/4097)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +893 / -175，13 个文件
> 时间: merged `2026-05-16T16:36:49Z`；closed `2026-05-16T16:36:49Z`

## 实现目标

围绕 `add interaction span and detailed sensitive attributes` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/settingsSchema.ts、packages/core/src/core/client.ts、packages/core/src/core/coreToolScheduler.ts`，这些文件承载 `add interaction span and detailed sensitive attributes` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `MAX_CONTENT_SIZE、SYSTEM_PROMPT_PREVIEW_LENGTH、isEnabled、truncateContent、shortHash、stringifyContentUnion、addUserPromptAttributes、addSystemPromptAttributes`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `MAX_CONTENT_SIZE、SYSTEM_PROMPT_PREVIEW_LENGTH`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('detailed-span-attributes', () => {、describe('truncateContent', () => {、it('returns content as-is when under limit', () => {、it('truncates content over limit', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/developers/development/telemetry.md、docs/users/configuration/settings.md、packages/vscode-ide-companion/schemas/settings.schema.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/settingsSchema.ts`
- `packages/core/src/core/client.ts`
- `packages/core/src/core/coreToolScheduler.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/detailed-span-attributes.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/telemetry/detailed-span-attributes.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- `docs/developers/development/telemetry.md`
- `docs/users/configuration/settings.md`
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `MAX_CONTENT_SIZE`
- `SYSTEM_PROMPT_PREVIEW_LENGTH`
- `isEnabled`
- `truncateContent`
- `shortHash`
- `stringifyContentUnion`
- `addUserPromptAttributes`
- `addSystemPromptAttributes`
- `addToolSchemaAttributes`
- `addModelOutputAttributes`
- `addToolInputAttributes`
- `addToolResultAttributes`

### 接口、命令或环境变量
- `MAX_CONTENT_SIZE`
- `SYSTEM_PROMPT_PREVIEW_LENGTH`

### 测试用例线索
- `describe('detailed-span-attributes', () => {`
- `describe('truncateContent', () => {`
- `it('returns content as-is when under limit', () => {`
- `it('truncates content over limit', () => {`
- `it('truncates at default 60KB limit', () => {`
- `describe('addUserPromptAttributes', () => {`
- `it('sets new_context with user prompt prefix', () => {`
- `it('no-ops when flag is disabled', () => {`
- `it('no-ops when SDK is not initialized', () => {`
- `it('no-ops when promptText is empty', () => {`

## 代码阅读结论

- 最终实现以 PR #4097 的合入代码为准，核心判断来自 13 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
