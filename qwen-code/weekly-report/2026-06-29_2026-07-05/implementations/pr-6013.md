# PR #6013 最终实现文档

> 作者: @doudouOUC
> 来源 PR: [fix(cli): Keep serve health responsive before runtime load](https://github.com/QwenLM/qwen-code/pull/6013)
> 状态: ✅ merged
> 变更规模: +1190/-55，8 个文件
> 时间: created `06-29 12:48`；merged/closed `06-30 10:53`

## 解决的问题

`qwen serve` 已经通过 fast path 提前 `listen()`，但首个 `/health` 请求仍可能被监听后的 runtime import/evaluation 阻塞。headless YOLO safety warning 也会经 core runtime import 拉入重依赖，导致监控方看到的首个 health 响应接近旧启动时长，而不是 fast path 的监听时长。

## 最终实现方案

`packages/cli/src/serve/run-qwen-serve.ts` 将 runtime 启动拆到 bootstrap health flush 之后：serve 先建立轻量 app、监听端口并响应首个 `/health`，再启动完整 runtime graph。为没有 health probe 的场景增加 fallback timer，保证 runtime 不会无限延迟。

`packages/cli/src/serve/fast-path.ts` 和 `runtime-startup-errors.ts` 保留 fast path 错误分类；`packages/cli/src/utils/headlessSafetyWarnings.ts` 改成轻量判断，避免警告路径提前动态导入 core runtime。测试集中在 `run-qwen-serve.test.ts`、`fast-path.test.ts`、`fast-path-open.test.ts` 与 headless warning 单测，覆盖首个 health 响应、fallback 启动和错误路径。

## 关键代码路径

- `packages/cli/src/serve/run-qwen-serve.ts`: fast path listen、首个 bootstrap health flush、fallback timer、runtime startup 调度。
- `packages/cli/src/serve/fast-path.ts`: fast path open/boot 状态与 lightweight app 装配。
- `packages/cli/src/serve/runtime-startup-errors.ts`: runtime 启动错误分类，避免 fast path 阶段吞掉失败。
- `packages/cli/src/utils/headlessSafetyWarnings.ts`: headless warning 走轻量判断，避免 pre-health 导入 core runtime。

## 验证方式

- `cd packages/cli && npx vitest run src/serve/run-qwen-serve.test.ts`: 首个 `/health`、runtime defer、fallback timer 与错误路径。
- `cd packages/cli && npx vitest run src/serve/fast-path.test.ts src/serve/fast-path-open.test.ts`: fast path closure 与 open behavior。
- `cd packages/cli && npx vitest run src/utils/headlessSafetyWarnings.test.ts`: headless safety warning 不再拉入重 runtime。

## 对应 feature 文档

已补 [daemon-serve-mode/README.md](../../../feature/daemon-serve-mode/README.md)。

## 归档口径

- 归档周目录：`2026-06-29_2026-07-05`。
- 本页按周报 README 的 PR 明细归档，内容结合 PR diff、changed files 与当前 main 代码路径整理。
- PR body 只作为目标线索；最终实现以后续 merged diff、changed files、测试和配置路径为准。
