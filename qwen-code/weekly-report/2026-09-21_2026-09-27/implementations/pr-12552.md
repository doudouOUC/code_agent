# PR #12552 当前实现观察：attestation 后采用本地 Runtime 进程

> PR: [feat(sdk-java): Adopt a Managed Runtime only after attestation](https://github.com/QwenLM/qwen-code/pull/12552)
> 状态：open；按 head `ac63adc48f89` 的当前 diff 记录，不能作为 `main` 已落地能力。
> 规模：+542 / -2，8 个文件

## 解决了什么问题

Java attestation client 已合入，但 Broker 仍缺少启动 worker、核验 ready 身份并在再次使用时确认进程存活的实际采用路径；仅把 lease 写成 READY 会错误复用失效端点。

## 当前实现方案

当前 diff 的 `LocalProcessRuntimeProvisioner` 生成实例/lease/seed/token，向子进程 stdin 写 boot JSON，等待读取 ready 记录，再用已合入的 Java client attestation；只有核验成功才返回 lease 供 Broker 置为 READY。Broker 对本 JVM 已有 live binding 调用 `confirm`；本地 provisioner 检查进程仍存活并重新 attestation。另新增 Java `execute` HTTP POST，但已合入 worker 只暴露 attest，该请求在 worker 上得到不可重试 404。测试使用 Node fake worker 与 accepting transport，并非真实 TypeScript worker 的完整 Tool Runtime 链路。

## 关键代码路径

- `packages/sdk-java/runtime-broker/src/main/java/com/alibaba/qwen/code/runtimebroker/LocalProcessRuntimeProvisioner.java`：启动、ready 验证、attestation 和 owned process 生命周期。
- `packages/sdk-java/runtime-broker/src/main/java/com/alibaba/qwen/code/runtimebroker/RuntimeBrokerService.java`：已有 live lease 的 confirm gate。
- `packages/sdk-java/runtime-broker/src/main/java/com/alibaba/qwen/code/runtimebroker/HttpRuntimeTransport.java`：新增 execute HTTP 客户端路径。
- `packages/sdk-java/runtime-broker/src/test/java/com/alibaba/qwen/code/runtimebroker/LocalProcessRuntimeProvisionerTest.java`：fake worker、404 与进程失活回归。

## 验证方式

本次核对当前 diff、测试、worker 主干能力与 CI：Java matrix 和 MariaDB lane 已通过；Integration Tests (no-AK, No Sandbox) 在 checkout head 校验时因 runner 的 Git `dubious ownership` 提前失败，未执行集成断言；web-shell E2E 随后取消。本文未本地重跑 Maven。合入前仍应修复或重跑失败检查，并以真实 worker 做 boot/attest 端到端验证。

## feature 文档处理

已更新 [[qwen-code/feature/managed-agents/README|Managed Agents]]、[[qwen-code/feature/managed-agents/managed-runtime-attestation.zh-CN|Runtime 身份核验]]、[[qwen-code/feature/sdk|SDK]] 与 [[qwen-code/feature/README|feature索引]]，将其标为 open 接线方案。

## 边界与风险

- 当前没有 production Tool handler、Spring/Flyway 部署装配或跨进程持久 READY reconcile/CAS 证明。
- Java `execute` 方法并不表示真实 worker 已能执行工具。
