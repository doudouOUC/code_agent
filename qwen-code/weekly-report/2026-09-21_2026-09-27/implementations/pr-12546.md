# PR #12546 当前实现观察：第二轮系统提示词去重

> PR: [fix(core): deduplicate system prompt guidance in a second pass](https://github.com/QwenLM/qwen-code/pull/12546)
> 状态：open；按 head `fad23c53ea1d` 的当前 diff 记录，不能作为 `main` 已落地能力。
> 规模：+200 / -214，5 个文件

## 解决了什么问题

第一轮提示词精简后，默认主会话仍重复描述工具选择、验证与结果报告、subagent 委派、适配式回复和 Git 来源，增加上下文占用，也可能让模型在重复措辞间偏移。

## 当前实现方案

当前 diff 合并测试与标准验证条款，把不能验证时的说明并入结果报告；压缩 direct/CodeModeOnly 两套工具和 subagent 指引、Git 提交/来源条款，同时保留工具过滤用的 gated line prefix、interaction-mode 问询、Todo 与安全边界。更新 prompt snapshots 和 ArenaManager 测试。文档记录同条件文本尺寸测量，但这不是账单 token 或真实模型效果验证。

## 关键代码路径

- `packages/core/src/core/prompts.ts:getCoreSystemPrompt`：主提示词组成和两种工具调用口径。
- `packages/core/src/core/__snapshots__/prompts.test.ts.snap`：各模式渲染快照。
- `packages/core/src/agents/arena/ArenaManager.test.ts`：worker prompt 消费回归。

## 验证方式

本次阅读当前 diff 与测试改动，核对 author、createdAt 和规模；未本地重跑。PR 文档声明 prompt tests、Arena tests、build/typecheck/lint 通过；真实模型配对 A/B 尚未验证。可运行 `npm test --workspace=@qwen-code/qwen-code-core -- src/core/prompts.test.ts src/agents/arena/ArenaManager.test.ts` 与 `npm run typecheck --workspace=@qwen-code/qwen-code-core`，并比较各 mode/model-family 渲染。

## feature 文档处理

已新增 [[qwen-code/feature/system-prompt-guidance|系统提示词指引]] 并更新 [[qwen-code/feature/README|feature索引]]；明确这是 open 方案。

## 边界与风险

- 结构测试和快照不能证明实际任务成功率或行为不变；当前 diff 未进入 `main`。
