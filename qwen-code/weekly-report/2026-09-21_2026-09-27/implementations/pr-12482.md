# PR #12482 关闭方案观察：刷新 VS Code 第三方声明

> PR: [fix(vscode): Refresh third-party notices](https://github.com/QwenLM/qwen-code/pull/12482)
> 状态：closed 未合入；以下仅记录关闭前 diff，不能作为 `main` 已落地能力。
> 规模：+3770 / -3770，1 个文件

## 解决了什么问题

VS Code companion 的第三方声明文件需要与生成结果一致；顺序漂移会让 freshness 检查出现无意义差异。

## 关闭前方案

该 PR 只重排 `NOTICES.txt` 的声明块，GitHub 按行记录为等量增删；没有修改运行时代码、依赖或许可条款。相同内容已由另一变更进入主干，因此本 PR 关闭未合入。不能把主干上的声明刷新归功于此 PR。

## 关键代码路径

- `packages/vscode-ide-companion/NOTICES.txt`：关闭前 diff 的唯一文件。

## 验证方式

本次核对 GitHub author、createdAt、closed/mergedAt、changed files 与 diff；未本地执行生成器或 VS Code 构建。声明 freshness 的项目命令是 `npm run generate:notices --workspace=qwen-code-vscode-ide-companion`，随后检查 `packages/vscode-ide-companion/NOTICES.txt` 无改动；这是 CI 的校验方式，不表示本关闭 PR 已通过该检查。

## feature 文档处理

未新增 feature 专题：仅有声明文件排序，且此 PR 未合入；保留本历史观察即可。

## 边界与风险

- 不能将该关闭 diff 写为已交付运行时能力或重复计算替代变更。
