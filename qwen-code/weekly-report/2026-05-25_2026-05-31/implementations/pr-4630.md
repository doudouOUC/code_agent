# PR #4630 最终实现文档

> 来源 PR: [feat(telemetry): add tool spans and session.id to daemon/ACP path](https://github.com/QwenLM/qwen-code/pull/4630)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1169 / -744，4 个文件
> 时间: merged `2026-05-30T16:39:33Z`；closed `2026-05-30T16:39:33Z`

## 实现目标

围绕 `add tool spans and session.id to daemon/ACP path` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/session/Session.ts、packages/core/src/telemetry/session-tracing.ts`，这些文件承载 `add tool spans and session.id to daemon/ACP path` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `resolveSessionId、InteractionSpanResultStatus`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `TODO_WRITE、EXIT_PLAN_MODE、AUTO_EDIT、ASK_USER_QUESTION、MAX_TRANSCRIPT_MESSAGES`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('conversation_finished telemetry (#4602 review)', () => {、it('emits conversation_finished once when a turn completes normally', async () => {、it('still emits conversation_finished when the turn throws (telemetry not lost on the error path)', async () => {、describe('tool outcome telemetry (#4602 review)', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/session/Session.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/cli/src/acp-integration/session/Session.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `resolveSessionId`
- `InteractionSpanResultStatus`

### 接口、命令或环境变量
- `TODO_WRITE`
- `EXIT_PLAN_MODE`
- `AUTO_EDIT`
- `ASK_USER_QUESTION`
- `MAX_TRANSCRIPT_MESSAGES`

### 测试用例线索
- `describe('conversation_finished telemetry (#4602 review)', () => {`
- `it('emits conversation_finished once when a turn completes normally', async () => {`
- `it('still emits conversation_finished when the turn throws (telemetry not lost on the error path)', async () => {`
- `describe('tool outcome telemetry (#4602 review)', () => {`
- `it('records a soft tool failure (toolResult.error) as error, not success', async () => {`
- `describe('session.id derives from the owning session, not the process-global (#4602 review)', () => {`
- `it('stamps a tool span with the interaction session.id even when the process-global belongs to another session', () => {`
- `it('stamps an llm_request span with the interaction session.id, not the global', () => {`
- `it('stamps a tool.execution span with the owning session id via the tool span context', () => {`
- `it('isolates concurrent sessions: each tool span carries its own session id', async () => {`

## 代码阅读结论

- 最终实现以 PR #4630 的合入代码为准，核心判断来自 4 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
