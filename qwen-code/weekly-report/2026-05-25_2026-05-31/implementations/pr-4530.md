# PR #4530 最终实现文档

> 来源 PR: [feat(serve): prompt absolute deadline + SSE writer idle timeout (#4514 T2.9)](https://github.com/QwenLM/qwen-code/pull/4530)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1458 / -151，12 个文件
> 时间: merged `2026-05-26T17:09:09Z`；closed `2026-05-26T17:09:09Z`

## 实现目标

围绕 `prompt absolute deadline + SSE writer idle timeout (#4514 T2.9)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/status.ts、packages/cli/src/commands/serve.ts、packages/cli/src/serve/capabilities.ts`，这些文件承载 `prompt absolute deadline + SSE writer idle timeout (#4514 T2.9)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `QWEN_SERVE_PROMPT_DEADLINE_MS_ENV、QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS_ENV、isPositiveIntegerMs、MAX_TIMEOUT_MS、assertTimerDelayInRange、parseDeadlineEnv、PromptDeadlineExceededError、resolvePromptDeadlineMs`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--writer-idle-timeout-ms、--prompt-deadline-ms、QWEN_SERVE_PROMPT_DEADLINE_MS、QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS、QWEN_SERVE_PROMPT_DEADLINE_MS_ENV、QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS_ENV、MAX_TIMEOUT_MS、ERR_STREAM_WRITE_AFTER_END、SERVE_ERROR_KINDS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('rejects promptDeadlineMs that exceeds the JS timer cap (#4514 T2.9 wenshao review)', async () => {、it('accepts writerIdleTimeoutMs above the JS timer cap (#4530 review)', async () => {、it('rejects QWEN_SERVE_PROMPT_DEADLINE_MS that exceeds JS timer cap (#4514 T2.9)', async () => {、it('accepts a valid QWEN_SERVE_PROMPT_DEADLINE_MS env var (#4514 T2.9 happy path)', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/users/qwen-serve.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/status.ts`
- `packages/cli/src/commands/serve.ts`
- `packages/cli/src/serve/capabilities.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/server.ts`
- `packages/cli/src/serve/types.ts`
- `packages/sdk-typescript/src/daemon/DaemonClient.ts`
- `packages/sdk-typescript/src/daemon/types.ts`

### 测试与验证文件
- `packages/acp-bridge/src/status.test.ts`
- `packages/cli/src/serve/server.test.ts`
- `packages/sdk-typescript/test/unit/daemon-public-surface.test.ts`

### 文档、配置与发布材料
- `docs/users/qwen-serve.md`

## 代码变更线索

### 新增或调整的实现符号
- `QWEN_SERVE_PROMPT_DEADLINE_MS_ENV`
- `QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS_ENV`
- `isPositiveIntegerMs`
- `MAX_TIMEOUT_MS`
- `assertTimerDelayInRange`
- `parseDeadlineEnv`
- `PromptDeadlineExceededError`
- `resolvePromptDeadlineMs`
- `emitPromptDeadline504`

### 接口、命令或环境变量
- `--writer-idle-timeout-ms`
- `--prompt-deadline-ms`
- `QWEN_SERVE_PROMPT_DEADLINE_MS`
- `QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS`
- `QWEN_SERVE_PROMPT_DEADLINE_MS_ENV`
- `QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS_ENV`
- `MAX_TIMEOUT_MS`
- `ERR_STREAM_WRITE_AFTER_END`
- `SERVE_ERROR_KINDS`

### 测试用例线索
- `it('rejects promptDeadlineMs that exceeds the JS timer cap (#4514 T2.9 wenshao review)', async () => {`
- `it('accepts writerIdleTimeoutMs above the JS timer cap (#4530 review)', async () => {`
- `it('rejects QWEN_SERVE_PROMPT_DEADLINE_MS that exceeds JS timer cap (#4514 T2.9)', async () => {`
- `it('accepts a valid QWEN_SERVE_PROMPT_DEADLINE_MS env var (#4514 T2.9 happy path)', async () => {`
- `it('rejects invalid QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS env var at boot (#4514 T2.9)', async () => {`
- `it('accepts QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS above JS timer cap (#4530 review)', async () => {`
- `it('accepts a valid QWEN_SERVE_WRITER_IDLE_TIMEOUT_MS env var (#4514 T2.9 happy path)', async () => {`
- `describe('T2.9 prompt absolute deadline (issue #4514)', () => {`
- `describe('resolvePromptDeadlineMs', () => {`
- `it('returns undefined when the server flag is unset', () => {`

## 代码阅读结论

- 最终实现以 PR #4530 的合入代码为准，核心判断来自 12 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
