# PR #4646 最终实现文档

> 来源 PR: [feat(daemon): clamp oversized inline media on the prompt path](https://github.com/QwenLM/qwen-code/pull/4646)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +256 / -16，6 个文件
> 时间: merged `2026-05-31T10:06:44Z`；closed `2026-05-31T10:06:44Z`

## 实现目标

围绕 `clamp oversized inline media on the prompt path` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/session/Session.ts、packages/cli/src/serve/acpHttp/dispatch.ts、packages/core/src/core/inlineMediaLimit.ts`，这些文件承载 `clamp oversized inline media on the prompt path` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DEFAULT_MAX_INLINE_MEDIA_BYTES、getMaxInlineMediaBytes、approxBase64Bytes、formatMb、oversizedMediaPlaceholder、clampInlineMediaPart`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `DEFAULT_MAX_INLINE_MEDIA_BYTES、QWEN_CODE_MAX_INLINE_MEDIA_BYTES`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('degrades an oversized inline image to a text placeholder before sending to the model', async () => {、describe('approxBase64Bytes', () => {、it('estimates decoded byte length from base64 length', () => {、it('accounts for padding', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/session/Session.ts`
- `packages/cli/src/serve/acpHttp/dispatch.ts`
- `packages/core/src/core/inlineMediaLimit.ts`
- `packages/core/src/index.ts`

### 测试与验证文件
- `packages/cli/src/acp-integration/session/Session.test.ts`
- `packages/core/src/core/inlineMediaLimit.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `DEFAULT_MAX_INLINE_MEDIA_BYTES`
- `getMaxInlineMediaBytes`
- `approxBase64Bytes`
- `formatMb`
- `oversizedMediaPlaceholder`
- `clampInlineMediaPart`

### 接口、命令或环境变量
- `DEFAULT_MAX_INLINE_MEDIA_BYTES`
- `QWEN_CODE_MAX_INLINE_MEDIA_BYTES`

### 测试用例线索
- `it('degrades an oversized inline image to a text placeholder before sending to the model', async () => {`
- `describe('approxBase64Bytes', () => {`
- `it('estimates decoded byte length from base64 length', () => {`
- `it('accounts for padding', () => {`
- `it('returns 0 for empty input', () => {`
- `it('ignores a data: URL prefix', () => {`
- `describe('getMaxInlineMediaBytes', () => {`
- `it('defaults to 10MB', () => {`
- `it('honors a valid positive env override', () => {`
- `it('ignores a non-numeric env override', () => {`

## 代码阅读结论

- 最终实现以 PR #4646 的合入代码为准，核心判断来自 6 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
