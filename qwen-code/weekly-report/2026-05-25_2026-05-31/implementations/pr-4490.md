# PR #4490 最终实现：daemon-mode feature batch 合入 main

> PR: [feat(daemon): merge daemon-mode feature batch into main](https://github.com/QwenLM/qwen-code/pull/4490)
> 状态：merged，2026-06-11 合入 `main`，merge commit `531a15dd931053588337d8d7bbe1dbc5a310c026`。
> 规模：+148,639 / -16,017，487 个文件

## 解决了什么问题

daemon Mode B 的 F1/F2/F3/F4-prereq、F5 alpha 文档及相关能力此前主要汇集在长期集成分支 `daemon_mode_b_main`，尚未成为 `main` 的正式交付面。随着集成分支持续吸收 ACP bridge、daemon server、WebUI、SDK、telemetry 等多子系统改动，分支分叉和批量回归风险也不断增加。

## 最终实现方案

#4490 通过一次 feature batch 反向集成，把 `daemon_mode_b_main` 中已经汇总的 daemon-mode 能力合入 `main`。合入范围覆盖 ACP bridge 与多客户端权限协调、MCP transport pool、daemon serve 控制面、WebUI/Web Shell、TypeScript SDK、telemetry 以及 F5 alpha 文档。该 PR 解决的是整批能力的 mainline 交付问题；各能力的后续精确行为应继续以最新 `main`、专题文档及 #5144 的 daemon 文档刷新为准。

## 关键代码路径

- `packages/acp-bridge/`：ACP bridge 与 daemon 子进程协作边界。
- `packages/cli/src/serve/`：daemon HTTP/SSE 服务、会话与扩展端点。
- `packages/webui/`、`packages/web-shell/`：daemon WebUI 与 Web Shell 客户端能力。
- `packages/sdk-typescript/`：daemon TypeScript SDK surface。
- `docs/developers/daemon/`：daemon developer docs；后续由 #5144 继续刷新并核对 `main`。

## 验证方式

- 本次独立核对 GitHub 当前状态为 `MERGED`，合入时间为 2026-06-11，merge commit 为 `531a15dd931053588337d8d7bbe1dbc5a310c026`。
- 本次未重新执行该巨型集成批次的历史测试矩阵；具体子能力的测试与验证以对应拆分 PR、当前 `main` 和专题文档为准。

## feature 文档处理

已由 [daemon/serve 模式技术方案](../../../feature/daemon-serve-mode/README.md) 和 [路线图、覆盖矩阵与当前缺口](../../../feature/daemon-serve-mode/09-roadmap-coverage-and-gaps.md) 登记 #4490 的 mainline 合入，并由 #5144 继续对齐 daemon 文档与 `main` 实现面。

## 边界与风险

- 这是跨多个子系统的巨型集成 PR，不能把单页摘要当作所有子能力的完整设计说明。
- 该周 README 保留周归档时的审查快照；本页记录后续确认的最终合入状态。
- 当前运行时行为可能已被后续 PR 演进，阅读时应以最新 `main` 和对应 feature 文档为准。
