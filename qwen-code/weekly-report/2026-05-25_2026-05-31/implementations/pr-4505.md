# PR #4505 最终实现文档

> 来源 PR: [fix(core): emit enable_thinking on DashScope when reasoning is disabled](https://github.com/QwenLM/qwen-code/pull/4505)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +419 / -6，2 个文件
> 时间: merged `2026-05-31T04:32:52Z`；closed `2026-05-31T04:32:53Z`

## 实现目标

围绕 `emit enable_thinking on DashScope when reasoning is disabled` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/openaiContentGenerator/pipeline.ts`，这些文件承载 `emit enable_thinking on DashScope when reasoning is disabled` 的核心行为或配置变化。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/core/src/core/openaiContentGenerator/pipeline.ts` 的文件级改动为锚点。
- 对外入口/配置：代码证据中出现 `QWEN_OAUTH、DEFAULT_QWEN_MODEL`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('emits enable_thinking:false on DashScope hostname when includeThoughts is false', async () => {、it('emits enable_thinking:false on DashScope hostname when reasoning is configured to false', async () => {、it('emits enable_thinking:false on QWEN_OAUTH regardless of baseUrl', async () => {、it('emits enable_thinking:false on internal alibaba-inc.com hostname', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/openaiContentGenerator/pipeline.ts`

### 测试与验证文件
- `packages/core/src/core/openaiContentGenerator/pipeline.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- `QWEN_OAUTH`
- `DEFAULT_QWEN_MODEL`

### 测试用例线索
- `it('emits enable_thinking:false on DashScope hostname when includeThoughts is false', async () => {`
- `it('emits enable_thinking:false on DashScope hostname when reasoning is configured to false', async () => {`
- `it('emits enable_thinking:false on QWEN_OAUTH regardless of baseUrl', async () => {`
- `it('emits enable_thinking:false on internal alibaba-inc.com hostname', async () => {`
- `it('does NOT emit enable_thinking on a non-DashScope hostname', async () => {`
- `it('does NOT emit enable_thinking on a non-qwen model routed through DashScope', async () => {`
- `it('emits enable_thinking:false on QWEN_OAUTH with the default coder-model', async () => {`
- `it('emits enable_thinking:false when baseUrl is unset (DashScope default)', async () => {`
- `it('gates on the wire model, not config: qwen config + non-qwen request.model does NOT emit', async () => {`
- `it('gates on the wire model, not config: non-qwen config + qwen request.model emits false', async () => {`

## 代码阅读结论

- 最终实现以 PR #4505 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
