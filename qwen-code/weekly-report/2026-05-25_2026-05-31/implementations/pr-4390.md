# PR #4390 最终实现文档

> 来源 PR: [feat(telemetry): client-side HTTP span + opt-in W3C traceparent propagation (#4384)](https://github.com/QwenLM/qwen-code/pull/4390)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1822 / -5，14 个文件
> 时间: merged `2026-05-25T14:16:54Z`；closed `2026-05-25T14:16:54Z`

## 实现目标

围绕 `client-side HTTP span + opt-in W3C traceparent propagation (#4384)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/config.ts、packages/cli/src/config/settingsSchema.ts、packages/core/src/config/config.ts`，这些文件承载 `client-side HTTP span + opt-in W3C traceparent propagation (#4384)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `normalizeOtlpPrefix、SESSION_ID_HEADER、wrapFetchWithCorrelation、staticCorrelationHeaders、FetchLikeLoose、DEFAULT_SESSION_ID_HEADER_HOSTS、matchesTrustedHost、extractRequestHost`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--include、--prompt、SESSION_ID_HEADER、DEFAULT_SESSION_ID_HEADER_HOSTS、REQUEST_CHANGES、NOOP_PROPAGATOR`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('Instrumentations', () => {、it('registers both HttpInstrumentation and UndiciInstrumentation', () => {、it('UndiciInstrumentation receives ignoreRequestHook that skips configured OTLP endpoints', () => {、it('ignoreRequestHook is a pure no-op when no OTLP endpoint is configured', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/telemetry-outbound-propagation-design.md、docs/developers/development/telemetry.md、package-lock.json、packages/core/package.json` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/config.ts`
- `packages/cli/src/config/settingsSchema.ts`
- `packages/core/src/config/config.ts`
- `packages/core/src/telemetry/sdk.ts`

### 测试与验证文件
- `packages/core/src/config/config.test.ts`
- `packages/core/src/core/anthropicContentGenerator/anthropicContentGenerator.test.ts`
- `packages/core/src/core/contentGenerator.test.ts`
- `packages/core/src/core/geminiContentGenerator/index.test.ts`
- `packages/core/src/telemetry/sdk.test.ts`

### 文档、配置与发布材料
- `docs/design/telemetry-outbound-propagation-design.md`
- `docs/developers/development/telemetry.md`
- `package-lock.json`
- `packages/core/package.json`
- `packages/vscode-ide-companion/schemas/settings.schema.json`

## 代码变更线索

### 新增或调整的实现符号
- `normalizeOtlpPrefix`
- `SESSION_ID_HEADER`
- `wrapFetchWithCorrelation`
- `staticCorrelationHeaders`
- `FetchLikeLoose`
- `DEFAULT_SESSION_ID_HEADER_HOSTS`
- `matchesTrustedHost`
- `extractRequestHost`
- `OutboundCorrelationSettings`
- `NOOP_PROPAGATOR`

### 接口、命令或环境变量
- `--include`
- `--prompt`
- `SESSION_ID_HEADER`
- `DEFAULT_SESSION_ID_HEADER_HOSTS`
- `REQUEST_CHANGES`
- `NOOP_PROPAGATOR`

### 测试用例线索
- `describe('Instrumentations', () => {`
- `it('registers both HttpInstrumentation and UndiciInstrumentation', () => {`
- `it('UndiciInstrumentation receives ignoreRequestHook that skips configured OTLP endpoints', () => {`
- `it('ignoreRequestHook is a pure no-op when no OTLP endpoint is configured', () => {`
- `it('ignoreRequestHook handles per-signal endpoint configuration', () => {`
- `it('ignoreRequestHook strips query string from incoming path for matching', () => {`
- `it('ignoreRequestHook strips #fragment from incoming path for matching', () => {`
- `it('ignoreRequestHook normalizes endpoint config quoted in settings.json', () => {`
- `it('ignoreRequestHook strips #fragment from configured endpoint', () => {`
- `it('omits X-Qwen-Code-Session-Id from httpOptions.headers when telemetry is disabled', () => {`

## 代码阅读结论

- 最终实现以 PR #4390 的合入代码为准，核心判断来自 14 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
