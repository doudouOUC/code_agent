# PR #4556 最终实现文档

> 来源 PR: [feat(telemetry): trace daemon prompt lifecycle](https://github.com/QwenLM/qwen-code/pull/4556)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1325 / -424，14 个文件
> 时间: merged `2026-05-29T06:03:18Z`；closed `2026-05-29T06:03:18Z`

## 实现目标

围绕 `trace daemon prompt lifecycle` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridge.ts、packages/acp-bridge/src/bridgeOptions.ts、packages/cli/src/acp-integration/session/Session.ts`，这些文件承载 `trace daemon prompt lifecycle` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DAEMON_TRACEPARENT_META_KEY、DAEMON_TRACESTATE_META_KEY、NOOP_BRIDGE_TELEMETRY、BridgeTelemetryAttributes、BridgeTelemetry、TelemetryRuntimeConfig、TelemetrySettings、createDaemonTelemetryRuntimeConfig`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `POST /session、DELETE /session/:id、GET /workspace/:id/sessions、--prompt-deadline、--resume、DAEMON_TRACEPARENT_META_KEY、DAEMON_TRACESTATE_META_KEY、NOOP_BRIDGE_TELEMETRY、PROTOCOL_VERSION、HOOK_EXECUTION_REQUEST`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('uses bridge telemetry for channel/session/prompt dispatch and prompt metadata injection', async () => {、describe('daemon-tracing', () => {、it('extracts daemon trace context from reserved prompt metadata keys', () => {、it('strips reserved metadata when no active daemon span exists', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridge.ts`
- `packages/acp-bridge/src/bridgeOptions.ts`
- `packages/cli/src/acp-integration/session/Session.ts`
- `packages/cli/src/serve/runQwenServe.ts`
- `packages/cli/src/serve/server.ts`
- `packages/core/src/telemetry/daemon-tracing.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/metrics.ts`
- `packages/core/src/telemetry/runtime-config.ts`
- `packages/core/src/telemetry/sdk.ts`
- `packages/core/src/telemetry/session-tracing.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridge.test.ts`
- `packages/core/src/telemetry/daemon-tracing.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `DAEMON_TRACEPARENT_META_KEY`
- `DAEMON_TRACESTATE_META_KEY`
- `NOOP_BRIDGE_TELEMETRY`
- `BridgeTelemetryAttributes`
- `BridgeTelemetry`
- `TelemetryRuntimeConfig`
- `TelemetrySettings`
- `createDaemonTelemetryRuntimeConfig`
- `resolveDaemonTelemetryRoute`
- `daemonTelemetryMiddleware`
- `Attributes`
- `Context`

### 接口、命令或环境变量
- `POST /session`
- `DELETE /session/:id`
- `GET /workspace/:id/sessions`
- `--prompt-deadline`
- `--resume`
- `DAEMON_TRACEPARENT_META_KEY`
- `DAEMON_TRACESTATE_META_KEY`
- `NOOP_BRIDGE_TELEMETRY`
- `PROTOCOL_VERSION`
- `HOOK_EXECUTION_REQUEST`
- `HOOK_EXECUTION_RESPONSE`
- `DEFAULT_OTLP_ENDPOINT`
- `DEFAULT_TELEMETRY_TARGET`
- `SERVICE_NAME`
- `SPAN_DAEMON_REQUEST`

### 测试用例线索
- `it('uses bridge telemetry for channel/session/prompt dispatch and prompt metadata injection', async () => {`
- `describe('daemon-tracing', () => {`
- `it('extracts daemon trace context from reserved prompt metadata keys', () => {`
- `it('strips reserved metadata when no active daemon span exists', () => {`
- `it('hashes workspace paths without exposing the raw path', () => {`
- `it('runs scoped interaction spans without mutating the global interaction context', async () => {`
- `it('emits bridge events as standalone spans without an active span', () => {`

## 代码阅读结论

- 最终实现以 PR #4556 的合入代码为准，核心判断来自 14 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
