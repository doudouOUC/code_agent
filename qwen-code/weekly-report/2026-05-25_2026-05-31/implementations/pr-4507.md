# PR #4507 最终实现文档

> 来源 PR: [feat(daemon): server-pushed followup_suggestion event for the webui](https://github.com/QwenLM/qwen-code/pull/4507)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1154 / -22，18 个文件
> 时间: merged `2026-05-27T13:19:08Z`；closed `2026-05-27T13:19:08Z`

## 实现目标

围绕 `server-pushed followup_suggestion event for the webui` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridgeClient.ts、packages/cli/src/acp-integration/session/Session.ts、packages/sdk-typescript/src/daemon/events.ts`，这些文件承载 `server-pushed followup_suggestion event for the webui` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DaemonFollowupSuggestionData、DaemonFollowupSuggestionEvent、DaemonAssistEvent、isFollowupSuggestionData、normalizeFollowupSuggestion、selectLastFollowupSuggestion、DaemonUiFollowupSuggestionEvent、UseDaemonFollowupSuggestionReturn`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `MAX_SUGGESTION_LENGTH`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('followup_suggestion (daemon assist push)', () => {、it('recognizes followup_suggestion frames as known events', () => {、it('rejects malformed followup_suggestion payloads', () => {、it('reducer stores lastFollowupSuggestion and overwrites on a fresh event', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridgeClient.ts`
- `packages/cli/src/acp-integration/session/Session.ts`
- `packages/sdk-typescript/src/daemon/events.ts`
- `packages/sdk-typescript/src/daemon/index.ts`
- `packages/sdk-typescript/src/daemon/ui/index.ts`
- `packages/sdk-typescript/src/daemon/ui/normalizer.ts`
- `packages/sdk-typescript/src/daemon/ui/store.ts`
- `packages/sdk-typescript/src/daemon/ui/terminal.ts`
- `packages/sdk-typescript/src/daemon/ui/transcript.ts`
- `packages/sdk-typescript/src/daemon/ui/types.ts`
- `packages/sdk-typescript/src/index.ts`
- `packages/webui/src/daemon/index.ts`
- `packages/webui/src/daemon/useDaemonFollowupSuggestion.ts`
- `packages/webui/src/index.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridge.test.ts`
- `packages/cli/src/acp-integration/session/Session.test.ts`
- `packages/sdk-typescript/test/unit/daemonEvents.test.ts`
- `packages/sdk-typescript/test/unit/daemonUi.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `DaemonFollowupSuggestionData`
- `DaemonFollowupSuggestionEvent`
- `DaemonAssistEvent`
- `isFollowupSuggestionData`
- `normalizeFollowupSuggestion`
- `selectLastFollowupSuggestion`
- `DaemonUiFollowupSuggestionEvent`
- `UseDaemonFollowupSuggestionReturn`
- `UseFollowupSuggestionsOptions`
- `useDaemonFollowupSuggestion`
- `MAX_SUGGESTION_LENGTH`

### 接口、命令或环境变量
- `MAX_SUGGESTION_LENGTH`

### 测试用例线索
- `describe('followup_suggestion (daemon assist push)', () => {`
- `it('recognizes followup_suggestion frames as known events', () => {`
- `it('rejects malformed followup_suggestion payloads', () => {`
- `it('reducer stores lastFollowupSuggestion and overwrites on a fresh event', () => {`
- `it('malformed payload routes to unrecognizedKnownEventCount', () => {`
- `describe('daemon assist push: followup_suggestion', () => {`
- `it('normalizes followup_suggestion to followup.suggestion with payload', () => {`
- `it('routes malformed followup_suggestion to debug fallback', () => {`
- `it('transcript reducer stores lastFollowupSuggestion without appending a block', () => {`
- `it('latest followup_suggestion replaces the prior one for the session', () => {`

## 代码阅读结论

- 最终实现以 PR #4507 的合入代码为准，核心判断来自 18 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
