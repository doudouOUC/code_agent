# PR #4500 最终实现文档

> 来源 PR: [chore(integration): sync main into daemon_mode_b_main (2026-05-25)](https://github.com/QwenLM/qwen-code/pull/4500)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +501 / -0，7 个文件
> 时间: merged `2026-05-25T09:32:42Z`；closed `2026-05-25T09:32:42Z`

## 实现目标

围绕 `sync main into daemon_mode_b_main (2026-05-25)` 做维护同步；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/extension/redaction.ts、packages/core/src/utils/projectRoot.ts`，这些文件承载 `sync main into daemon_mode_b_main (2026-05-25)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `REDACTED_URL_CREDENTIAL、URL_CREDENTIALS_PATTERN、redactUrlCredentials、findProjectRoot`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--heapsnapshot-signal、--experimentalMemory、--headless、--no-usage-statistics、QWEN_CODE_NO_RELAUNCH、NODE_OPTIONS、NODE_PID、NODE_ENV、REDACTED_URL_CREDENTIAL、URL_CREDENTIALS_PATTERN`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('redactUrlCredentials', () => {、it('redacts username and password from HTTPS URLs', () => {、it('redacts token-only URL credentials', () => {、it('redacts raw hash characters echoed in URL credentials', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `.qwen/skills/memory-leak-debug/SKILL.md、.qwen/skills/memory-leak-debug/examples/react-reconciler-performance-measure-leak.md、.qwen/skills/memory-leak-debug/scripts/find-leaf-node.sh` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/core/src/extension/redaction.ts`
- `packages/core/src/utils/projectRoot.ts`

### 测试与验证文件
- `packages/core/src/extension/redaction.test.ts`
- `packages/core/src/utils/projectRoot.test.ts`

### 文档、配置与发布材料
- `.qwen/skills/memory-leak-debug/SKILL.md`
- `.qwen/skills/memory-leak-debug/examples/react-reconciler-performance-measure-leak.md`
- `.qwen/skills/memory-leak-debug/scripts/find-leaf-node.sh`

## 代码变更线索

### 新增或调整的实现符号
- `REDACTED_URL_CREDENTIAL`
- `URL_CREDENTIALS_PATTERN`
- `redactUrlCredentials`
- `findProjectRoot`

### 接口、命令或环境变量
- `--heapsnapshot-signal`
- `--experimentalMemory`
- `--headless`
- `--no-usage-statistics`
- `QWEN_CODE_NO_RELAUNCH`
- `NODE_OPTIONS`
- `NODE_PID`
- `NODE_ENV`
- `REDACTED_URL_CREDENTIAL`
- `URL_CREDENTIALS_PATTERN`
- `PROJECT_ROOT`

### 测试用例线索
- `describe('redactUrlCredentials', () => {`
- `it('redacts username and password from HTTPS URLs', () => {`
- `it('redacts token-only URL credentials', () => {`
- `it('redacts raw hash characters echoed in URL credentials', () => {`
- `it('redacts unencoded at signs inside echoed URL credentials', () => {`
- `it('redacts unencoded question marks inside echoed URL credentials', () => {`
- `it('redacts percent-encoded URL credentials', () => {`
- `it('does not redact at signs after the URL path starts', () => {`
- `it('redacts custom URL schemes used by extension sources', () => {`
- `it('redacts credentialed URLs embedded in diagnostic messages', () => {`

## 代码阅读结论

- 最终实现以 PR #4500 的合入代码为准，核心判断来自 7 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
