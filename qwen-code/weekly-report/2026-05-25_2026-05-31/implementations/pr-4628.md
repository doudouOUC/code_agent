# PR #4628 最终实现文档

> 来源 PR: [feat(telemetry): add client_id attribute and permission route spans](https://github.com/QwenLM/qwen-code/pull/4628)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +167 / -10，6 个文件
> 时间: merged `2026-05-30T16:09:43Z`；closed `2026-05-30T16:09:43Z`

## 实现目标

围绕 `add client_id attribute and permission route spans` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/bridge.ts、packages/cli/src/serve/server.ts、packages/core/src/telemetry/daemon-tracing.ts`，这些文件承载 `add client_id attribute and permission route spans` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `addDaemonRequestAttribute`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `POST /session/:id/permission/:requestId、POST /permission/:requestId、CLIENT_ID_HEADER、MAX_CLIENT_ID_LENGTH、CLIENT_ID_RE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('includes clientId and permissionRequestId in request span attributes', async () => {、it('omits clientId and permissionRequestId when not provided', async () => {、it('addDaemonRequestAttribute sets attribute on the active span', () => {、it('addDaemonRequestAttribute is a no-op without an active span', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/bridge.ts`
- `packages/cli/src/serve/server.ts`
- `packages/core/src/telemetry/daemon-tracing.ts`
- `packages/core/src/telemetry/index.ts`

### 测试与验证文件
- `packages/acp-bridge/src/bridge.test.ts`
- `packages/core/src/telemetry/daemon-tracing.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `addDaemonRequestAttribute`

### 接口、命令或环境变量
- `POST /session/:id/permission/:requestId`
- `POST /permission/:requestId`
- `CLIENT_ID_HEADER`
- `MAX_CLIENT_ID_LENGTH`
- `CLIENT_ID_RE`

### 测试用例线索
- `it('includes clientId and permissionRequestId in request span attributes', async () => {`
- `it('omits clientId and permissionRequestId when not provided', async () => {`
- `it('addDaemonRequestAttribute sets attribute on the active span', () => {`
- `it('addDaemonRequestAttribute is a no-op without an active span', () => {`

## 代码阅读结论

- 最终实现以 PR #4628 的合入代码为准，核心判断来自 6 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
