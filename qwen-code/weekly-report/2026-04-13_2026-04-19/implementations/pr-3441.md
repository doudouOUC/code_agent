# PR #3441 最终实现文档

> 来源 PR: [feat(cli): add conversation rewind feature with double-ESC and /rewind command](https://github.com/QwenLM/qwen-code/pull/3441)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1533 / -6，21 个文件
> 时间: merged `2026-04-25T14:12:30Z`；closed `2026-04-25T14:12:30Z`

## 实现目标

围绕 `add conversation rewind feature with double-ESC and /rewind command` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/services/BuiltinCommandLoader.ts、packages/cli/src/ui/AppContainer.tsx、packages/cli/src/ui/commands/rewindCommand.ts`，这些文件承载 `add conversation rewind feature with double-ESC and /rewind command` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `rewindCommand、RewindSelectorProps、MAX_VISIBLE_ITEMS、getUserTurns、TurnItemViewProps、TurnItemView、RewindSelector、DOUBLE_PRESS_TIMEOUT_MS`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `BUILT_IN、MAX_VISIBLE_ITEMS、DOUBLE_PRESS_TIMEOUT_MS、STARTUP_CONTEXT_MODEL_ACK`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('computeApiTruncationIndex', () => {、it('returns 0 for empty API history', () => {、describe('without startup context', () => {、it('rewinds to the first user turn (keep nothing)', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/services/BuiltinCommandLoader.ts`
- `packages/cli/src/ui/AppContainer.tsx`
- `packages/cli/src/ui/commands/rewindCommand.ts`
- `packages/cli/src/ui/commands/types.ts`
- `packages/cli/src/ui/components/DialogManager.tsx`
- `packages/cli/src/ui/components/Footer.tsx`
- `packages/cli/src/ui/components/RewindSelector.tsx`
- `packages/cli/src/ui/contexts/UIActionsContext.tsx`
- `packages/cli/src/ui/contexts/UIStateContext.tsx`
- `packages/cli/src/ui/hooks/slashCommandProcessor.ts`
- `packages/cli/src/ui/hooks/useDoublePress.ts`
- `packages/cli/src/ui/hooks/useHistoryManager.ts`
- `packages/cli/src/ui/hooks/useResumeCommand.ts`
- `packages/cli/src/ui/utils/historyMapping.ts`
- 另有 4 个实现文件未展开。

### 测试与验证文件
- `packages/cli/src/ui/hooks/useResumeCommand.test.ts`
- `packages/cli/src/ui/utils/historyMapping.test.ts`
- `scripts/test-rewind-e2e.sh`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `rewindCommand`
- `RewindSelectorProps`
- `MAX_VISIBLE_ITEMS`
- `getUserTurns`
- `TurnItemViewProps`
- `TurnItemView`
- `RewindSelector`
- `DOUBLE_PRESS_TIMEOUT_MS`
- `useDoublePress`
- `STARTUP_CONTEXT_MODEL_ACK`
- `isUserTextContent`
- `hasStartupContext`

### 接口、命令或环境变量
- `BUILT_IN`
- `MAX_VISIBLE_ITEMS`
- `DOUBLE_PRESS_TIMEOUT_MS`
- `STARTUP_CONTEXT_MODEL_ACK`

### 测试用例线索
- `describe('computeApiTruncationIndex', () => {`
- `it('returns 0 for empty API history', () => {`
- `describe('without startup context', () => {`
- `it('rewinds to the first user turn (keep nothing)', () => {`
- `it('rewinds to the second user turn (keep first turn)', () => {`
- `it('rewinds to the third user turn', () => {`
- `describe('with startup context pair', () => {`
- `it('keeps startup context when rewinding to the first turn', () => {`
- `it('keeps startup + first turn when rewinding to second turn', () => {`
- `describe('with tool call entries (functionResponse)', () => {`

## 代码阅读结论

- 最终实现以 PR #3441 的合入代码为准，核心判断来自 21 个 changed files 和已提取的 patch 线索。
- 为控制文档长度，本页只展开代表性路径；完整文件列表可通过 PR 页面或 GitHub files API 复查。
