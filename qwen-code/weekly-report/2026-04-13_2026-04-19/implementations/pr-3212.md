# PR #3212 最终实现文档

> 来源 PR: [fix(core): respect custom Gemini baseUrl from modelProviders](https://github.com/QwenLM/qwen-code/pull/3212)
> 状态: ✅ merged
> 变更规模: +57/-1，2 个文件
> 时间: created `04-13`；merged/closed `04-15`

## 解决的问题

最小精准的 gemini baseUrl 修复

## 最终实现方案

在 `createGeminiContentGenerator` 按描述把 `config.baseUrl` 注入 `httpOptions`，仅在配置时添加，headers 不变。 `config.baseUrl ? {headers,baseUrl} : {headers}` 干净，避免写 `undefined`；两路径均有回归测试。

## 审查结论

- 一致性: 在 `createGeminiContentGenerator` 按描述把 `config.baseUrl` 注入 `httpOptions`，仅在配置时添加，headers 不变。
- 描述准确性: 准确；root cause（只透传 headers、丢弃 baseUrl）与 issue 自带源码定位一致。
- 正确性: `config.baseUrl ? {headers,baseUrl} : {headers}` 干净，避免写 `undefined`；两路径均有回归测试。
- 结论: 最小且精准的 bug 修复，与 issue 一一对应。

## 归档口径

- 归档周目录：`2026-04-13_2026-04-19`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
