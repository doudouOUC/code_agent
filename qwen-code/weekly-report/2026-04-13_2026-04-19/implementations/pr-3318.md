# PR #3318 最终实现文档

> 来源 PR: [feat(cli): add API preconnect to reduce first-call latency](https://github.com/QwenLM/qwen-code/pull/3318)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +731 / -13，8 个文件
> 时间: merged `2026-04-26T22:54:56Z`；closed `2026-04-26T22:54:56Z`

## 实现目标

围绕 `add API preconnect to reduce first-call latency` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/gemini.tsx、packages/cli/src/utils/apiPreconnect.ts、packages/core/src/index.ts`，这些文件承载 `add API preconnect to reduce first-call latency` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `DEFAULT_BASE_URLS、shouldSkipPreconnect、isInSandboxMode、isDefaultBaseUrl、ENV_BASE_URL_MAP、getEnvBaseUrlForAuthType、getPreconnectTargetUrl、preconnectApi`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `DEFAULT_BASE_URLS、NO_PROXY、HTTPS_PROXY、HTTP_PROXY、NODE_EXTRA_CA_CERTS、ENV_BASE_URL_MAP、OPENAI_BASE_URL、ANTHROPIC_BASE_URL、GEMINI_BASE_URL、QWEN_CODE_DISABLE_PRECONNECT`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('apiPreconnect', () => {、describe('shouldSkipPreconnect', () => {、it('should skip when HTTPS_PROXY is set', () => {、it('should skip when https_proxy is set', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `eslint.config.js` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/gemini.tsx`
- `packages/cli/src/utils/apiPreconnect.ts`
- `packages/core/src/index.ts`
- `packages/core/src/utils/runtimeFetchOptions.ts`
- `scripts/benchmark-api-latency.mjs`

### 测试与验证文件
- `packages/cli/src/utils/apiPreconnect.test.ts`
- `packages/core/src/utils/runtimeFetchOptions.test.ts`

### 文档、配置与发布材料
- `eslint.config.js`

## 代码变更线索

### 新增或调整的实现符号
- `DEFAULT_BASE_URLS`
- `shouldSkipPreconnect`
- `isInSandboxMode`
- `isDefaultBaseUrl`
- `ENV_BASE_URL_MAP`
- `getEnvBaseUrlForAuthType`
- `getPreconnectTargetUrl`
- `preconnectApi`
- `resetPreconnectState`
- `getOrCreateSharedDispatcher`
- `resetDispatcherCache`
- `ITERATIONS`

### 接口、命令或环境变量
- `DEFAULT_BASE_URLS`
- `NO_PROXY`
- `HTTPS_PROXY`
- `HTTP_PROXY`
- `NODE_EXTRA_CA_CERTS`
- `ENV_BASE_URL_MAP`
- `OPENAI_BASE_URL`
- `ANTHROPIC_BASE_URL`
- `GEMINI_BASE_URL`
- `QWEN_CODE_DISABLE_PRECONNECT`

### 测试用例线索
- `describe('apiPreconnect', () => {`
- `describe('shouldSkipPreconnect', () => {`
- `it('should skip when HTTPS_PROXY is set', () => {`
- `it('should skip when https_proxy is set', () => {`
- `it('should skip when HTTP_PROXY is set', () => {`
- `it('should skip when http_proxy is set', () => {`
- `it('should skip when NODE_EXTRA_CA_CERTS is set', () => {`
- `it('should skip when custom baseUrl is set', () => {`
- `it('should not skip when baseUrl is a default URL', () => {`
- `describe('preconnect behavior', () => {`

## 代码阅读结论

- 最终实现以 PR #3318 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
