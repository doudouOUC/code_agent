# PR #3407 最终实现文档

> 来源 PR: [fix(cli): auto-submit on number key press in AskUserQuestionDialog](https://github.com/QwenLM/qwen-code/pull/3407)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +135 / -65，2 个文件
> 时间: merged `2026-04-18T02:03:32Z`；closed `2026-04-18T02:03:32Z`

## 实现目标

围绕 `auto-submit on number key press in AskUserQuestionDialog` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/ui/components/messages/AskUserQuestionDialog.tsx`，这些文件承载 `auto-submit on number key press in AskUserQuestionDialog` 的核心行为或配置变化。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/cli/src/ui/components/messages/AskUserQuestionDialog.tsx` 的文件级改动为锚点。
- 验证方式：测试标题覆盖 `it('auto-submits when pressing a number key for a predefined option', async () => {、it('does not auto-submit when pressing number key for "Other" custom input', async () => {、it('does not auto-submit when pressing number key in multi-select mode', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/ui/components/messages/AskUserQuestionDialog.tsx`

### 测试与验证文件
- `packages/cli/src/ui/components/messages/AskUserQuestionDialog.test.tsx`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `it('auto-submits when pressing a number key for a predefined option', async () => {`
- `it('does not auto-submit when pressing number key for "Other" custom input', async () => {`
- `it('does not auto-submit when pressing number key in multi-select mode', async () => {`

## 代码阅读结论

- 最终实现以 PR #3407 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
