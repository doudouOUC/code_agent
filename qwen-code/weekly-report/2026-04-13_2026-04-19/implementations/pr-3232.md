# PR #3232 最终实现文档

> 来源 PR: [feat(cli): add startup performance profiler](https://github.com/QwenLM/qwen-code/pull/3232)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +383 / -0，6 个文件
> 时间: merged `2026-04-14T06:14:43Z`；closed `2026-04-14T06:14:43Z`

## 实现目标

围绕 `add startup performance profiler` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/index.ts、packages/cli/src/gemini.tsx、packages/cli/src/utils/startupProfiler.ts`，这些文件承载 `add startup performance profiler` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `Checkpoint、StartupPhase、StartupReport、initStartupProfiler、profileCheckpoint、getStartupReport、finalizeStartupProfile、resetStartupProfiler`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `QWEN_CODE_PROFILE_STARTUP`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('startupProfiler', () => {、describe('when disabled (no env var)', () => {、it('should return null from getStartupReport', () => {、it('should not write any files on finalize', () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/users/configuration/settings.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/cli/index.ts`
- `packages/cli/src/gemini.tsx`
- `packages/cli/src/utils/startupProfiler.ts`

### 测试与验证文件
- `packages/cli/src/gemini.test.tsx`
- `packages/cli/src/utils/startupProfiler.test.ts`

### 文档、配置与发布材料
- `docs/users/configuration/settings.md`

## 代码变更线索

### 新增或调整的实现符号
- `Checkpoint`
- `StartupPhase`
- `StartupReport`
- `initStartupProfiler`
- `profileCheckpoint`
- `getStartupReport`
- `finalizeStartupProfile`
- `resetStartupProfiler`

### 接口、命令或环境变量
- `QWEN_CODE_PROFILE_STARTUP`

### 测试用例线索
- `describe('startupProfiler', () => {`
- `describe('when disabled (no env var)', () => {`
- `it('should return null from getStartupReport', () => {`
- `it('should not write any files on finalize', () => {`
- `describe('when outside sandbox (SANDBOX not set)', () => {`
- `it('should not enable profiler even with QWEN_CODE_PROFILE_STARTUP=1', () => {`
- `describe('when enabled (QWEN_CODE_PROFILE_STARTUP=1 + SANDBOX)', () => {`
- `it('should collect checkpoints and return a report', () => {`
- `it('should have non-negative durations for each phase', () => {`
- `it('should write JSON file on finalize and print path to stderr', () => {`

## 代码阅读结论

- 最终实现以 PR #3232 的合入代码为准，核心判断来自 6 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
