# PR #3319 最终实现文档

> 来源 PR: [feat(cli): add early input capture to prevent keystroke loss during startup](https://github.com/QwenLM/qwen-code/pull/3319)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +775 / -0，4 个文件
> 时间: merged `2026-04-18T16:40:44Z`；closed `2026-04-18T16:40:44Z`

## 实现目标

围绕 `add early input capture to prevent keystroke loss during startup` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/gemini.tsx、packages/cli/src/ui/contexts/KeypressContext.tsx、packages/cli/src/utils/earlyInputCapture.ts`，这些文件承载 `add early input capture to prevent keystroke loss during startup` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `MAX_BUFFER_SIZE、InputBuffer、isTerminalResponse、skipTerminalResponse、filterTerminalResponses、startEarlyInputCapture、stopEarlyInputCapture、getAndClearCapturedInput`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `--version、EARLY_INPUT、MAX_BUFFER_SIZE、QWEN_CODE_DISABLE_EARLY_CAPTURE、SCRIPT_DIR、CLI_PATH、RESULTS_DIR、CLI_CMD`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('earlyInputCapture', () => {、describe('capture lifecycle', () => {、it('should start and stop capture correctly', () => {、it('should not capture after stop', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/gemini.tsx`
- `packages/cli/src/ui/contexts/KeypressContext.tsx`
- `packages/cli/src/utils/earlyInputCapture.ts`

### 测试与验证文件
- `packages/cli/src/utils/earlyInputCapture.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `MAX_BUFFER_SIZE`
- `InputBuffer`
- `isTerminalResponse`
- `skipTerminalResponse`
- `filterTerminalResponses`
- `startEarlyInputCapture`
- `stopEarlyInputCapture`
- `getAndClearCapturedInput`
- `hasCapturedInput`
- `resetCaptureState`
- `stopAndGetCapturedInput`
- `EscapeSequenceClassification`

### 接口、命令或环境变量
- `--version`
- `EARLY_INPUT`
- `MAX_BUFFER_SIZE`
- `QWEN_CODE_DISABLE_EARLY_CAPTURE`
- `SCRIPT_DIR`
- `CLI_PATH`
- `RESULTS_DIR`
- `CLI_CMD`

### 测试用例线索
- `describe('earlyInputCapture', () => {`
- `describe('capture lifecycle', () => {`
- `it('should start and stop capture correctly', () => {`
- `it('should not capture after stop', () => {`
- `it('should not start capture if not TTY', () => {`
- `it('should not start capture twice', () => {`
- `describe('terminal response filtering', () => {`
- `it('should filter DEC private mode responses (ESC [ ?)', () => {`
- `it('should filter DA2 responses (ESC [ >)', () => {`
- `it('should filter OSC sequences (ESC ])', () => {`

## 代码阅读结论

- 最终实现以 PR #3319 的合入代码为准，核心判断来自 4 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
