# PR #4765 最终实现文档

> 来源 PR: [fix(daemon): preserve parentToolCallId in compaction engine for parallel subagent streams](https://github.com/QwenLM/qwen-code/pull/4765)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +569 / -29，2 个文件
> 时间: merged `2026-06-04T15:37:18Z`；closed `2026-06-04T15:37:18Z`

## 实现目标

围绕 `preserve parentToolCallId in compaction engine for parallel subagent streams` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/acp-bridge/src/compactionEngine.ts`，这些文件承载 `preserve parentToolCallId in compaction engine for parallel subagent streams` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `extractParentToolCallIdFromMeta`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 验证方式：测试标题覆盖 `describe('parentToolCallId-aware text merging', () => {、it('separates text chunks with different parentToolCallIds', () => {、it('merges interleaved thought chunks with the same parentToolCallId', () => {、it('does not merge top-level text with subagent text', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/acp-bridge/src/compactionEngine.ts`

### 测试与验证文件
- `packages/acp-bridge/src/compactionEngine.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `extractParentToolCallIdFromMeta`

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `describe('parentToolCallId-aware text merging', () => {`
- `it('separates text chunks with different parentToolCallIds', () => {`
- `it('merges interleaved thought chunks with the same parentToolCallId', () => {`
- `it('does not merge top-level text with subagent text', () => {`
- `it('same subagent thought + text produce separate slots', () => {`
- `it('same-parent tool call segments subagent text into separate slots', () => {`
- `it('non-parent tool call does not evict subagent text slots', () => {`
- `it('[subA, main, main, subA] produces two merged events', () => {`
- `it('handles 9 parallel subagent thought streams without garbling', () => {`
- `it('defensive backfill: parentToolCallId in output even if last chunk _meta lost it', () => {`

## 代码阅读结论

- 最终实现以 PR #4765 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
