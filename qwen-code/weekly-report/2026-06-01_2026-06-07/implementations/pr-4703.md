# PR #4703 最终实现文档

> 来源 PR: [fix(core): explicitly set stream: false in non-streaming requests](https://github.com/QwenLM/qwen-code/pull/4703)
> 状态: ✅ merged
> 变更规模: +6/-1，2 个文件
> 时间: created `06-02`；merged/closed `06-02`

## 解决的问题

非流式请求显式 stream:false，最小安全修复

## 最终实现方案

`pipeline.ts:331` else 分支加 `stream: false`。 流式路径不变（仍 `stream: true` + `stream_options`）；最小安全修复。

## 审查结论

- 一致性: `pipeline.ts:331` else 分支加 `stream: false`。
- 描述准确性: 准确（根因：网关在字段缺失时默认 SSE）。
- 正确性: 流式路径不变（仍 `stream: true` + `stream_options`）；最小安全修复。
- 结论: 一行修复，干净。

## 归档口径

- 归档周目录：`2026-06-01_2026-06-07`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
