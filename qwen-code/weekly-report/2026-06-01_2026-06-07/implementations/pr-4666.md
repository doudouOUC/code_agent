# PR #4666 最终实现文档

> 来源 PR: [fix(daemon): btw cross-session leak + timeout + input cap + permission](https://github.com/QwenLM/qwen-code/pull/4666)
> 状态: ✅ merged
> 变更规模: +67/-45，5 个文件
> 时间: created `06-01`；merged/closed `06-01`

## 解决的问题

btw 修复 4/5 项到位；shallow copy 项仅在描述中、代码未落地

## 最终实现方案

描述列 5 项修复，但 "btw shallow copy: `getHistoryTail(40, false)`" 未在 diff 中；`btwUtils.ts` 当前仍为 `getHistoryTail(maxHistoryEntries, true)`（deep clone）。其余 4 项（cross-session leak、timeout、input cap、requestId cardinality）均落地。

## 审查结论

- 一致性: 描述列 5 项修复，但 "btw shallow copy: `getHistoryTail(40, false)`" 未在 diff 中；`btwUtils.ts` 当前仍为 `getHistoryTail(maxHistoryEntries, true)`（deep clone）。其余 4 项（cross-session leak、timeout、input cap、requestId cardinality）均落地。
- 描述准确性: 4/5 准确，shallow copy 仅存于描述。
- 正确性: 已实现的 4 项均正确：移除 `getCacheSafeParams()` 回退；`childSignal.aborted` 替代不可靠的 `instanceof DOMException`（并修正 `RequestError.internalError` 参数顺序）；`BTW_MAX_INPUT_LENGTH` 统一三入口；`CLIENT_ID_RE` + 长度上限防基数爆炸。
- 结论: 安全性改进到位，但 shallow copy 项虚报——需补实现或修正描述。

## 归档口径

- 归档周目录：`2026-06-01_2026-06-07`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
