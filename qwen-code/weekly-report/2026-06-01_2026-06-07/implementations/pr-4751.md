# PR #4751 最终实现文档

> 来源 PR: [feat(daemon): optimize ACP child lifecycle — skip relaunch, preheat, i](https://github.com/QwenLM/qwen-code/pull/4751)
> 状态: ✅ merged
> 变更规模: +2224/-21，15 个文件
> 时间: created `06-03`；merged/closed `06-05`

## 解决的问题

ACP 子进程生命周期优化（skip relaunch+preheat+idle）；1852 行 benchmark 测试维护面大 + POSIX-only

## 最终实现方案

skip relaunch + preheat + idle keep-alive 三项优化全落地；描述含架构图。核心逻辑（`getAcpMemoryArgs` cgroup 感知内存分配 + 16GB cap）正确；

## 审查结论

- 一致性: skip relaunch + preheat + idle keep-alive 三项优化全落地；描述含架构图。
- 正确性: 核心逻辑（`getAcpMemoryArgs` cgroup 感知内存分配 + 16GB cap）正确；但 1852 行 benchmark 测试文件（`qwen-daemon-vs-cli-benchmark.test.ts`）虽有 `QWEN_BENCHMARK_ENABLED=1` 门控，维护面大且 POSIX-only（`ps`/`pgrep`/`/usr/bin/time`）。

## 归档口径

- 归档周目录：`2026-06-01_2026-06-07`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
