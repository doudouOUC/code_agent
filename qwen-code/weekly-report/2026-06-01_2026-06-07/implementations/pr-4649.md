# PR #4649 最终实现文档

> 来源 PR: [feat(core): inject context env vars (session/agent/prompt ID) into shell subprocesses](https://github.com/QwenLM/qwen-code/pull/4649)
> 作者: @yiliang114
> 状态: 已合入
> 变更规模: +317 / -0，7 个文件
> 时间: merged `2026-06-02T07:31:10Z`；closed `2026-06-02T07:31:10Z`

## 实现目标

围绕 `inject context env vars (session/agent/prompt ID) into shell subprocesses` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/config/config.ts、packages/core/src/hooks/hookRunner.ts、packages/core/src/services/shellExecutionService.ts`，这些文件承载 `inject context env vars (session/agent/prompt ID) into shell subprocesses` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `getShellContextEnvVars`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `QWEN_CODE_SESSION_ID、QWEN_CODE_AGENT_ID、QWEN_CODE_PROMPT_ID`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('getShellContextEnvVars', () => {、it('returns empty strings for agent/prompt when no context is available', () => {、it('returns QWEN_CODE_SESSION_ID when set in process.env', () => {、it('returns QWEN_CODE_AGENT_ID when called within agent context', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/config/config.ts`
- `packages/core/src/hooks/hookRunner.ts`
- `packages/core/src/services/shellExecutionService.ts`
- `packages/core/src/tools/monitor.ts`
- `packages/core/src/utils/shellContextEnv.ts`

### 测试与验证文件
- `packages/core/src/config/config-session-env.test.ts`
- `packages/core/src/utils/shellContextEnv.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `getShellContextEnvVars`

### 接口、命令或环境变量
- `QWEN_CODE_SESSION_ID`
- `QWEN_CODE_AGENT_ID`
- `QWEN_CODE_PROMPT_ID`

### 测试用例线索
- `describe('getShellContextEnvVars', () => {`
- `it('returns empty strings for agent/prompt when no context is available', () => {`
- `it('returns QWEN_CODE_SESSION_ID when set in process.env', () => {`
- `it('returns QWEN_CODE_AGENT_ID when called within agent context', async () => {`
- `it('returns QWEN_CODE_PROMPT_ID when called within prompt context', () => {`
- `it('returns all vars when all contexts are active', async () => {`
- `it('sets empty string for agent/prompt to override inherited env', () => {`
- `describe('Config sessionEnvClaimed guard', () => {`
- `it('first Config sets process.env QWEN_CODE_SESSION_ID to its sessionId', async () => {`
- `it('subsequent Config does not overwrite the env var set by the first', async () => {`

## 代码阅读结论

- 最终实现以 PR #4649 的合入代码为准，核心判断来自 7 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
