# PR #4811 最终实现文档

> 来源 PR: [feat(cli): enable /remember, /forget, /dream in ACP mode](https://github.com/QwenLM/qwen-code/pull/4811)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +278 / -43，6 个文件
> 时间: merged `2026-06-06T00:34:22Z`；closed `2026-06-06T00:34:22Z`

## 实现目标

围绕 `enable /remember, /forget, /dream in ACP mode` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/ui/commands/dreamCommand.ts、packages/cli/src/ui/commands/forgetCommand.ts、packages/cli/src/ui/commands/rememberCommand.ts`，这些文件承载 `enable /remember, /forget, /dream in ACP mode` 的核心行为或配置变化。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/cli/src/ui/commands/dreamCommand.ts、packages/cli/src/ui/commands/forgetCommand.ts、packages/cli/src/ui/commands/rememberCommand.ts` 的文件级改动为锚点。
- 验证方式：测试标题覆盖 `it('declares acp in supportedModes', () => {、it('returns error when config is not loaded', async () => {、describe('forgetCommand', () => {、it('returns error when no argument is given', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/ui/commands/dreamCommand.ts`
- `packages/cli/src/ui/commands/forgetCommand.ts`
- `packages/cli/src/ui/commands/rememberCommand.ts`

### 测试与验证文件
- `packages/cli/src/ui/commands/dreamCommand.test.ts`
- `packages/cli/src/ui/commands/forgetCommand.test.ts`
- `packages/cli/src/ui/commands/rememberCommand.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `it('declares acp in supportedModes', () => {`
- `it('returns error when config is not loaded', async () => {`
- `describe('forgetCommand', () => {`
- `it('returns error when no argument is given', async () => {`
- `it('returns info message on successful forget', async () => {`
- `it('returns fallback message when no entries match', async () => {`
- `it('returns error message when memory manager throws', async () => {`
- `describe('rememberCommand', () => {`
- `it('returns error when no argument is given', () => {`
- `it('returns submit_prompt for managed auto-memory', () => {`

## 代码阅读结论

- 最终实现以 PR #4811 的合入代码为准，核心判断来自 6 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
