# PR #4333 最终实现文档

> 来源 PR: [feat(core): atomic write rollout for credentials, memory, config, JSONL (closes #3681, #4095 Phase 2)](https://github.com/QwenLM/qwen-code/pull/4333)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +2421 / -157，34 个文件
> 时间: merged `2026-06-02T11:36:50Z`；closed `2026-06-02T11:36:50Z`

## 实现目标

围绕 `atomic write rollout for credentials, memory, config, JSONL (closes #3681, #4095 Phase 2)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/config/trustedFolders.ts、packages/cli/src/services/tips/tipHistory.ts、packages/cli/src/ui/components/InputPrompt.tsx`，这些文件承载 `atomic write rollout for credentials, memory, config, JSONL (closes #3681, #4095 Phase 2)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `blockingSleep、renameWithRetrySync、resolveSymlinkChainSync、atomicWriteFileSync、annotateWriteError`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `SYMLOOP_MAX、O_EXCL、O_WRONLY、O_CREAT、ATOMIC_WRITE`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('atomicWriteFileSync', () => {、it('should write string content to a new file', () => {、it('should write Buffer content to a new file', () => {、it('should not leave temp files on success', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/config/trustedFolders.ts`
- `packages/cli/src/services/tips/tipHistory.ts`
- `packages/cli/src/ui/components/InputPrompt.tsx`
- `packages/cli/src/ui/components/background-view/BackgroundTasksDialog.tsx`
- `packages/cli/src/ui/contexts/BackgroundTaskViewContext.tsx`
- `packages/core/src/core/logger.ts`
- `packages/core/src/extension/extensionManager.ts`
- `packages/core/src/hooks/trustedHooks.ts`
- `packages/core/src/index.ts`
- `packages/core/src/lsp/NativeLspService.ts`
- `packages/core/src/mcp/oauth-token-storage.ts`
- `packages/core/src/mcp/token-storage/file-token-storage.ts`
- `packages/core/src/memory/dream.ts`
- `packages/core/src/memory/extract.ts`
- 另有 10 个实现文件未展开。

### 测试与验证文件
- `packages/cli/src/config/trustedFolders.test.ts`
- `packages/core/src/core/logger.test.ts`
- `packages/core/src/hooks/trustedHooks.test.ts`
- `packages/core/src/lsp/NativeLspService.test.ts`
- `packages/core/src/mcp/oauth-token-storage.test.ts`
- `packages/core/src/mcp/token-storage/file-token-storage.test.ts`
- `packages/core/src/qwen/sharedTokenManager.test.ts`
- `packages/core/src/tools/todoWrite.test.ts`
- `packages/core/src/utils/atomicFileWrite.test.ts`
- `packages/core/src/utils/jsonl-utils.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `blockingSleep`
- `renameWithRetrySync`
- `resolveSymlinkChainSync`
- `atomicWriteFileSync`
- `annotateWriteError`

### 接口、命令或环境变量
- `SYMLOOP_MAX`
- `O_EXCL`
- `O_WRONLY`
- `O_CREAT`
- `ATOMIC_WRITE`

### 测试用例线索
- `describe('atomicWriteFileSync', () => {`
- `it('should write string content to a new file', () => {`
- `it('should write Buffer content to a new file', () => {`
- `it('should not leave temp files on success', () => {`
- `it('should clean up temp file when write fails', () => {`
- `it('should overwrite existing file atomically', () => {`
- `it('should respect encoding option', () => {`
- `it('should resolve symlinks and write to the real target', () => {`
- `it('should write through a broken symlink without replacing it', () => {`
- `it('should resolve multi-level symlink chains', () => {`

## 代码阅读结论

- 最终实现以 PR #4333 的合入代码为准，核心判断来自 34 个 changed files 和已提取的 patch 线索。
- 为控制文档长度，本页只展开代表性路径；完整文件列表可通过 PR 页面或 GitHub files API 复查。
