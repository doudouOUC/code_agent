# PR #4689 最终实现文档

> 来源 PR: [fix(daemon): isolate parallel subAgent text streams in transcript reducer](https://github.com/QwenLM/qwen-code/pull/4689)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +798 / -18，8 个文件
> 时间: merged `2026-06-03T14:08:37Z`；closed `2026-06-03T14:08:37Z`

## 实现目标

围绕 `isolate parallel subAgent text streams in transcript reducer` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/cli/src/acp-integration/session/SubAgentTracker.ts、packages/cli/src/acp-integration/session/emitters/MessageEmitter.ts、packages/sdk-typescript/src/daemon/ui/normalizer.ts`，这些文件承载 `isolate parallel subAgent text streams in transcript reducer` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `clearActiveText、extractParentToolCallId`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 验证方式：测试标题覆盖 `describe('parallel subAgent text interleaving — normalizer', () => {、it('extracts parentToolCallId from _meta on agent_message_chunk', () => {、it('extracts parentToolCallId from _meta on agent_thought_chunk', () => {、it('omits parentToolCallId when _meta is absent', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/cli/src/acp-integration/session/SubAgentTracker.ts`
- `packages/cli/src/acp-integration/session/emitters/MessageEmitter.ts`
- `packages/sdk-typescript/src/daemon/ui/normalizer.ts`
- `packages/sdk-typescript/src/daemon/ui/store.ts`
- `packages/sdk-typescript/src/daemon/ui/transcript.ts`
- `packages/sdk-typescript/src/daemon/ui/types.ts`

### 测试与验证文件
- `packages/cli/src/acp-integration/session/SubAgentTracker.test.ts`
- `packages/sdk-typescript/test/unit/daemonUi.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `clearActiveText`
- `extractParentToolCallId`

### 接口、命令或环境变量
- patch 中未提取到新的 HTTP 路由、CLI 参数或环境变量。

### 测试用例线索
- `describe('parallel subAgent text interleaving — normalizer', () => {`
- `it('extracts parentToolCallId from _meta on agent_message_chunk', () => {`
- `it('extracts parentToolCallId from _meta on agent_thought_chunk', () => {`
- `it('omits parentToolCallId when _meta is absent', () => {`
- `it('drops non-string parentToolCallId from _meta', () => {`
- `describe('parallel subAgent text interleaving fix', () => {`
- `it('T1: separates text chunks by parentToolCallId into independent blocks', () => {`
- `it('T2: scoped clearActiveText — subAgent A tool does not interrupt subAgent B text', () => {`
- `it('T3: finishAssistant sets streaming=false on all keyed-map blocks', () => {`
- `it('T4: regression — text without parentToolCallId uses scalar path', () => {`

## 代码阅读结论

- 最终实现以 PR #4689 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
