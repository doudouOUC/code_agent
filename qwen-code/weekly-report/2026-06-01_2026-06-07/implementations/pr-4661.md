# PR #4661 最终实现文档

> 来源 PR: [feat(telemetry): per-prompt traceId for bounded, renderable traces](https://github.com/QwenLM/qwen-code/pull/4661)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +107 / -144，8 个文件
> 时间: merged `2026-06-01T11:07:27Z`；closed `2026-06-01T11:07:27Z`

## 实现目标

围绕 `per-prompt traceId for bounded, renderable traces` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/telemetry/sdk.ts、packages/core/src/telemetry/session-tracing.ts、packages/core/src/telemetry/tracer.ts`，这些文件承载 `per-prompt traceId for bounded, renderable traces` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `ReadableSpan、Span、SpanProcessor、SessionIdSpanProcessor`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `ROOT_CONTEXT`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('defaults to ROOT_CONTEXT when no parentContext is provided', async () => {、describe('interaction span — per-prompt traceId', () => {、it('uses ROOT_CONTEXT as parent (each interaction is a trace root)', () => {、it('ignores active OTel span — interaction always starts a new trace', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/telemetry/sdk.ts`
- `packages/core/src/telemetry/session-tracing.ts`
- `packages/core/src/telemetry/tracer.ts`
- `packages/core/src/utils/debugLogger.ts`

### 测试与验证文件
- `packages/core/src/telemetry/sdk.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`
- `packages/core/src/telemetry/tracer.test.ts`
- `packages/core/src/utils/debugLogger.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- `ReadableSpan`
- `Span`
- `SpanProcessor`
- `SessionIdSpanProcessor`

### 接口、命令或环境变量
- `ROOT_CONTEXT`

### 测试用例线索
- `it('defaults to ROOT_CONTEXT when no parentContext is provided', async () => {`
- `describe('interaction span — per-prompt traceId', () => {`
- `it('uses ROOT_CONTEXT as parent (each interaction is a trace root)', () => {`
- `it('ignores active OTel span — interaction always starts a new trace', () => {`
- `it('still stamps session.id attribute for cross-prompt correlation', () => {`
- `it('always uses context.active() as parent', async () => {`
- `it('uses context.active() even when no active span exists', async () => {`
- `it('uses deriveTraceId(sessionId) as fallback trace context', async () => {`

## 代码阅读结论

- 最终实现以 PR #4661 的合入代码为准，核心判断来自 8 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
