# PR #4432 最终实现文档

> 来源 PR: [feat(telemetry): Phase 4b — retry visibility for qwen-code.llm_request (#3731)](https://github.com/QwenLM/qwen-code/pull/4432)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +1240 / -40，18 个文件
> 时间: merged `2026-06-05T05:45:47Z`；closed `2026-06-05T05:45:47Z`

## 实现目标

围绕 `Phase 4b — retry visibility for qwen-code.llm_request (#3731)` 做新增能力；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/core/baseLlmClient.ts、packages/core/src/core/client.ts、packages/core/src/core/geminiChat.ts`，这些文件承载 `Phase 4b — retry visibility for qwen-code.llm_request (#3731)` 的核心行为或配置变化。
- 代码落地：新增或调整的实现符号包括 `RetryAttemptContext、snapshotRetryMetadata、EVENT_API_RETRY、logApiRetry、API_RETRY_COUNT、recordApiRetry、ApiRetryEvent、RetryAttemptInfo`，这些符号把 PR 目标落到具体代码入口、类型、常量或服务实现上。
- 对外入口/配置：代码证据中出现 `EVENT_CONTENT_RETRY、EVENT_API_RETRY、SERVICE_NAME、API_RETRY_COUNT、INVALID_CONTENT_RETRY_OPTIONS`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `describe('LoggingContentGenerator — Phase 4b retry context propagation', () => {、it('non-stream: forwards retryContext.attempt/requestSetupMs/retryTotalDelayMs to endLLMRequestSpan', async () => {、it('non-stream: defaults attempt=1 and leaves setup/delay undefined when no retry context (direct call / warmup)', async () => {、it('stream: snapshots retry context in synchronous prelude and forwards through stream wrapper finally block', async () => {`，用于验证核心路径和边界行为。
- 配套材料：同时更新 `docs/design/telemetry-llm-request-timing-design.md` 等文档、配置或发布材料。

## 关键实现路径

### 主要实现文件
- `packages/core/src/core/baseLlmClient.ts`
- `packages/core/src/core/client.ts`
- `packages/core/src/core/geminiChat.ts`
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.ts`
- `packages/core/src/telemetry/constants.ts`
- `packages/core/src/telemetry/index.ts`
- `packages/core/src/telemetry/loggers.ts`
- `packages/core/src/telemetry/metrics.ts`
- `packages/core/src/telemetry/qwen-logger/qwen-logger.ts`
- `packages/core/src/telemetry/session-tracing.ts`
- `packages/core/src/telemetry/types.ts`
- `packages/core/src/utils/retry.ts`
- `packages/core/src/utils/retryContext.ts`

### 测试与验证文件
- `packages/core/src/core/loggingContentGenerator/loggingContentGenerator.test.ts`
- `packages/core/src/telemetry/loggers.test.ts`
- `packages/core/src/telemetry/session-tracing.test.ts`
- `packages/core/src/utils/retry.test.ts`

### 文档、配置与发布材料
- `docs/design/telemetry-llm-request-timing-design.md`

## 代码变更线索

### 新增或调整的实现符号
- `RetryAttemptContext`
- `snapshotRetryMetadata`
- `EVENT_API_RETRY`
- `logApiRetry`
- `API_RETRY_COUNT`
- `recordApiRetry`
- `ApiRetryEvent`
- `RetryAttemptInfo`
- `retryContext`

### 接口、命令或环境变量
- `EVENT_CONTENT_RETRY`
- `EVENT_API_RETRY`
- `SERVICE_NAME`
- `API_RETRY_COUNT`
- `INVALID_CONTENT_RETRY_OPTIONS`

### 测试用例线索
- `describe('LoggingContentGenerator — Phase 4b retry context propagation', () => {`
- `it('non-stream: forwards retryContext.attempt/requestSetupMs/retryTotalDelayMs to endLLMRequestSpan', async () => {`
- `it('non-stream: defaults attempt=1 and leaves setup/delay undefined when no retry context (direct call / warmup)', async () => {`
- `it('stream: snapshots retry context in synchronous prelude and forwards through stream wrapper finally block', async () => {`
- `it('stream: defaults attempt=1 when iterated outside any retry frame', async () => {`
- `describe('logApiRetry (Phase 4b)', () => {`
- `it('fans out to all 3 sinks: QwenLogger, OTel log, and metric counter', () => {`
- `it('propagates subagent_name when present', () => {`
- `it('skips logger.emit and metric counter when SDK is not initialized (QwenLogger still called)', () => {`
- `it('endLLMRequestSpan does NOT subtract requestSetupMs from sampling_ms (Phase 4b bug fix)', () => {`

## 代码阅读结论

- 最终实现以 PR #4432 的合入代码为准，核心判断来自 18 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
