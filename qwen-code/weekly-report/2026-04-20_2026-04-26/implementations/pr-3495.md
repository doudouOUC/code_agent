# PR #3495 最终实现文档

> 来源 PR: [fix(core): preserve settings-sourced apiKey when registry model envKey is absent](https://github.com/QwenLM/qwen-code/pull/3495)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +675 / -0，2 个文件
> 时间: merged `2026-04-25T23:37:56Z`；closed `2026-04-25T23:37:56Z`

## 实现目标

围绕 `preserve settings-sourced apiKey when registry model envKey is absent` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/models/modelsConfig.ts`，这些文件承载 `preserve settings-sourced apiKey when registry model envKey is absent` 的核心行为或配置变化。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/core/src/models/modelsConfig.ts` 的文件级改动为锚点。
- 对外入口/配置：代码证据中出现 `--openaiApiKey、QWEN_OAUTH、OPENAI_API_KEY`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should fall back to settings-sourced apiKey when registry model envKey is not in process.env (restart scenario)', () => {、it('should prefer env var over settings apiKey when both exist (restart scenario)', () => {、it('should NOT preserve programmatic apiKey when registry model envKey is absent', () => {、it('should NOT preserve env apiKey with via.modelProviders during model switch', () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/models/modelsConfig.ts`

### 测试与验证文件
- `packages/core/src/models/modelsConfig.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- `--openaiApiKey`
- `QWEN_OAUTH`
- `OPENAI_API_KEY`

### 测试用例线索
- `it('should fall back to settings-sourced apiKey when registry model envKey is not in process.env (restart scenario)', () => {`
- `it('should prefer env var over settings apiKey when both exist (restart scenario)', () => {`
- `it('should NOT preserve programmatic apiKey when registry model envKey is absent', () => {`
- `it('should NOT preserve env apiKey with via.modelProviders during model switch', () => {`
- `it('should preserve general env var apiKey (e.g. OPENAI_API_KEY) when provider envKey is absent', () => {`
- `it('should preserve CLI-sourced apiKey (--openaiApiKey) when registry model envKey is absent', () => {`
- `it('should preserve programmatic apiKey when authType and modelId unchanged (restart scenario)', () => {`
- `it('should NOT preserve settings-sourced apiKey when switching to a different provider within same authType', () => {`
- `it('should NOT preserve CLI-sourced apiKey when switching to a different provider within same authType', () => {`
- `it('should NOT preserve apiKey on first syncAfterAuthRefresh when previousAuthType is undefined (cold start)', () => {`

## 代码阅读结论

- 最终实现以 PR #3495 的合入代码为准，核心判断来自 2 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
