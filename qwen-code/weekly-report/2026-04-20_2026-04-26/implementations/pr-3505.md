# PR #3505 最终实现文档

> 来源 PR: [fix(core): reject truncated subagent write_file calls](https://github.com/QwenLM/qwen-code/pull/3505)
> 作者: @doudouOUC
> 状态: 已合入
> 变更规模: +305 / -20，4 个文件
> 时间: merged `2026-04-22T07:01:42Z`；closed `2026-04-22T07:01:42Z`

## 实现目标

围绕 `reject truncated subagent write_file calls` 做问题修复；最终实现方案按下方代码路径、接口/配置、符号和测试线索归纳。
PR body 只能辅助理解意图；本页的最终实现口径以 changed files、已提取的 patch 线索、测试和配置路径为准。

## 最终实现方案

- 实现摘要：主要改动落在 `packages/core/src/agents/runtime/agent-core.ts、packages/core/src/core/coreToolScheduler.ts`，这些文件承载 `reject truncated subagent write_file calls` 的核心行为或配置变化。
- 代码落地：未提取到稳定新增导出符号，最终实现以 `packages/core/src/agents/runtime/agent-core.ts、packages/core/src/core/coreToolScheduler.ts` 的文件级改动为锚点。
- 对外入口/配置：代码证据中出现 `MAX_TOKENS、TRUNCATION_EDIT_REJECTION、OUTPUT_TRUNCATED`，说明最终实现已经接入对应 HTTP 路由、CLI 参数、协议常量或环境变量。
- 验证方式：测试标题覆盖 `it('should mark truncated subagent write_file calls as output-truncated errors', async () => {、it('should prefer truncation rejection over validation errors for truncated write_file calls', async () => {、it('should NOT reject write_file when truncated attempt is followed by successful retry', async () => {`，用于验证核心路径和边界行为。

## 关键实现路径

### 主要实现文件
- `packages/core/src/agents/runtime/agent-core.ts`
- `packages/core/src/core/coreToolScheduler.ts`

### 测试与验证文件
- `packages/core/src/agents/runtime/agent-headless.test.ts`
- `packages/core/src/core/coreToolScheduler.test.ts`

### 文档、配置与发布材料
- 无。

## 代码变更线索

### 新增或调整的实现符号
- patch 中未提取到稳定的新增实现符号；以关键路径中的文件级变更为准。

### 接口、命令或环境变量
- `MAX_TOKENS`
- `TRUNCATION_EDIT_REJECTION`
- `OUTPUT_TRUNCATED`

### 测试用例线索
- `it('should mark truncated subagent write_file calls as output-truncated errors', async () => {`
- `it('should prefer truncation rejection over validation errors for truncated write_file calls', async () => {`
- `it('should NOT reject write_file when truncated attempt is followed by successful retry', async () => {`

## 代码阅读结论

- 最终实现以 PR #3505 的合入代码为准，核心判断来自 4 个 changed files 和已提取的 patch 线索。
- 本页已列出该 PR 的全部 changed files。
