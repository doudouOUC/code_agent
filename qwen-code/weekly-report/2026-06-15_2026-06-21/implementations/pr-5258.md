# PR #5258 最终实现文档

> 来源 PR: [fix(cli): Stop after cancelled permissions](https://github.com/QwenLM/qwen-code/pull/5258)
> 状态: ✅ merged
> 变更规模: +601/-140，4 个文件
> 时间: created `06-18`；merged/closed `06-19`

## 解决的问题

把 #5218 的停止语义推广到所有 ACP 工具权限取消，不只限于 `ask_user_question`。

## 最终实现方案

当权限 vote 解析为 cancelled、reject option 映射到 `Cancel`、或权限请求通道失败时，记录被拒工具并跳过同一模型响应里的后续工具；嵌套 Agent 权限取消也 fail-closed 中止父 Agent turn，并用 daemon/WebShell HTTP/SSE E2E 验证取消后两个 shell sentinel 都不会落盘，已于 06-19 合入。

## 归档口径

- 归档周目录：`2026-06-15_2026-06-21`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
