# PR #5179 最终实现文档

> 来源 PR: [fix(model): remember selected provider when multiple share a model id (#5173)](https://github.com/QwenLM/qwen-code/pull/5179)
> 状态: ✅ merged
> 变更规模: +525/-14，16 个文件
> 时间: created `06-16`；merged/closed `06-18`

## 解决的问题

修复多个 `modelProviders` 条目共享同一 model id 但 `baseUrl` 不同时，模型选择器重启后丢失用户实际选择的 provider。

## 最终实现方案

选择器持久化 `model.name` 时同步写入 `model.baseUrl`；启动解析和 pre-flight auth 按 `id + baseUrl` 精确匹配 provider，找不到时回退首个 id；`/model <id>`、ACP `setModel`、`qwen/settings/setCoreValue` 和 provider install 等 id-only 写入路径用空字符串 tombstone 清掉旧 `model.baseUrl`，避免跨 scope stale disambiguator 继续生效，并补 model config/auth/dialog/command/ACP/provider install tests，已于 06-18 合入。

## 归档口径

- 归档周目录：`2026-06-15_2026-06-21`。
- 本页按周报 README 的 PR 明细归档，内容来自同周 README/review 已整理的 diff 审查摘要。
- PR body 只作为目标线索；描述最终行为时应优先以后续补读的 merged diff、changed files、patch、测试和配置路径为准。
