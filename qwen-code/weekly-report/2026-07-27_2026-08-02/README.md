# qwen-code PRs · 2026-07-27 ~ 2026-08-02 (W31 最终版)

> 本文件已整理 2026-07-27 至 2026-08-02（Asia/Shanghai）创建的 @doudouOUC 个人 PR。口径为 `QwenLM/qwen-code` 中 author 为 @doudouOUC 且 createdAt 落在对应北京时间周窗口内的 PR；只在窗口内更新、关闭或合入，但创建时间不在窗口内的 PR 不计入新增统计。open PR 只记录当前 diff 方案，不能视为 `main` 已落地能力。

**主题**: daemon first-output latency benchmark、submitted prompt provenance、ACP provider preload、managed session writer shutdown、first-output benchmark validity/schema hardening、Todo Stop Guard continuation hardening、external-context submitted-prompt auto recall、transcript timestamp drift tolerance、session writer lease opt-in、ARMS session user ID、Serve large text bounded reads、handle-bound text range reader refactor、daemon maintenance writer leases、certified writer handoff、immediate prompt dispatch benchmark、workspace text byte-cursor paging、workspace skill status read model、daemon resource budgeting foundations、GenAI time-to-first-chunk tracing、tool-call terminal telemetry normalization、tool execution outcome telemetry、external-context dependency hardening、daemon memory budget reporting、mobile-mcp Node.js 22 / Hono dependency hardening、Auto Recall proxy dispatcher lifecycle、workspace text lineEnding consistency

**PR 统计**: 27 PRs - 25 merged / 0 open / 2 closed
**当前已合并 PR 代码量**: +41,281 / -5,039，376 个文件变更
**全量代码量**: +43,179 / -5,171，391 个文件变更
**类型分布**: fix ×13, feat ×10, test ×2, perf ×1, refactor ×1
**范围 (scope)**: serve ×10, core ×5, external-context ×3, test ×2, acp ×1, daemon ×1, hooks ×1, integration ×1, mobile-mcp ×1, telemetry ×1, unscoped ×1

---

## PR 明细

| PR | 状态 | 作者 | 标题 | 变更 | 文件 | 创建(UTC) | 合并/关闭(UTC) |
|---|---|---|---|---:|---:|---|---|
| [#7761](https://github.com/QwenLM/qwen-code/pull/7761) | ✅ merged | @doudouOUC | test(serve): Add first-output latency benchmark | +4632/-7 | 6 | 07-26 19:57 | 07-27 03:38 |
| [#7762](https://github.com/QwenLM/qwen-code/pull/7762) | ✅ merged | @doudouOUC | feat(hooks): Add submitted prompt provenance | +2535/-223 | 37 | 07-26 20:07 | 07-27 15:53 |
| [#7767](https://github.com/QwenLM/qwen-code/pull/7767) | ✅ merged | @doudouOUC | perf(acp): Preload providers after session creation | +646/-10 | 6 | 07-26 22:13 | 07-27 16:48 |
| [#7812](https://github.com/QwenLM/qwen-code/pull/7812) | ✅ merged | @doudouOUC | fix(serve): Release managed session writer locks on shutdown | +2363/-317 | 20 | 07-27 06:57 | 07-28 10:18 |
| [#7820](https://github.com/QwenLM/qwen-code/pull/7820) | ✅ merged | @doudouOUC | fix(test): Restore first-output benchmark measurement validity and correct its artifact schema | +382/-112 | 7 | 07-27 08:20 | 07-28 06:51 |
| [#7821](https://github.com/QwenLM/qwen-code/pull/7821) | ✅ merged | @doudouOUC | fix(daemon): harden Todo Stop Guard continuations | +4281/-720 | 17 | 07-27 08:21 | 07-28 06:51 |
| [#7825](https://github.com/QwenLM/qwen-code/pull/7825) | ✅ merged | @doudouOUC | fix(test): Correct first-output benchmark artifact schema and simplify | +47/-74 | 4 | 07-27 08:48 | 07-27 08:51 |
| [#7877](https://github.com/QwenLM/qwen-code/pull/7877) | ✅ merged | @doudouOUC | feat(external-context): Add submitted-prompt auto recall | +2197/-113 | 23 | 07-27 23:53 | 07-29 05:21 |
| [#7886](https://github.com/QwenLM/qwen-code/pull/7886) | ✅ merged | @doudouOUC | fix(core): Tolerate transcript timestamp drift | +1820/-146 | 8 | 07-28 02:17 | 07-31 08:12 |
| [#7894](https://github.com/QwenLM/qwen-code/pull/7894) | ✅ merged | @doudouOUC | feat: Gate session writer lease behind opt-in | +215/-8 | 14 | 07-28 03:07 | 07-28 04:37 |
| [#7921](https://github.com/QwenLM/qwen-code/pull/7921) | ✅ merged | @doudouOUC | feat(core): Add ARMS session user ID | +569/-8 | 18 | 07-28 09:23 | 07-28 11:10 |
| [#7947](https://github.com/QwenLM/qwen-code/pull/7947) | ✅ merged | @doudouOUC | fix(serve): allow bounded reads of large text files | +1433/-171 | 18 | 07-28 15:45 | 07-29 07:50 |
| [#7967](https://github.com/QwenLM/qwen-code/pull/7967) | ✅ merged | @doudouOUC | refactor(core): thread the descriptor instead of forking text-read helpers | +76/-12 | 7 | 07-29 00:38 | 08-01 02:07 |
| [#7975](https://github.com/QwenLM/qwen-code/pull/7975) | ✅ merged | @doudouOUC | fix(serve): Isolate daemon session maintenance writers | +3065/-1011 | 31 | 07-29 02:02 | 07-30 14:02 |
| [#7976](https://github.com/QwenLM/qwen-code/pull/7976) | ✅ merged | @doudouOUC | fix(serve): Add certified session writer handoff | +2513/-44 | 9 | 07-29 02:03 | 07-29 23:37 |
| [#7994](https://github.com/QwenLM/qwen-code/pull/7994) | ✅ merged | @doudouOUC | test(integration): Measure immediate prompt dispatch stages | +311/-53 | 4 | 07-29 02:59 | 07-29 23:38 |
| [#8002](https://github.com/QwenLM/qwen-code/pull/8002) | ✅ merged | @doudouOUC | feat(serve): page large text files by byte cursor | +2559/-397 | 31 | 07-29 04:21 | 07-30 12:07 |
| [#8080](https://github.com/QwenLM/qwen-code/pull/8080) | ✅ merged | @doudouOUC | fix(serve): Prevent repeated workspace skill rescans | +1681/-151 | 18 | 07-30 02:56 | 07-30 12:52 |
| [#8093](https://github.com/QwenLM/qwen-code/pull/8093) | ❌ closed draft | @doudouOUC | feat(serve): Add daemon resource budgeting foundations | +1757/-0 | 7 | 07-30 05:46 | 07-31 16:24 |
| [#8150](https://github.com/QwenLM/qwen-code/pull/8150) | ✅ merged | @doudouOUC | feat(core): add GenAI time-to-first-chunk tracing | +296/-71 | 8 | 07-30 15:57 | 07-31 13:47 |
| [#8176](https://github.com/QwenLM/qwen-code/pull/8176) | ✅ merged | @doudouOUC | feat(core): Normalize tool-call terminal telemetry | +545/-30 | 13 | 07-31 01:59 | 07-31 11:51 |
| [#8180](https://github.com/QwenLM/qwen-code/pull/8180) | ✅ merged | @doudouOUC | feat(telemetry): Track tool execution outcomes | +6765/-1195 | 36 | 07-31 02:55 | 08-03 10:22 |
| [#8206](https://github.com/QwenLM/qwen-code/pull/8206) | ✅ merged | @doudouOUC | fix(external-context): harden MCP dependencies | +60/-35 | 7 | 07-31 07:31 | 08-01 01:04 |
| [#8245](https://github.com/QwenLM/qwen-code/pull/8245) | ✅ merged | @doudouOUC | feat(serve): resolve and report the daemon memory budget | +1879/-58 | 22 | 07-31 15:56 | 08-02 04:19 |
| [#8311](https://github.com/QwenLM/qwen-code/pull/8311) | ❌ closed | @doudouOUC | fix(mobile-mcp): Remove legacy Hono dependency | +141/-132 | 8 | 08-01 10:46 | 08-03 02:08 |
| [#8352](https://github.com/QwenLM/qwen-code/pull/8352) | ✅ merged | @doudouOUC | fix(external-context): Dispose of Auto Recall proxy dispatcher | +207/-64 | 7 | 08-02 01:57 | 08-02 15:16 |
| [#8383](https://github.com/QwenLM/qwen-code/pull/8383) | ✅ merged | @doudouOUC | fix(serve): detect lineEnding across the file, not the returned slice | +204/-9 | 5 | 08-02 14:50 | 08-04 16:27 |

---

## PR 解决问题、实现方式与 feature 处理

| PR | 解决了什么问题 | 最终怎么实现（open 只登记当前观察） | 对应 feature 文档 |
|---|---|---|---|
| [#7761](https://github.com/QwenLM/qwen-code/pull/7761) | daemon/ACP 首次输出延迟此前只有粗粒度 startup profile，无法把进程启动、session readiness、provider request 到达、首个模型输出、首段 answer text 和 terminal 分开度量，也无法用成对实验判断 preload 类优化是否真的改善用户体感。 | 最终实现新增 opt-in first-output benchmark harness，隔离进程、workspace、home、Qwen home、端口与 compile cache，支持单 bundle cold/warm baseline 与双 bundle AB/BA paired comparison；事件关联以 prompt/session/request 为准，过滤 replay/status/usage/role-only/user echo/compression diagnostics/tool updates，产出 versioned JSON+Markdown artifact、所有 sample、failure code、relative timestamp、request count、cleanup evidence、percentile summary、paired bootstrap CI 与 order-sensitivity。fake OpenAI server 在 benchmark 响应后确定性关闭连接，避免连接池污染。 | 已更新 [cli-startup-performance.md](../../feature/cli-startup-performance.md) 与 daemon feature。完整实现见 [implementations/pr-7761.md](implementations/pr-7761.md)。 |
| [#7762](https://github.com/QwenLM/qwen-code/pull/7762) | `UserPromptSubmit` hook 只能看到已经扩展后的 model-bound `prompt`，无法区分用户原始输入和 reminder、file/resource expansion、slash command、extension、vision 等系统扩展；安全审计和 provenance 需要知道用户实际提交文本，但又不能改变模型上下文或既有 hook 顺序。 | 最终实现给 `UserPromptSubmit` 增加可选 `submitted_prompt`，TUI 只在 fresh `UserQuery` 上填入 trimmed text projection，sidecar 让 deferred turn 与 exact in-memory restoration 继承该 provenance；large paste 使用 compact placeholder projection。对 ambiguous batches、edited restorations、same-turn steering、recursive continuation、retry、machine traffic、unsupported producer、`--prompt-interactive` startup、Vim NORMAL direct submission、image-only 等场景直接省略。取消恢复路径保留 main-turn ownership，同时允许并发 `/btw` side question。 | 已更新 [hooks.md](../../feature/hooks.md)。完整实现见 [implementations/pr-7762.md](implementations/pr-7762.md)。 |
| [#7767](https://github.com/QwenLM/qwen-code/pull/7767) | #7761 证明 ACP child 在 session accepted 后到首个 provider 输出之间仍会为 lazy Provider 构造付费；如果等用户第一条 prompt 才加载 provider，首字延迟仍偏高。 | 最终实现是在 ACP `session/new` response 写回 child transport 后，best-effort 触发内部 lazy Provider preparation；第一条 prompt 复用同一个 in-flight/completed promise，preload 与立即 prompt 并发时只构造一次 Provider。非 lazy generator no-op，background failure 被捕获但 rejected promise 仍 memoized 给首次真实调用观察；Qwen OAuth hot model switch、ACP worktree relocation 会丢弃未使用 preload，已经用于请求的 generator 不被替换。 | 已更新 [cli-startup-performance.md](../../feature/cli-startup-performance.md) 与 daemon feature。完整实现见 [implementations/pr-7767.md](implementations/pr-7767.md)。 |
| [#7812](https://github.com/QwenLM/qwen-code/pull/7812) | daemon-managed ACP child 在 daemon shutdown / replacement 时可能没有释放 exact-owned session writer lock；新 daemon 在不同 hostname 上无法证明旧 owner 已死，如果直接抢锁会产生双写风险，不抢锁又可能留下不可恢复的 writer conflict。 | 最终实现建立 cooperative shutdown：首个 shutdown signal 同步关闭 session/turn admission，等待已接受 transcript work drain，原子 retire exact-owned writer locks；SessionEnd hooks 与 resource cleanup 在 writer phase settle 后运行。daemon 级 registry 跟踪 primary/secondary/dynamic runtimes 的所有 ACP children，先 SIGTERM，5s 后 SIGKILL，10s 内要求 raw process reap；partial channel construction 与 overlapping teardown join 同一 terminal outcome。managed acquisition 不再基于 hostname、age、container-visible PID 抢 existing writer lock，standalone ACP 才保留 local stale-owner recovery。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md) 与 [daemon-serve-mode/12-daemon-sdk-reliability-audit.md](../../feature/daemon-serve-mode/12-daemon-sdk-reliability-audit.md)。完整实现见 [implementations/pr-7812.md](implementations/pr-7812.md)。 |
| [#7820](https://github.com/QwenLM/qwen-code/pull/7820) | #7761 benchmark 在 review 中暴露了若干有效性问题：dwell anchor 绑在 session readiness 而非 SSE readiness、共享 integration config 会并发干扰 benchmark、Phase 1 gate 只看裸 percentile diff、artifact schema 中 bundle-level git commit 等字段口径不准。 | 最终实现把 dwell anchor 改成 SSE readiness 并记录实际 idle window；benchmark runner 从共享 integration config 中拆出 serial 专用 config，helper tests 保持在共享套件；Phase 1 gate 改用 paired bootstrap CI lower bound，percentile diff 仅作连续观察。artifact schema 删除错误的 bundle-level git commit、重命名 prompt shape prose 字段并提升 schema version；所有 invalid duration 归一化，success predicate 合并，comparison-only dwell 在 single-mode reject 后解析，设计文档同步更正。 | 已更新 [cli-startup-performance.md](../../feature/cli-startup-performance.md)。完整实现见 [implementations/pr-7820.md](implementations/pr-7820.md)。 |
| [#7821](https://github.com/QwenLM/qwen-code/pull/7821) | Todo Stop Guard continuation 与 daemon bridge/desktop/channel consumer 之间的 prompt ordering 仍不够原子；continuation 可能与用户输入、model selection、compression/provider submission、workspace relocation、session disposal 或外部 Stop hooks 交错，导致续跑拿错 owner、误删内容或污染较新的 prompt。 | 最终实现把 continuation 排序变成 owner-scoped state machine：bridge invocation prompt id 作为可信 owner claim/release key，Guard 在 queued input/model selection 之后、compression/provider submission 之前 claim 排序位置；失败恢复保留已 drain 用户内容与成功 function responses，只移除 synthetic Guard prompt。若 continuation supersedes inspected response，会重跑 Stop hooks；非阻断 allow 后重置 consecutive block accounting；claim 失败时 fail closed 并保留独立外部 Stop hook。session lifecycle 还把 work-chain agent/monitor lineage、workspace relocation close gate、overlapping prompt ownership 与 cron queue cap 一并收紧。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md)。完整实现见 [implementations/pr-7821.md](implementations/pr-7821.md)。 |
| [#7825](https://github.com/QwenLM/qwen-code/pull/7825) | #7761 的 artifact schema 和若干 helper 还存在 review follow-up：bundle-level commit provenance 错误、prompt shape 字段名像结构化数据但实际是说明文字、order balance check 在当前实现下不可达，success 判定也有重复。 | 最终实现删除 bundle-level git commit、重命名 prompt shape 说明字段、提升 schema version；移除不可能触发的 ordering balance check，合并重复 success predicate，并把 comparison-only dwell 解析放到 single-mode reject 之后以给出准确错误。设计文档补充 buffer overflow 同时 latch failure 和 drop frame、cold/warm delta 只是 preload recoverability upper bound、两个 median 定义并存、compile-cache dir 会记录但 teardown 删除。 | 已更新 [cli-startup-performance.md](../../feature/cli-startup-performance.md)。完整实现见 [implementations/pr-7825.md](implementations/pr-7825.md)。 |
| [#7877](https://github.com/QwenLM/qwen-code/pull/7877) | retrieval-only external context 只能等模型显式调用 MCP，无法在每次用户提交时基于原始 `submitted_prompt` 做确定性召回；团队希望管理员可安装一个只读、短超时、fail-open 的自动 recall profile，同时不让 `@file` 扩展、系统 reminder 或 credential 泄漏给 provider。 | 最终实现新增 Direct External Context Auto Recall profile：管理员通过 `UserPromptSubmit` command hook 安装，且与既有 on-demand MCP profile 互斥；v2 config 增加 `autoRecall.repositoryRoot` 与 `timeoutMs`。hook 只接受 `hook_event_name=UserPromptSubmit` 且存在非空 `submitted_prompt` 的 payload，真实 `cwd` 必须是配置 repo root 的 realpath 子孙；query sanitizer 去掉常见 secret、限制 512 code point，provider 调用最多一次、默认 1500ms、无 retry/cache。结果最多 5 条、content 单项 1000 code point、序列化 4000 code unit，作为 `additionalContext` 追加到用户层 untrusted JSON envelope，不提升为 system 指令。 | 已更新 [external-context-provider.md](../../feature/external-context-provider.md) 与 [hooks.md](../../feature/hooks.md)。完整实现见 [implementations/pr-7877.md](implementations/pr-7877.md)。 |
| [#7886](https://github.com/QwenLM/qwen-code/pull/7886) | session writer lease 开启后，把 transcript `birthtime`/`ctime`/`mtime` 作为强完整性信号会在 virtiofs、mtime 抖动或平台 timestamp drift 下误报 `session_transcript_changed`，但又不能放松 symlink/identity/owner/tail 等真正会导致双写的边界。 | 最终实现将 timestamp 变成 advisory：file existence、length、device/inode、file type、mode、owner/group、link count 与 tail validity 仍 fail-closed。获取已有 transcript 时建立一次 streaming SHA-256 baseline；普通 append 增量推进 digest。若只出现 timestamp drift，进入全内容 reconciliation，带 pre/post handle/path 与 exact-owner 校验，最多重试 3 次；仍不稳定则 `session_writer_unavailable`。shutdown release 会在 pending lease activation 前启动 exact-record release，长 baseline read 每 1 MiB chunk 检查 release，避免 managed shutdown 被 snapshot 阻塞。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md) 与 [daemon-serve-mode/12-daemon-sdk-reliability-audit.md](../../feature/daemon-serve-mode/12-daemon-sdk-reliability-audit.md)。完整实现见 [implementations/pr-7886.md](implementations/pr-7886.md)。 |
| [#7894](https://github.com/QwenLM/qwen-code/pull/7894) | session writer lease 只有所有并发 writer 都参与时才安全；如果在混合版本、混合配置或未重启的 ACP/daemon 里默认开启，会出现一部分进程用 lease、一部分仍旧直接写 transcript 的危险 rollout。 | 最终实现增加 restart-required 的 `experimental.sessionWriterLease` opt-in，默认 `false`；只有设置值是 boolean `true` 且会话运行在 ACP/daemon 路径时才启用 lease，interactive/headless 继续 legacy recorder。ACP 从 bootstrap config 固定一次 effective gate，并在该 ACP 进程服务的所有 session 中复用，避免 per-session reload 把同一进程变成混合 writer。settings 文档、UI schema、生成 schema 与 config/core/ACP 测试同步覆盖。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md)。完整实现见 [implementations/pr-7894.md](implementations/pr-7894.md)。 |
| [#7921](https://github.com/QwenLM/qwen-code/pull/7921) | ARMS Session Analysis 需要 span 级 end-user identifier 才能按用户聚合会话，但现有 telemetry 只提供 session/operation/provider 维度，无法把 pseudonymous user id 写入 GenAI span；同时不能自动推断真实身份或污染 Resource/log/metric。 | 最终实现新增 `telemetry.userId` 设置与 `QWEN_TELEMETRY_USER_ID` 环境变量，env trim 后优先，空 env 回退 settings，非 string setting 直接配置失败。`Config.getTelemetryUserId()` 供 telemetry 使用，并在 interaction span 创建时写 `gen_ai.user.id`，随后传播到 LLM、Tool、Agent spans，包括 tool-result continuation 与 linked-root background agents。该字段不写 Resource/log/metric，不写 baggage，也不别名到 `enduser.id` / `user.id`。 | 已更新 [telemetry-observability/](../../feature/telemetry-observability/)。完整实现见 [implementations/pr-7921.md](implementations/pr-7921.md)。 |
| [#7947](https://github.com/QwenLM/qwen-code/pull/7947) | Core 已支持大文本按行范围读取，但 Serve workspace `/file` 在行切片前先用 256 KiB 大小门拒绝，导致 `line=1&limit=20` 这类 bounded 读取也返回 `file_too_large`，Web/SDK 无法查看大型日志局部。 | 最终实现允许 Serve 对超过 256 KiB 的 UTF-8 文本做有界行窗口读取。large-window path 从验证到 binary probe 到 streaming read 复用 caller-owned fd，仍保留 256 KiB 输出 cap，并在读后重查 path/inode；full snapshot、edit、hash 与 optimistic locking 仍维持旧大小门。大文件 partial response 报完整 file size 与 `truncated:true`，不返回 full-file hash，只有读到 EOF 才返回 original line count；binary、unsupported non-UTF-8、symlink swap 或 read 中变更继续拒绝。 | 已更新 [file-reading.md](../../feature/file-reading.md) 与 [daemon-serve-mode/05-workspace-files-and-fs-boundary.md](../../feature/daemon-serve-mode/05-workspace-files-and-fs-boundary.md)。完整实现见 [implementations/pr-7947.md](implementations/pr-7947.md)。 |
| [#7967](https://github.com/QwenLM/qwen-code/pull/7967) | #7947 引入了 path-read 与 handle-read 两条相近 helper，`fileHandle`、`forceStreaming`、`maxScanBytes` 等参数混在同一个入口里，容易让 caller-owned fd 被误关、让 streaming 规则分叉，也让后续 byte-cursor paging 难以复用同一 descriptor。 | 最终实现是在 #7947/#8002 合并后折叠为较窄的 descriptor-threading follow-up：保留 `readTextRangeFromHandle()` 作为 caller-owned fd 入口，补 route/service 层测试锁定 Infinity、unlimited limit、read-to-EOF 与 stat-size bound 等边界，并清理 `fileUtils.ts` 中仍可能误走 path 的死分支。 | 已更新 [file-reading.md](../../feature/file-reading.md) 与 [daemon-serve-mode/05-workspace-files-and-fs-boundary.md](../../feature/daemon-serve-mode/05-workspace-files-and-fs-boundary.md)。完整实现见 [implementations/pr-7967.md](implementations/pr-7967.md)。 |
| [#7975](https://github.com/QwenLM/qwen-code/pull/7975) | daemon session archive/delete/unarchive、scheduled cleanup、keepalive late-spawn cleanup 与 ACP orphan cleanup 这类维护任务会修改 transcript 或 session state；若它们不绑定正确 workspace runtime 和 writer lease，可能跨 runtime 写错存储根，或在 shutdown/replacement 时与 live writer 交叉。 | 最终实现让每个 workspace runtime pin 自己的 absolute session runtime root，并通过 `runWithWorkspaceRuntimeStorage()` / `createWorkspaceRuntimeSessionService()` 在选中 runtime 内执行维护。所有会改 transcript 的维护路径先获取 daemon writer lease；batch 任务保留已完成结果，后续冲突只影响对应 session。shutdown 会 seal maintenance admission、等待已准入维护 lease，并把 REST/ACP 错误映射成 typed `daemon_draining`。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md) 与 [daemon-serve-mode/12-daemon-sdk-reliability-audit.md](../../feature/daemon-serve-mode/12-daemon-sdk-reliability-audit.md)。完整实现见 [implementations/pr-7975.md](implementations/pr-7975.md)。 |
| [#7976](https://github.com/QwenLM/qwen-code/pull/7976) | #7812 能在 cooperative shutdown 释放 exact-owned writer lock，但 managed daemon replacement 仍缺少“旧 writer 已完整 flush 且新 writer 可安全接手”的可证明交接；直接 reclaim active lock 有双写风险，不接手又会留下冲突。 | 最终实现引入 schema v2 writer lock sealed state。managed shutdown 在 durable drain 后可把 active lock seal 为带 transcript path、存在性、长度与 SHA-256 的 handoff proof；trusted managed replacement 只接受 sealed lock，先打开并 hash expected transcript，再用固定 claim 文件保护 sealed→active 转换，revalidate primary record、descriptor、path、metadata、length 与 digest 后才安装新 active owner。未 sealed 的 active lock、proof mismatch、symlink/truncate、残留 claim 或竞争失败都 fail closed。 | 已更新 [daemon-serve-mode/03-session-lifecycle.md](../../feature/daemon-serve-mode/03-session-lifecycle.md) 与 [daemon-serve-mode/12-daemon-sdk-reliability-audit.md](../../feature/daemon-serve-mode/12-daemon-sdk-reliability-audit.md)。完整实现见 [implementations/pr-7976.md](implementations/pr-7976.md)。 |
| [#7994](https://github.com/QwenLM/qwen-code/pull/7994) | #7761 benchmark 能量到 prompt 到 provider / first output，但还缺少“prompt 被 daemon 接受、用户 echo 出现、provider request 到达”之间的细分，无法判断首 prompt 延迟是 queue wait、HTTP/SSE echo、还是 provider dispatch 造成。 | 最终实现把 first-output artifact 升到 schema v2，增加 `promptAcceptedAt`、`userEchoAt`、`daemonPromptQueueWaitMs`、`promptToAcceptanceMs`、`acceptanceToProviderRequestArrivalMs`、`promptToUserEchoMs`、`userEchoToProviderRequestArrivalMs` 等阶段。tracker 只用 accepted top-level prompt id 关联 relayed `user_message_chunk`，排除 transcript replay，并允许 provider request 比 HTTP 202/SSE echo 更早到达时出现 signed negative delta。 | 已更新 [cli-startup-performance.md](../../feature/cli-startup-performance.md)。完整实现见 [implementations/pr-7994.md](implementations/pr-7994.md)。 |
| [#8002](https://github.com/QwenLM/qwen-code/pull/8002) | #7947 只能读大文本的一个窗口；客户端翻页时仍需要从文件开头重新扫描到目标行，超大日志按行翻页成本高，也无法用协议明确表达“还有下一页”。 | 最终实现为 workspace text read 增加 opaque byte cursor。`GET /file` 在 line/limit/maxBytes 响应里返回 `hasMore` 与 `nextCursor`；下一次带 `cursor` 直接从下一行字节位置继续，禁止与 `line` 同时使用。cursor 绑定 path/snapshot/descriptor，替换、截断或 snapshot mismatch 返回 `hash_mismatch`；输出仍保留 256 KiB cap、8 MiB line-offset scan cap、UTF-8/BOM/CRLF metadata 与 invalid cursor `parse_error`。TS SDK 与 daemon MCP 同步暴露 request cursor 和 response paging fields。 | 已更新 [file-reading.md](../../feature/file-reading.md)、[daemon-serve-mode/05-workspace-files-and-fs-boundary.md](../../feature/daemon-serve-mode/05-workspace-files-and-fs-boundary.md)、[daemon-serve-mode/10-client-adapters-and-sdk.md](../../feature/daemon-serve-mode/10-client-adapters-and-sdk.md) 与 [sdk.md](../../feature/sdk.md)。完整实现见 [implementations/pr-8002.md](implementations/pr-8002.md)。 |
| [#8080](https://github.com/QwenLM/qwen-code/pull/8080) | workspace status polling 会在每次读取时触发 extension discovery、settings load 和完整 skill cache refresh，导致后台 dashboard/IDE 轮询把 11 个 extension skill 目录重复扫描数千次，并产生成十万级 `SKILL_LOAD` 日志。 | 最终实现把 workspace skill status read 改成副作用隔离的 read model：ACP child 只返回已提交的 in-memory skill snapshot，settings/content/extension mutation 走显式 refresh reason；daemon 侧对 workspace skill status 做 5s TTL、generation guard、single-flight 与 mutation 前后 invalidation。workspace skill HTTP 响应支持 `ETag` / `If-None-Match` 和 CORS exposure；extension source revalidation 只做轻量 fingerprint 检查，必要时刷新一次，并 coalesce ACP child extension refresh / bootstrap refresh。 | 已更新 [daemon-serve-mode/08-extension-endpoints.md](../../feature/daemon-serve-mode/08-extension-endpoints.md) 与 daemon feature。完整实现见 [implementations/pr-8080.md](implementations/pr-8080.md)。 |
| [#8093](https://github.com/QwenLM/qwen-code/pull/8093) | daemon 目前主要限制 workspace/session 数量，无法限制 queued bytes、process output buffer、bulk 操作并发、completion reserve 或 emergency response pool；多 workspace 下单个 workspace 可以通过大批量任务挤占进程资源。 | 关闭前 draft diff 新增资源预算 foundation，但不接生产路由、不广告 capability、不改变 daemon 行为。它提供同步 process-wide `ResourceBudget`、composite atomic admission、category cap、normal/completion reserve、emergency pool、lease split/grow/shrink/transfer/release；同时提供 bulk/spawn/process 三条 fair scheduler lane，按 global/workspace active/wait limit、公平轮转、abort、timeout、seal 与 nested-operation 拒绝约束重活；buffered process runner 在进入 process lane 前先预留输出 buffer 容量并在所有完成路径释放。 | 已新增 [daemon-serve-mode/13-resource-budgeting.md](../../feature/daemon-serve-mode/13-resource-budgeting.md) 并更新 daemon feature。关闭前观察见 [implementations/pr-8093.md](implementations/pr-8093.md)。 |
| [#8150](https://github.com/QwenLM/qwen-code/pull/8150) | 现有 LLM span 上的私有 `ttft_ms` 是毫秒单位、首个用户可见输出语义，不能等同于 OTel GenAI v1.41 的 `gen_ai.response.time_to_first_chunk`；继续用私有字段做标准 first-token 查询会混淆 first chunk 与 first visible output。 | 最终实现为流式 LLM span 写标准 `gen_ai.request.stream=true` 与 `gen_ai.response.time_to_first_chunk`（秒）。计时从 wrapped provider invocation 前的 monotonic `performance.now()` 到第一个 normalized `GenerateContentResponse`；metadata-only / usage-only normalized response 也算 chunk。非流式请求保留 `llm_request.stream=false` 并省略标准 streaming 字段；私有 span-level `ttft_ms` 被移除，内部 `ttftMs` 仍用于 API response logs、`sampling_ms`、吞吐和 breakdown metric。 | 已更新 [telemetry-observability/](../../feature/telemetry-observability/) 与 [telemetry-observability/06-genai-ttft-retry-and-metrics.md](../../feature/telemetry-observability/06-genai-ttft-retry-and-metrics.md)。完整实现见 [implementations/pr-8150.md](implementations/pr-8150.md)。 |
| [#8176](https://github.com/QwenLM/qwen-code/pull/8176) | Core 与 ACP 通过不同路径产生 tool-call terminal event，`status`、兼容 `success`、`error`、`error_type` 可能互相矛盾或缺失；未分类 soft error 会落到空 bucket，日志、metrics、recording 与 UI telemetry 对同一次工具调用的结果解释不一致。 | 最终实现新增统一 terminal normalization boundary：所有 tool-call event 在 fan-out 到 UI telemetry、chat recording、QwenLogger、OTLP logs 与 metrics 前先归一化。authoritative `status` 派生兼容 `success`，success/cancelled 事件清掉 stale error 字段，未分类 core soft error 归为 `unknown`，空工具名归为 `unknown_tool`。tool-call counter 增加低基数 `status` 维度，QwenLogger 增加 `status` 与 `tool_type`，但不采集 tool arguments、results、stack trace 或 MCP server name。 | 已更新 [telemetry-observability/](../../feature/telemetry-observability/) 与 [telemetry-observability/06-genai-ttft-retry-and-metrics.md](../../feature/telemetry-observability/06-genai-ttft-retry-and-metrics.md) 的月度覆盖口径。完整实现见 [implementations/pr-8176.md](implementations/pr-8176.md)。 |
| [#8180](https://github.com/QwenLM/qwen-code/pull/8180) | terminal `status` 只能说明整个 tool call 最终结果，无法区分 validation/permission/PreToolUse blocking 等未进入 `invocation.execute()` 的失败，和工具实现真正执行后失败/取消；因此观测到的工具失败率无法判断 executable boundary 是否健康。 | 最终实现是在 terminal status 之外新增 execution-specific outcome。Core scheduler 与 ACP tool call 共用状态契约：validation、permission deny、PreToolUse blocking、host invocation guard、duplicate call 等 synthetic pre-execution 响应标记为 not started；真正进入 `invocation.execute()` 后冻结 execution outcome，并穿过 hooks、result persistence、recording、image bridging 与 batch post-processing。telemetry normalization 增加 call id、execution status、execution child spans 和低基数 execution-outcome counter；user-facing TUI/JSON 协议保持兼容。 | 已更新 [telemetry-observability/](../../feature/telemetry-observability/)。完整实现见 [implementations/pr-8180.md](implementations/pr-8180.md)。 |
| [#8206](https://github.com/QwenLM/qwen-code/pull/8206) | external-context workspace 的生产 audit 命中 MCP SDK、Hono server、body parser 与 URI parser dependency path 的 advisories；需要修复 direct external-context MCP profile 的依赖安全性，但该 PR 自身不能把 mobile MCP package 强行迁移到新的 runtime baseline。 | 最终实现将 direct external-context integration 升到 MCP SDK 1.30.0，并只对 MCP SDK 1.30+ 的 consumer 选择 patched Hono 2 line；同时刷新 request/body/URI parsing 相关传递依赖并重新生成 third-party notices。mobile MCP 不在 #8206 内迁移；它的 Node.js 22 / Hono 2 迁移另由 #8311 记录。 | 已更新 [external-context-provider.md](../../feature/external-context-provider.md)。完整实现见 [implementations/pr-8206.md](implementations/pr-8206.md)。 |
| [#8245](https://github.com/QwenLM/qwen-code/pull/8245) | daemon 已每 5 秒采样自身 RSS/heap 和 primary ACP child RSS，但没有 cgroup/host/heap/budget denominator，`/daemon/status` 只能报告绝对字节数，无法回答“离内存耗尽还有多近”，后续资源预算和 admission work 也没有统一容量基线。 | 最终实现是在 daemon boot 时解析一组 memory budget figures 并通过 status/protocol/SDK/docs 报告。configured 与 effective budget 分离，effective 会被 cgroup/host memory cap 截断；derived budget 低于 documented minimum 时报告 `insufficientMemory` 而不是向上 clamp。该 PR 不改变 ACP child spawn argv，不实施 enforcement；wire 上显式 `enforced:false`，derived quantities 放在 `modeled` object，per-child shares 标为 advisory。新增 `daemon-memory-budget.ts`、serve status wiring、config/docs/protocol/SDK 类型与测试。 | 已更新 [daemon-serve-mode/13-resource-budgeting.md](../../feature/daemon-serve-mode/13-resource-budgeting.md) 与 daemon feature。完整实现见 [implementations/pr-8245.md](implementations/pr-8245.md)。 |
| [#8311](https://github.com/QwenLM/qwen-code/pull/8311) | 独立发布的 `@qwen-code/mobile-mcp` 仍声明 Node.js 18 兼容，但仓库和发布流程已使用 Node.js 22；旧 MCP SDK 1.26 会把 Hono Node Server 锁在存在 GHSA-frvp-7c67-39w9 advisory 的 1.x 路径上，仅升级 SDK 也可能让既有 lockfile 保留 Hono 1.x。 | 关闭前 diff 将 mobile-mcp 的 runtime baseline 提升到 Node.js 22，升级 `@modelcontextprotocol/sdk` 到 1.30.0，并直接依赖 `@hono/node-server` `^2.0.12` 作为“永不解析到 1.x”的 resolution floor；根 lockfile 与 package lockfile 同步移除嵌套旧 SDK/Hono tree。`src/server.ts` 增加 type-only `sdkInputSchema()` 边界，覆盖两个 `inputSchema` 注册点，用于工作区 split-Zod 布局；独立发布图仍是单 Zod。设计文档、README、CHANGELOG 与 vendored patch 记录 Node 22 breaking change、release bump 和 CI/Zod 后续项。 | 已新增 [mobile-mcp.md](../../feature/mobile-mcp.md)。关闭前观察见 [implementations/pr-8311.md](implementations/pr-8311.md)。 |
| [#8352](https://github.com/QwenLM/qwen-code/pull/8352) | Auto Recall Hook 在 provider timeout 后只 abort 请求但没有销毁 `EnvHttpProxyAgent` 持有的 CONNECT socket，可能已经输出 `{}` 但 Node child 仍超过外层 8000ms command-Hook timeout；同批文档还把 v1/v2 config entrypoint 与 timeout 归属写得不准确。 | 最终实现让一次性 Hook 安装并持有自己的 environment-aware proxy dispatcher，在 provider construction/search/rendering 之后通过 `finally` await `dispatcher.destroy()`；成功、空结果、provider failure 和 render failure 都会释放，early no-op / v1 config / cwd mismatch / empty query 在安装 proxy 前返回。长驻 MCP 入口仍安装一个 process-lifetime dispatcher，不按请求销毁。测试新增黑洞 CONNECT proxy 子进程回归、proxy mock cleanup 断言，并修正文档：shared loader 接受 v1/v2，MCP entrypoint 只接受 v1，Hook 只接受 v2，Hook 请求 timeout 只由 `autoRecall.timeoutMs` 控制。 | 已更新 [external-context-provider.md](../../feature/external-context-provider.md)。完整实现见 [implementations/pr-8352.md](implementations/pr-8352.md)。 |
| [#8383](https://github.com/QwenLM/qwen-code/pull/8383) | Serve `readText` 的 `meta.lineEnding` 应描述整个文件，但分页和 byte truncation 路径会在即将返回的 slice 上重新检测；单行 CRLF slice 因 `\n` 被作为 terminator 消耗，只剩 `\r`，第一页可能报告 `lf`，下一页报告 `crlf`，客户端若信任第一页 metadata 会把 CRLF 文件静默保存成 LF。 | 最终实现把 `lineEnding` 改为在完整 decoded file 上只检测一次，slice/truncation 不再重算；新增 CRLF 分页回归测试，读取 `limit:1` 后沿 `nextCursor` 取第二页，断言两页都报告 `crlf`。改动只触及 `workspace-file-system.ts` 与对应测试，保持 content slice、cursor、hash、encoding/BOM/size metadata 和 truncation 行为不变。 | 已更新 [file-reading.md](../../feature/file-reading.md) 与 daemon FS feature。完整实现见 [implementations/pr-8383.md](implementations/pr-8383.md)。 |

## PR 对应 feature 覆盖

| feature 文档 | 本周新增/复核 PR | 文档动作 |
|---|---|---|
| [cli-startup-performance.md](../../feature/cli-startup-performance.md) | #7761 / #7767 / #7820 / #7825 / #7994 | 补 daemon/ACP first-output latency benchmark 的阶段边界、artifact、paired comparison、schema follow-up、provider preload 与 immediate prompt dispatch 分段指标，并把 #7994 更新为 merged final。 |
| [hooks.md](../../feature/hooks.md) | #7762 / #7877 | 补 `submitted_prompt` provenance 对 auto recall hook 的输入边界，记录 `additionalContext` 用户层追加与 hook profile 互斥关系，并把 #7877 更新为 merged final。 |
| [daemon-serve-mode/](../../feature/daemon-serve-mode/) | #7761 / #7767 / #7812 / #7821 / #7886 / #7894 / #7947 / #7967 / #7975 / #7976 / #7994 / #8002 / #8080 / #8093(closed draft) / #8245 / #8383 | 补 provider preload、writer lease opt-in、managed shutdown、timestamp drift reconciliation、Todo Guard continuation、Serve large-text bounded read、borrowed fd text range refactor、maintenance writer isolation、certified handoff、dispatch metrics、byte-cursor paging、workspace skill status read model、resource budgeting foundations、daemon memory budget reporting 与 workspace text `lineEnding` metadata 修复对 daemon/session/filesystem/extension/status surface 的影响。 |
| [telemetry-observability/](../../feature/telemetry-observability/) | #7921 / #8150 / #8176 / #8180 | 补 `telemetry.userId` / `QWEN_TELEMETRY_USER_ID` 到 span-level `gen_ai.user.id` 的 ARMS Session Analysis 实现与限制，记录 #8150 对标准 streaming first-chunk span 字段和私有 `ttft_ms` 迁移的影响，并补 tool-call terminal status / execution outcome telemetry 口径。 |
| [file-reading.md](../../feature/file-reading.md) | #7947 / #7967 / #8002 / #8383 | 补 Serve workspace filesystem 对大型 UTF-8 文本的 bounded line-window read、borrowed fd range reader 分层、byte-cursor paging，以及 #8383 merged diff 对 `lineEnding` 文件级 metadata 的一致性修复。 |
| [sdk.md](../../feature/sdk.md) / [daemon-serve-mode/10-client-adapters-and-sdk.md](../../feature/daemon-serve-mode/10-client-adapters-and-sdk.md) | #8002 | 补 TS daemon client 与 daemon MCP 的 workspace file paging additive protocol fields，并把 #8002 更新为 merged final。 |
| [external-context-provider.md](../../feature/external-context-provider.md) | #7586 / #7877 / #8206 / #8352 | 补 retrieval-only MCP 与 auto recall 的最终状态，记录 #8206 对 direct external-context MCP SDK / Hono / parser dependency hardening 的 scoped boundary，并补 #8352 对一次性 Auto Recall Hook proxy dispatcher lifecycle 与 v1/v2 entrypoint 文档的修正。 |
| [mobile-mcp.md](../../feature/mobile-mcp.md) | #8311(closed) | 记录 closed mobile-mcp Node.js 22 runtime baseline、MCP SDK 1.30.0 / Hono 2 dependency hardening、split-Zod type bridge、release/CI follow-up 边界。 |

_最终版按个人 PR 口径更新于 2026-08-20_
